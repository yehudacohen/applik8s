# Applik8s v0.8 goal prompt

Copy the text below into a dedicated Codex task when starting or resuming the
implementation goal. The prompt deliberately delegates detail to the checked-in
execution plan and registries instead of duplicating a second, drift-prone plan.

```text
Continue the Applik8s v0.8 implementation to completion in:

  /Users/yehudac/workspace/applik8s-v08-rfps

Use branch `codex/v0.8-rfps`. The authoritative contract and implementation order
is `docs/v0.8-execution-plan.md`, together with the v0.8 manifesto, accepted v0.8
RFPs, scorecard, acceptance manifest, target-compatibility matrix, reconciliation
ledger, format registry, admission matrix, scheduling matrix, Runtime Integrity
source inventory, and checked-in evidence packets. Read the active goal objective
at the start of every continuation. Do not rely on remembered phase state.

First follow the plan's Start and resume protocol:

1. Inspect the exact branch, dirty worktree, recent commits, running processes,
   execution-plan Current execution cursor, and reconciliation ledger.
2. Preserve every intentional edit. Do not reset, overwrite, merge a stale branch
   wholesale, or port patched dist files.
3. Verify the current bounded cutover from source and its smallest regression.
   Finish, checkpoint, or truthfully block it before opening another writer,
   migration, or execution-family cutover.
4. Update the relevant machine-readable registry and evidence record whenever a
   state changes. Never replay a completed slice merely because its phase appears
   earlier in the plan.
5. Ensure the active evidence row names its implementation owner, review owner,
   relative size, uncertainty, and one exact next command. Split an `L` package
   into reviewable findings before implementation rather than producing a
   package-wide mega-diff.

Current resume facts, which must still be verified against the worktree:

- portable callable-provider operation metadata and generated stream-processor
  hydration are checkpointed at implementation commit `bb7be3e` and evidence
  commit `2d1e3c2`;
- the canonical Release-A actor-turn authority envelope, legacy normalization,
  focused runtime export, celld hydration, regressions, format/admission registry,
  and source-gate updates are checkpointed at `77291ee`;
- `AC-1` is checkpointed at `b076ee7`: bounded actor execution principals,
  handler-scoped workload envelopes, stable nested-call idempotency and narrowed
  authority, future-bounded alarm authority, causal-principal preservation,
  command-authorized transactional cancellation, cancellation fencing, redacted
  telemetry, and retained Release-A recovery were checkpointed after the original
  117-test actor contract; the current expanded actor contract passes 133 tests
  together with Runtime Integrity, packed-consumer, and workspace typecheck gates;
- actor callable-provider integration and the complete bounded `AC-2` provider
  packet are checkpointed through `b13b953`: generated distributed celld and
  TypeKro/Alchemy Kubernetes lifecycles prove state, receipts, alarms, pod/node
  loss, rollout, resource-loss drift repair, signed realtime admission,
  hibernatable WebSockets, reconnection, lease convergence, and leak-free teardown;
- workflow/task callable providers are integrated through `ee2435b`, `44e9fa6`,
  and `f388883`: a clean external provider supports direct, extracted, and
  helper-mediated durable calls; generated workers hydrate one public static
  operation; framework-managed profile configuration, Secret references,
  readiness, access, network, and consumer-only placement are preserved; negative
  bindings fail closed; and independent recompilation is deterministic;
- typed HTTP callable providers are integrated at `07e3ef3`: a clean external
  provider passes direct, extracted, and helper-mediated calls, public static
  runtime import, generic configuration, Secret/readiness projection, exact
  consumer-only access/network placement, deterministic recompilation, packed
  consumption, and fail-closed missing or placement-only runtime metadata;
- `44042b9` gives the aggregate target-parity gate its required 8 GiB heap. The
  gate reaches its real remaining failure: 85/86 tests pass, and only Chirp's
  second-precision workflow-start schedules are incompatible with EventBridge's
  minute precision. Do not weaken scheduling semantics to make that target green;
- maintained Billing provider operations and explicit object-store placement
  provenance are checkpointed at `26186fb`; Billing now exposes exact public
  static runtime operations while a resource-backed object-store placement is
  explicitly distinguishable from a callable provider leaf;
- agent callable-provider lowering is checkpointed at `39e8f74`: direct,
  extracted, and helper-mediated calls from a clean external package hydrate
  only the declared public operation, preserve generic configuration,
  Secret/readiness, exact access/network placement, deterministic output, and
  fail-closed missing or placement-only operation metadata;
- `bun run check:package-consumer` now passes all 59 packed packages, 105 public
  entrypoints, external workflow/HTTP/schedule/agent/reactive provider proofs,
  clean CLI generation, and complete Agentic Start discovery, compilation,
  browser, and server builds; the current checkpoint also passes the 61-package,
  four-script-shard, and root-config typecheck;
- `GP-1` canonical graph and ApplicationPlan qualification is integrated at
  `0dee112`: plans use Canonical JSON v1 and a declared source-graph version,
  provider guarantees follow profile/target selection and aliases, semantic
  diffs classify create/update/replace/delete/no-op changes, incompatible plan
  versions fail closed, and sensitive/non-canonical values are rejected. The
  exact graph-provenance gate passes 66/66, the ApplicationPlan gate passes
  48/48 across core, compiler, CLI, GuestBook, Chirp, and Agentic Start, clean
  packed consumption builds 59 packages and imports 105 public entrypoints, and
  workspace typecheck covers 61 packages, four script shards, and root configs;
- `RA-1a` exact semantic policy is checkpointed at `f6649ae`: runtime-access
  plans retain exact requirements, graph/policy digests, source provenance,
  collision-resistant workload identities, namespaced/cross-namespace/cluster
  Kubernetes scopes, named Secret access, explicit-access diagnostics, wildcard
  rejection, and selected provider-guarantee intersection. Deterministic gates
  are green, but local child processes still inherit ambient `process.env` and
  physical policy enforcement/live narrowing evidence remains open;
- `RA-1b1` declared host-environment isolation is checkpointed at `d6a0bf5`:
  local and production targets consume one compiler-derived generated-secret
  contract; local plans retain only declared variable names and Secret
  identities; a closed child-process allowlist prevents ambient `.env` leakage;
  and exact consumer placement, non-persistence, a real process canary, the
  123-test runtime-access gate, the 24-test local gate, and full workspace
  typecheck are green. Framework-generated credentials remain over-projected
  inside that closed boundary and are the next runtime-access cutover;
- default/unqualified ApplicationHost schedule-control callable providers are
  integrated at `ae04dab`: a clean packed external provider passes direct,
  extracted, and helper-mediated calls, exact public runtime import, deterministic
  generation, exact access, generic configuration and one Secret binding on the
  host, and fail-closed missing or operation-less metadata;
- the qualified hosted-scheduler implementation is checkpointed at `44d32d8`
  and `550f6d9`. It contains a provider-neutral PostgreSQL occurrence authority, qualified
  Hatchet projection/runtime, exact scheduler identity, ApplicationHost
  execution-boundary hydration, compiler/deployment/local-supervisor lowering,
  and clean external callable-provider evidence. Do not open another execution
  family until its ledger and scheduling-matrix rows are reconciled;
- reactive callback qualification is checkpointed at `9525a04`:
  `Stream.onEvent(...)`, `Stream.onBatch(...)`, and
  `Model.on.create/update/delete(...)` share one generated stream-processor
  boundary; direct, extracted, helper-mediated, and clean packed external-provider
  calls retain their exact public runtime operation, profile configuration,
  Secret projection, and least-privilege access on consuming workers only;
  function-native projection transforms fail closed at registration when they
  are asynchronous or recursively capture provider/effect handles;
- transaction-local and componentized-WASM provider boundaries are checkpointed
  at `8543138`: `beforeCommit(...)` rejects recursively captured external
  provider operations while retaining staged commands/events, and
  `Resource.on.reconcile(...)` rejects recursively captured provider operations
  with a stable host-mediated-bridge diagnostic. The clean packed-consumer proof
  covers both negative boundaries. Do not embed Node provider runtimes, provider
  credentials, or ambient environment access into serialized WASM closures;
- the focused scheduling gate passes 165 tests, provider conformance passes 37,
  packed consumption passes 59 packages and 102 public entrypoints, and the
  OrbStack scheduling receipt suite passes three live cases with verified cleanup,
  including terminal-failure redelivery without a duplicate effect. The full
  workspace typecheck passes 61 packages, four script shards, and root configs;
  changed-file error checks, documentation consistency, and diff checks pass.
  Branch-wide lint remains red on the recorded pre-existing typecast/warning
  baseline. These are resume facts to revalidate, not a substitute for the
  exact-commit evidence packet;
- preserve the canonical schedule graph and durable occurrence record as the
  authority. Hatchet is a convergent delivery projection into canonical
  Applik8s occurrence admission, not an alternate application execution model.
  A generated delivery worker must contain transport/admission machinery only;
  callable provider operations and their credentials remain on the generated
  application execution boundary;
- the current branch is ahead of the remote. A prior push was blocked by source-
  egress approval. Preserve the local commits and continue additive work; do not
  rewrite history or retry external source egress without explicit authority.

The callable-provider inventory is complete. Keep `beforeCommit(...)`
transaction-local and preserve its fail-closed external-effect rule. Treat
`Resource.on.reconcile(...)` as effect-permitted in the language model but do not
claim provider support until a host-mediated runtime-access bridge can hydrate a
declared public operation with exact credentials and least-privilege access. That
bridge follows `GP-1` and `RA-1`; embedding Node modules or ambient credentials in
WASM is not an interim solution.

`RI-1a` is checkpointed at `ff68f62`, `RI-1b` is checkpointed at `363d002`,
search canonical-value convergence is checkpointed at `87c3a3c`, and request
ingress admission parity is checkpointed at `f7d0df1`.
Application graph, deployment contract, compiler runtime-identity/payload-schema,
browser query-key bytes, and deterministic/PostgreSQL/OpenSearch search digest
inputs now use named Canonical JSON v1 policies with exact
retained fixtures. TypeKro/Alchemy planning proxies cross an explicit evidence
adapter. The focused `@applik8s/core/canonical-json` subpath remains portable and
browser-safe without pulling the core application barrel into operator or browser
bundles; clean packed consumption imports 104 public entrypoints and completes
Agentic Start browser/server builds. The search providers retain provider-specific
offset versus ordered-value continuation and an explicit Release-A legacy Date
digest reader. Command, query, stream/SSE, and Kubernetes request ingress now use
one trace-aware canonical constructor and expose only immutable narrowed views;
the Runtime Integrity source gate rejects bypasses. Workflow/task delivery
admission is checkpointed at `77bb297`: generated workers report bounded,
redacted canonical observations, arbitrary gateway error messages are excluded,
and reattached cancellation preserves one run and one effect. Broker and
verified-webhook admission parity is checkpointed at `8a07641`: both retain
transport-specific proof rules, use the focused `@applik8s/core/admission`
observation contract, reject provenance substitution, and keep sensitive
evidence out of current-state observations. Managed execution admission parity
is now checkpointed at `1dc42f4` as well:
schedule hosts, canonical actor turns and retained alarms, and generated agent
workers all use the focused bounded observation contract without replacing
occurrence, actor-state, or AI-attempt authority. Request observation parity is
checkpointed at `7c3ff6f`: command, query, stream/SSE, Kubernetes, object-storage,
and agent gateways use one framework-owned non-interfering observer, and generated
hosts persist only sampled current state without raw request or exception evidence.
Resume at `RA-1b2`: `RA-1a` is checkpointed at `f6649ae` and declared
host-environment isolation at `d6a0bf5`. Inventory the generated runtime families
and replace the local supervisor's unconditional framework-credential projection
with exact per-execution requirements and sibling-process denial evidence. Then
close policy ceilings, Kubernetes/AWS materialization, host preflight/denial,
live narrowing/drift, and provider-limit evidence. Do not introduce a second
policy model or claim live enforcement from deterministic plan evidence.

Then continue through the plan's reviewable work packages and dependency graph:

- close remaining `RI-1`, `RI-2`, and `RI-3` cohorts without reimplementing
  completed canonical JSON, signed-envelope, or admission work;
- complete `PI-1` callable providers one execution family at a time;
- complete `SC-1` portable scheduling semantics before `SC-2` provider claims;
- complete `AC-1` actor authority before `AC-2` provider qualification;
- deliver `GP-1`, `RA-1`, `LR-1`, `OB-1`, `LH-1`, `TP-1`, `DX-1`, `MS-1`, and
  `PC-1` as independently reviewable packages rather than one compiler diff;
- qualify GuestBook, Chirp, and Agentic Start through `PQ-1`; and
- assemble, but do not publish, the exact-candidate `RQ-1` maintainer packet.

Treat the execution plan's Program completion contract as the release definition.
In particular, prove one authored graph, one registered integrity foundation, one
canonical admission model, framework-managed callable dependencies, explicit time
semantics, durable distributed actors, truthful target guarantees, a cleaner
authored experience, restart-safe migration, and a reconstructible requirement-
to-evidence join.

Required engineering discipline:

- Canonical JSON bytes precede signatures and digests. Signed envelopes precede
  admission-token migration. Canonical admission precedes new ingress lowerings.
- Execute Runtime Integrity in the plan's explicit `RI-1a` through `RI-3c`
  consumer cohorts. Each cohort must close its source-inventory row,
  format-registry phase, differential evidence, compatibility disposition,
  rollback boundary, and removal condition before another cohort changes the
  same writer or wire format.
- Share mechanics, not domain meaning. Execution families retain payload,
  authorization, recovery, and lifecycle semantics while consuming the shared
  integrity and admission foundations.
- Keep one shared-foundation migration slice in cutover at a time. Other lanes
  may characterize or add independent evidence but may not introduce a competing
  writer, compatibility shim, serializer, signer, or admission shape.
- Preserve the developer experience. Internal consolidation must not make users
  author serializers, signers, admission wrappers, credentials, target branches,
  provider-runtime imports, or scheduling adapters. Prove direct calls, stable
  extracted operations, module-local helpers, and clean external packages.
- Actor alarms remain actor-owned timers. Processor scheduling remains processor
  lifecycle. Dynamic reconciliation remains desired-instance convergence. Raw
  Kubernetes cron is an explicit infrastructure escape hatch. Converge only the
  schedule semantics that the shared model can preserve exactly.
- Provider lowering preserves authored cadence exactly. Reject an incompatible
  precision, fixed interval, calendar, timezone, overlap, or misfire contract
  before mutation; never approximate it with a cron expression that changes the
  firing semantics.
- Use one semantic authority per capability. Telemetry is evidence, provider
  metadata is not admission, provider process identity is not actor identity, and
  generated target plans are derived from the semantic graph.
- Every public or persisted change records compatibility class, readers, writers,
  lifetime, migration phase, rollback boundary, telemetry, owner, and removal
  condition. Durable mixed-version formats use the plan's three-release rollout
  unless exact lifetime evidence proves a narrower safe migration.
- Characterize generated behavior and add the failing semantic regression before
  large compiler lowering changes. Make every bounded slice reviewable and commit
  it before opening the next cutover.
- At every milestone, write the plan's scope burn-down before opening another
  large slice. Incomplete release requirements stay blocking; claim requirements
  block only their named claim; non-gating improvements may be deferred only when
  no accepted RFP, compatibility statement, maintained acceptance failure, or
  security/authority/durability/lifecycle invariant depends on them.
- Treat estimates as review-surface and uncertainty controls, not fictional
  calendar promises. Re-estimate when characterization discovers a new semantic,
  migration, provider, or environment constraint, and record why.

Evidence rules:

- Use `not-started`, `characterized`, `implemented`, `integrated`,
  `live-qualified`, `externally-blocked`, and `rejected` exactly as defined in the
  plan. Never substitute deterministic tests for live qualification.
- Keep evidence state separate from `release-required`, `claim-required`, and
  `non-gating`. External blockage never waives the requirement.
- Every claimed transition names the exact commit, command, deterministic or
  content-addressed artifact, environment, cleanup result, compatibility state,
  developer-experience fixture, limitation, and blocker.
- Make the requirement-to-evidence join executable: accepted RFP requirement ->
  scorecard pillar -> classified acceptance gate -> work package -> qualifying
  commit -> checked-in packet -> command/artifact/environment/cleanup receipt.
- Run the smallest focused gate first, then affected package, clean-consumer,
  generated-artifact, browser, Docker, OrbStack, MiniStack, migration, security,
  and performance gates in proportion to the claim. A live cleanup leak fails the
  gate.
- AWS-local is API-fidelity evidence only. Do not claim real AWS, CloudWatch,
  Athena, EventBridge Scheduler, IAM, networking, encryption, lifecycle, or cost
  maturity without its exact real-environment receipt.

Scope and authority:

- Continue autonomously for implementation decisions that preserve the accepted
  contract. Ask only before narrowing/removing an accepted stable claim,
  weakening security/authority/tenancy/durability/lifecycle, adopting a
  destructive or non-rollback-safe migration, inventing a genuinely new public
  execution concept, publishing/releasing, or mutating production.
- A missing decision blocks only its dependent lane. Continue independent work.
- v0.8 is an implementation-and-convergence release. Do not add unrelated public
  capability. New API must be required by an accepted RFP or an in-repository
  acceptance failure and must be simpler than the surface it replaces.
- GuestBook, Chirp, and Agentic Start are the only in-repository acceptance apps.
  Downstream products are separate dogfooding consumers. Keep their names,
  schemas, source, migrations, confidential architecture, and product acceptance
  out of this public repository and out of the release gate. Produce only the
  provider-neutral handoff defined by the plan.
- Safely free only Applik8s/TypeKro test resources when required. Use owning
  Applik8s/Alchemy/TypeKro lifecycle teardown and verify absence; raw Kubernetes
  deletion is emergency repair evidence, not the normal path.
- Do not read, print, overwrite, or commit `.env` files or secrets.
- Do not tag, publish packages or images, create a GitHub Release, or deploy to
  production without explicit maintainer authorization.

Stop only for a real external blocker, missing authority, a plan stop-rule, or a
required maintainer decision. Otherwise continue through the dependency graph,
keep all claims truthful, and finish with the Phase 8 exact-commit maintainer
review packet without releasing it.
```
