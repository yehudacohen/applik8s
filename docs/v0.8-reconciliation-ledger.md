# v0.8 reconciliation ledger

**Status:** Active Phase 0–2 evidence

This ledger preserves source fixes across the v0.7.x release line, focused
worktrees, downstream compatibility patches, and the v0.8 implementation. It
contains no downstream product source or confidential product design.

Every row must reach `integrated` or `intentionally rejected with evidence`
before Phase 2 exits. Patched package `dist` files are evidence only; fixes land
in canonical Applik8s source.

## Checkpoint series

The exact checkpoint commits are recorded after the worktree has been sliced and
committed. The branch is pushed before any rebase or history change.

| Slice | Commit | Evidence at checkpoint |
| --- | --- | --- |
| Runtime/application/compiler contracts | `6ed3fe5` | Targeted v0.8 contract suites recorded in the execution log |
| Deployment targets, stateful runtimes, and provider lowering | `cdd3cf8` | Local/AWS-local/TypeKro contract suites recorded in the execution log; actor contracts 109/109 and Docker/celld lifecycle passed; Kubernetes lifecycle was blocked by OrbStack capacity and cleaned 14/14 resources |
| Development environment and product surfaces | `ff4eec1` | Generated product, development environment, and package-boundary contracts captured |
| Program, release manifests, benchmarks, and evidence tooling | `0ba4085` | Documentation consistency and scorecard contract passed; Runtime Integrity characterization gate intentionally red |

## Finding ledger

| Finding | Origin | Canonical source owner | Regression command | v0.7.x disposition | v0.8 disposition | Migration requirement | Removal condition |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Typed HTTP handlers cannot invoke statically declared function-native workflows through an injected provider-neutral handle | focused `codex/v0.7-http-workflow-release` commits `0a5ffd0`, `d8ab26e`; provider/model handle compatibility is isolated at `2274785` on `codex/v0.7-compatibility-fixes` | compiler HTTP discovery/emission, workflow contracts/resources, selected runtime lowering | focused HTTP/workflow suites plus lost-response emitted-container E2E | implemented and focused retry/reattachment tests pass; clean packed-consumer and exact live emitted-worker evidence remain before integrated/live-qualified claims | integrated in canonical v0.8 source at `f303a2c` plus the exact generated-worker regression; all 59 packed packages and 98 public entrypoints pass clean consumption, and a destroyed post-admission response retries with one key, one run, and one effect; real Hatchet lifecycle evidence remains before live qualification | request idempotency and run reattachment must remain compatible across the release update | released source fix adopted and exact emitted-worker lost-response/one-effect regression passes |
| Recursive Drizzle JSON values are narrowed incorrectly by default promotion | detached v0.7.x worktree and downstream package patch | `packages/applik8s/src/native-models.ts` | `bunx vitest run --maxWorkers=1 packages/applik8s/test/native-models.vertical.test.ts` | implemented on `codex/v0.7-compatibility-fixes` at `e36110e` (8/8 focused tests) | compare with v0.8 schema/runtime work and integrate once | model schema digest and stored JSON compatibility fixture | canonical recursive JSON test passes and downstream patch is removed |
| Recursive JSON Schema `$ref`/`$defs`, composed null, and explicit undefined differ between authoring and runtime validation | detached v0.7.x worktree and downstream SDK patch | `packages/sdk/src/schema-runtime.ts` | `bunx vitest run --maxWorkers=1 packages/sdk/test/schema-runtime.vertical.test.ts` | implemented on `codex/v0.7-compatibility-fixes` at `bc3a4b7` (10/10 focused tests) | integrate with Canonical JSON v1 without weakening schema validation | schema input/digest vectors across v0.7 and v0.8 | canonical source and exact runtime regressions pass |
| Function-native workflow tasks do not completely lower transactional model operations and provider dependencies | detached v0.7.x worktree and downstream compiler patch | compiler workflow contracts/emitter/source/resources and operation catalog | `bunx vitest run --maxWorkers=1 packages/compiler/test/application-workflows.vertical.test.ts` | implemented in two focused commits on `codex/v0.7-compatibility-fixes`: `186d580` for transactional model operations and `2274785` for merged model/provider handles | deduplicate against current v0.8 workflow/actor/schedule lowering | workflow worker metadata and operation identity compatibility | one canonical compiler path passes workflow, HTTP, and packed-consumer regressions |
| Declared durable failures are emitted as retryable workflow errors | older dirty release worktree | generated workflow source | focused compiler workflow suite and live declared-error retry assertion | implemented on `codex/v0.7-compatibility-fixes` at `6170e55`; focused compiler regression passes | integrate after comparing runtime-engine semantics | existing workflow histories must decode the same declared-error payload | declared failure is non-retryable without changing typed payloads |
| Query bindings lose schema-input typing and relationship behavior | downstream application package patch | application query contracts/runtime and builder | query gateway, application query, and compiler emitted-source suites | classify against maintained release-line source | integrate only canonical missing behavior | query cursor/digest compatibility | clean package consumer compiles without patch and query parity passes |
| Search projections diverge across deterministic/PostgreSQL/OpenSearch identity, high-watermark, RLS, pagination, and compatible reopen behavior | downstream application and search package patches | framework search contract/cursor codec plus provider runtimes | deterministic/PostgreSQL/OpenSearch differential suite | preserve required bugfixes without copying dist | shared provider-neutral offset/ordered-value codec, Release-A legacy writer, v1 reader, expiry, and provider wiring implemented in `6313341`; live PostgreSQL differential and retained-state evidence remain | bounded legacy cursor decoding and index reopening evidence | provider differential suite and retained-state migration pass without patches |
| Semantic partial native-model updates and SQL NULL versus JSON root null are conflated | downstream application package patch | native model command and PostgreSQL runtime | native-model/PostgreSQL command suites | pending canonical source comparison | integrate under Canonical JSON/schema policy | retained rows, JSON root null, SQL NULL, and update-diff fixtures | exact fixtures pass from packed packages |
| Generated workspace package discovery omits valid workspace dependency shapes | downstream CLI patch | CLI development workspace discovery | CLI workspace/packed-consumer suites | compare with current main fixes | integrate only unique behavior | no persisted format | clean external workspace discovers packages without patch |
| Operator break-glass state does not preserve all required server-shaped authority fields | downstream CLI patch | CLI operator authority command and core authority contracts | operator authority CLI/security suites | compare with released operator-authority fixes | integrate exact missing fields through canonical admission/authority | existing authority receipt validation and expiry | break-glass and revocation evidence pass without patch |
| Older agentic worktree may contain unique public source | stale worktree inspection | none unless source evidence is found | file inventory and source comparison | no release-line action | classified as generated/hidden artifacts only; never merge wholesale | none | all non-generated tracked changes remain absent; generated outputs are ignored |
| Repeated canonical JSON, HMAC/base64url envelopes, and admission shapes permit digest and authority drift | current released source and v0.8 characterization gate | core Canonical JSON/admission plus runtime signed-envelope implementation | `bun run check:v08:runtime-integrity` | narrow release fixes may retain compatibility adapters | Canonical contracts landed in `3b1e200`; maintained Canonical JSON adoption landed in `a6c507b`; task-query admission integration landed in `dff4658` and was made Release-A rollback-safe in `6313341`; shared search cursor integrity also landed in `6313341`; application query, stream, command, object-intent, lakehouse, and Kubernetes gateway formats migrated in `2ee9422`, `70a17a1`, `0d84712`, `e9a50f4`, and `d26eb76`; purpose-separated digests, released raw-HMAC compatibility, constant-time text comparison, and exact external-protocol exemptions were centralized in `7819ce4`; the aggregate source gate now passes | three-stage durable rolling migration unless explicitly shortened by evidence; every migrated phase is recorded in `docs/v0.8-format-registry.json` | Release-B/C telemetry and mixed-version evidence pass, then temporary legacy adapters are removed |

## Runtime Integrity evidence transitions

| Slice | State | Commit | Deterministic evidence | Remaining boundary |
| --- | --- | --- | --- | --- |
| Canonical JSON v1 contracts and portable Signed Envelope v1 | implemented | `3b1e200` | fixed vectors and focused core/runtime contracts | maintained adopters and durable migration |
| Canonical JSON v1 maintained-source adoption | integrated | `a6c507b` | 47 first-cohort tests, 92 second-cohort tests, affected provider suites, full workspace typecheck, and source gate with zero private canonical-JSON findings | persisted envelope and admission migrations |
| `RI-1a` application-graph and deployment-contract canonical bytes | integrated | `ff68f62` | Application graphs and deployment contracts use named Canonical JSON v1 policies; retained graph bytes and value digests remain exact, non-finite and unsupported values fail closed, and public TypeKro/Alchemy reference protocols cross an explicit inert evidence adapter. Twelve affected suites pass 132/132 tests, including direct and KRO deployment reconstruction. Runtime Integrity and docs gates pass; all 61 package units, four script shards, and root configs typecheck; clean packed consumption builds 59 packages, imports 102 public entrypoints, proves every maintained provider/boundary fixture, and completes CLI plus Agentic Start browser/server builds. | `RI-1b` compiler runtime-identity/bundle and client-store snapshot serializers remain private; add their retained-byte and Node/browser differential evidence before completing Canonical JSON convergence. |
| `RI-1b` compiler-manifest and client-store canonical bytes | integrated | `363d002` | Compiler runtime-identity and payload-schema digests use named Canonical JSON v1 policies with exact retained digest fixtures. Browser query keys use a named policy and explicit compatibility adapter, preserve exact UTF-8/base64url and undefined-as-null keys, and reject unsupported objects. The public `@applik8s/core/canonical-json` subpath is imported directly by browser, WASM-safe, compiler, and deployment consumers; minimal operator bundling excludes the core application barrel, while the Vite browser zone admits only this exact subpath. Compiler artifacts pass 47/47; client/core/deployment tests pass 70/70; the focused Vite zone regression passes; Runtime Integrity and docs gates pass; the full workspace run passed every unit except the initially stale compiler test diagnostic, whose corrected isolated compiler unit passes; clean packed consumption builds 59 packages, imports 103 public entrypoints, and completes all provider, CLI, and Agentic Start browser/server proofs. | `RI-2b` search input canonicalization remains: deterministic/PostgreSQL private normalizers and the explicit OpenSearch Date adapter must converge under a named policy without collapsing provider-specific cursor continuations. |
| `RI-2b` search canonical-value convergence | integrated | `87c3a3c` | Deterministic, PostgreSQL, and OpenSearch consume the named `application-search-value` Canonical JSON v1 policy and explicit Date adapter while preserving offset and ordered-value continuation semantics. Unsupported prototypes, `undefined`, non-finite numbers, and cycles fail closed. Release A keeps an explicitly registered legacy offset-provider Date digest reader and writes only the new digest. Focused provider/integrity tests pass 22/22, a disposable PostgreSQL 16 instance passes the complete live PostgreSQL search suite 7/7 with schema and container cleanup, affected package typechecks pass, Runtime Integrity/docs/diff gates pass, and clean packed consumption builds 59 packages and imports 104 public entrypoints before all provider, CLI, and Agentic Start builds. | Search cursor rejection/version telemetry and Release-B/C retirement evidence remain format-lifecycle work; the next active cutover is `RI-3a` request-ingress admission parity. |
| `RI-3a` authenticated HTTP/browser/SSE request admission parity | integrated | `f7d0df1` | Core owns one trace-aware request-admission constructor. Command, query, stream/SSE, and Kubernetes gateways all consume it; query/stream provider identities, command operation authority, and Kubernetes authorization receive immutable narrowed admission views. Table-driven identity/authority/trusted-context/operation/trace parity and malformed-provenance negatives pass. The Runtime Integrity gate now fails if any maintained request gateway bypasses the shared constructor or retains a private construction path. The focused request cohort passes 67/67; all 61 packages, four script shards, and root configs typecheck; clean packed consumption builds 59 packages, imports 104 public entrypoints, proves every external provider fixture, and completes CLI plus Agentic Start browser/server builds. | Redacted request rejection/version telemetry remains a release-evidence concern. The module-boundary gate still reports only its recorded pre-existing baseline. Advance `RI-3b` workflow/task, broker, webhook, and provider-callback admission. |
| `RI-3b` workflow/task delivery observations and cancellation recovery | live-qualified | `77bb297`, `8a07641`, `0e58331`, `cbd1699` | Generated task/workflow admission reports only the shared bounded observation version, state, execution boundary, workflow transport, canonical compatibility path, and classified rejection code. Stable handler-level operational observations expire and update in place; observation failures cannot replace the original admission result. The workflow gateway no longer logs arbitrary exception messages. Root starts use a caller/contract/idempotency admission identity rather than Hatchet's child-only key: a namespaced Lease fences concurrent gateway replicas, records the admitted provider run, and recovers a post-start interruption by querying the same opaque admission ID. The authority now has a provider-neutral replay-window policy and bounded paginated compaction: only expired terminal or confirmed-missing runs are deleted, every deletion uses a UID precondition, and active, recent, starting, malformed, or uncertain records remain. The exact OrbStack receipt proves one matching Lease, one Hatchet root run, exactly two intentional effect attempts, durable waiting through generated-worker replacement, typed signal branches, compensation, intervention, explicit cancellation, a collector-visible workflow/task retry link graph over custom-CA OTLP, and complete TypeKro/Alchemy cleanup. | This qualifies the generated in-cluster Hatchet gateway and its long-running Namespace retention contract. Arbitrary external Hatchet service behavior and lifecycle remain provider-owned and unclaimed. |
| `RI-3b` broker and verified-webhook admission observations | integrated | `8a07641` | Core owns one `applik8s.admission-observation/v1` record and bounded rejection classifier through the focused `@applik8s/core/admission` subpath. Generated broker workers and verified webhook routes retain their transport-specific proof boundaries, then emit the same redacted record; processor current-state writes are sampled and admission failures cannot persist arbitrary messages. Stripe rejects signed-body substitution and stale timestamps; processor provenance substitution never reaches the handler or advances its checkpoint. Core/broker/Stripe/HTTP tests pass 63/63; complete generated workflow/reactive/HTTP suites pass 44/45 with the sole failure being a minifier-coupled assertion, whose corrected exact test passes and whose other 44 tests were green; full workspace typecheck passes 61 packages, four script shards, and root configs; clean packed consumption passes 59 packages and 105 public entrypoints plus all external provider, CLI, and Agentic Start builds; Runtime Integrity, docs, package catalog, bundle ceiling, and diff gates pass. | Live Stripe delivery and emitted-worker PostgreSQL retry/restart/cleanup remain truthful live qualification rows. Advance `RI-3c`; do not reinterpret provider-specific signature algorithms as framework envelopes. |
| `RI-3c` managed schedule, actor/alarm, and agent/AI admission observations | integrated | `1dc42f4` | Generated local/Kubernetes/AWS/Hatchet schedule execution, canonical actor authority including Release-A retained alarms, and generated agent execution admission now use `applik8s.admission-observation/v1`. Schedule and agent rows are sampled stable current state; actor evidence is bounded telemetry. Raw provider errors are reduced to bounded codes, while schedule occurrence state, durable actor authority, and AI invocation/attempt history remain canonical in their existing stores. The 59-test deterministic cohort passes after one minifier-coupled assertion was corrected and rerun; the full workspace typecheck passes 61 packages, four script shards, and root configs; clean packed consumption passes 59 packages, 105 public entrypoints, every external provider fixture, CLI, and Agentic Start browser/server builds; Runtime Integrity and diff gates pass. | Distributed celld/Kubernetes actor qualification, OrbStack schedule lifecycle, real EventBridge, and live AI/provider restart receipts remain governed by their explicit evidence rows. Advance `RI-3d` request rejection/version observations. |
| Task-query admission envelope | integrated | `dff4658`, `6313341` | 13 runtime/task-query tests including both legacy-reader and pre-v0.8-reader compatibility, 22 generated-reactive compiler tests, full workspace typecheck, docs consistency, and diff checks | Release A writes the canonicalized legacy wire and reads legacy plus v1; rejection/version telemetry and the later writer transition remain |
| Shared deterministic/PostgreSQL/OpenSearch search cursor codec | implemented | `6313341` | 31 focused tests with 5 live PostgreSQL cases truthfully skipped, targeted core/Applik8s/OpenSearch typechecks, build of all publishable packages, and clean packed consumption of 59 packages/98 entrypoints | run live PostgreSQL differential and retained-state migration; add telemetry before claiming integrated/live-qualified |
| Application query cursor and Node keyed-digest adapter | integrated | `2ee9422` | 25 focused runtime/query tests, including Release-A v0.7-compatible writes, v1 reads, bounded lifetime, malformed payloads, adapter revalidation, and fixed keyed-digest compatibility; full 61-package workspace typecheck and diff checks pass | aggregate gate still names 13 owners; add rejection/version telemetry before the Release-B writer transition |
| Application stream-subscription cursor | integrated | `70a17a1` | 16 focused stream/runtime tests, including v0.7-compatible output, v1 resume, bounded lifetime, identity transfer, authority changes, and retained-cursor behavior; full 61-package workspace typecheck and diff checks pass | aggregate gate still names 12 owners; add rejection/version telemetry before the Release-B writer transition |
| Application command progress cursor | integrated | `0d84712` | 21 focused command/stream/runtime tests, including v0.7-compatible output, v1 result observation, durable authorization bindings, bounded lifetime, malformed input, and canonical persisted-receipt comparison; full 61-package workspace typecheck and diff checks pass | aggregate gate still names 11 owners; add rejection/version telemetry before the Release-B writer transition |
| Application object intent and completion receipt | integrated | `e9a50f4` | object-gateway and Node-integrity contracts pass with v0.7-compatible writes, v1 upload/receipt reads, principal/admission binding compatibility, tamper rejection, and bounded lifetime; all publishable packages build; 22 generated-reactive tests and 5 Agentic Start generator/compiler tests pass; Chirp and the full workspace typecheck pass | aggregate gate still names 10 owners; add rejection/version telemetry before the Release-B writer transition; the checked-in 580 KB gateway plus 10 KB Runtime Integrity overhead ceiling must not increase again |
| Shared Node integrity adapters and exact external-protocol inventory | integrated | `7819ce4` | 95 focused Runtime Integrity, relational, identity, operations, compiler, and Stripe tests pass; the loopback local-resource authority test passes; all 61 package typechecks, four script shards, and root TypeScript configs pass; `git diff --check` passes | aggregate gate now names only the deterministic lakehouse, AWS lakehouse, and Kubernetes gateway cursor implementations; Stripe remains a registered external protocol rather than a framework envelope |
| Deterministic/AWS lakehouse and Kubernetes gateway cursors | integrated | `d26eb76` | 28 focused local lakehouse, AWS lakehouse, and Kubernetes gateway tests pass, including Release-A legacy readability, Signed Envelope v1 adoption, expiry, identity binding, and ordered async watch delivery; affected package typechecks and `git diff --check` pass; `bun run check:v08:runtime-integrity` is green | add version/rejection telemetry and mixed-version rollout evidence before Release B; no private maintained-source cryptographic envelope remains |
| Canonical request admission and gateway cohort | implemented | `a4d5f55` | 78 core, server, application gateway, command, query, stream, and complete generated-reactive tests pass; the optimized generated gateway is 584,151 bytes under the unchanged 585 KiB ceiling; core, server, Applik8s, and compiler package typechecks pass; `bun run check:v08:runtime-integrity` and `git diff --check` pass | finish the exhaustive adapter matrix for webhooks, broker deliveries, workflows/tasks, schedules, actors, agents, and AI; add parity/source gates before calling the admission contract integrated |
| Canonical agent, tool, and AI-attempt admission | implemented | `46d4c95`, `d5db3df` | Release-A dual-written agent and internal-operation records preserve every 0.7.x field; pre-v0.8 tokens hydrate into canonical contexts; mismatched compatibility fields fail closed; exact trace/correlation/causation/cancellation/deadline/delivery evidence survives gateway, worker, and tool hops; provider attempts retain credential-free canonical evidence rather than trusted-context values; 80 focused tests, affected package typechecks, and the executable matrix/AST source gate pass | add version/rejection telemetry before the whole admission foundation is integrated |
| Workflow task-query canonical admission | integrated | `b2cd862` | 60 focused core, operations, query, command, and task-query tests; all 22 generated-reactive cases; core, operations, Applik8s, and compiler typechecks; `bun run check:v08:runtime-integrity`; 587,511-byte full reactive gateway under the checked-in 590,000-byte ceiling | task-query delivery now uses a bounded service principal and Release-A dual context; workflow starts/resumes, task operations, signals, cancellation, and reattachment remain before the complete workflow/task adapter can advance |
| Canonical workflow/task delivery admission | implemented | `3ba056b` | 40 focused core, authority, and task-operation tests pass; all 12 generated-workflow cases pass with real compilation and bundling under an explicit 8 GiB test heap; core, operations, Applik8s, and compiler typechecks pass; the Runtime Integrity and diff gates pass; changed-file Biome lint exits successfully | task queries intentionally retain their bounded service projection; signal action delivery, cancellation propagation/recovery, lost-response reattachment, and version/rejection telemetry remain; the branch-wide lint gate is still red on the separately tracked legacy warning/typecast baseline |
| Typed HTTP and resource-driven workflow admission, private gateway, and opaque run reference | live-qualified | `f303a2c`, `e8f79e8`, `0e58331`, `cbd1699` | The isolated fault-injection receipt proves a destroyed post-admission response reattaches to one run/effect. The exact generated resource gateway adds cross-replica Lease fencing and provider-run recovery, uses Kubernetes `V1MicroTime`, persists only the admission digest rather than the raw idempotency key, and exposes only an opaque signed run reference. Its configurable admission policy defaults to a seven-day replay window, five-minute cleanup cadence, and 1,000-record page; cleanup verifies terminal provider state and deletes only with the observed UID. On OrbStack, one resource reconcile produced exactly one Lease, one matching Hatchet root run, and two intentional effect attempts; the same generated application survived worker replacement and exercised success, compensation, intervention, and cancellation before full managed teardown. The refreshed receipt also proves custom-CA OTLP export and exact workflow/task retry links. The focused runtime/core/API cohort passes 49 checks, the complete generated-workflow suite passes 16/16, clean consumption packs 59 packages and imports 105 public entrypoints, and the refreshed 61-package/four-script/root-config typecheck passes. | Observation abort and timeout deliberately do not cancel an admitted run, while explicit cancel remains caller-and-contract authorized. Expired admission deletion deliberately permits a later identical tuple to start a new run; shortening the replay window is an application idempotency decision. |
| Canonical broker and stream-processor delivery admission | implemented | `dbb686f` | 24 focused stream/signal runtime tests and all 22 generated-reactive compiler cases pass; runtime delivery admits before capability hydration on every retry, shares the exact narrowed context with decoder and handler, retains per-event admission for frozen batches, and refuses invocation after a deadline; generated workers use the current operation-authority revision, preserve causal identity, fail closed without trusted context, and remove the duplicate signal authority client; Applik8s/compiler typechecks, Runtime Integrity, docs consistency, and diff checks pass | add rejection/version telemetry and an emitted-worker PostgreSQL retry/restart/cleanup receipt before live qualification; heterogeneous batches intentionally expose per-event admission rather than fabricating a batch-wide caller |
| Canonical verified-webhook admission | implemented | `37a0ad3` | 14 focused Stripe, authored HTTP, and generated HTTP tests pass; the provider adapter verifies exact raw bytes before parsing, the framework derives the bounded service identity and canonical trusted-context digest, authorization receives a receiptless narrowed admission, and the handler receives the validated route receipt without client-authored identity; Canonical JSON owns invocation/input digests, Applik8s/compiler typechecks, Runtime Integrity, and diff checks pass | add provider-rejection/version telemetry and a live Stripe webhook lifecycle receipt before live qualification; provider-specific signature algorithms remain registered external protocols rather than framework envelopes |
| Canonical durable actor-turn admission envelope | implemented | `77291ee` | 21 focused actor/celld tests and 41 generated workflow, agent, and Fetch-gateway compiler tests pass; Applik8s, celld, and compiler typechecks pass; Runtime Integrity, docs, scorecard-contract, lint-without-errors, and diff gates pass; clean packed consumption builds 59 packages and imports 101 public entrypoints including the focused actor-authority runtime, then passes generated workflow and Agentic Start builds | Release A reads complete v0.7 authority and dual-writes legacy mirrors plus canonical admission; add legacy/version/rejection telemetry and retained-alarm recovery evidence; implement bounded actor execution and alarm-service principals, nested-call narrowing, cancellation, causation, and the remaining `AC-1` contract before advancing the adapter or any actor provider claim |
| Bounded actor execution and alarm authority (`AC-1`) | implemented | `b076ee7` | 25 focused authority/actor/celld tests; at this checkpoint `bun run check:v08:actors:contracts` passed 117 tests across Applik8s, celld, client, compiler, TypeKro, Alchemy, AWS-plan, and core boundaries; `bun run check:v08:runtime-integrity` passed its canonical/source gate; `bun run check:package-consumer` packed 59 packages, imported 101 public entrypoints, and passed clean workflow/model/CLI/Agentic Start generation and builds; the full workspace typecheck passed 61 packages, four script shards, and root configs; changed-file Biome reported warnings but no errors; docs consistency and diff checks passed | AC-1 uses framework-derived bounded actor execution principals, handler-scoped workload envelopes, stable nested-call idempotency, exact actor-key digests, narrowed nested authority, future-bounded persisted alarm authority, preserved causal principals, transactional command-authorized cancellation, cancellation-revision fencing, redacted format/rejection telemetry, and retained Release-A alarm recovery. The separate `AC-2a`–`AC-2c` rows now close its distributed provider lifecycle; the branch-wide lint typecast baseline remains separately tracked. |
| Distributed celld state, receipt, alarm, and node-loss lifecycle (`AC-2a`) | live-qualified | `e5ceb71`, `323fb6e` | `bun run test:v08:actors:celld-live` passes against the local OrbStack Docker daemon using pinned celld and SeaweedFS images. The gate emits and builds the real generated Worker artifact, preflights Docker, AWS CLI, pinned images, and conflicting identities before creating its network, proves awaited full-turn serialization, duplicate-result idempotency, state recovery after forced node loss, durable alarm delivery, recovery through a newly joined third node, and eventual clean fleet diagnosis after the expired peer lease is removed. The refreshed gate uses the shared Canonical JSON v1 live-authority fixture and re-inspects all four owned containers, the generated image, network, and temporary directory. `APPLIK8S_TYPECHECK_FILTER=package:e2e bun run typecheck` passes. | This qualifies the bounded Docker state/receipt/alarm lifecycle. `AC-2b` and `AC-2c` are separately live-qualified below; deterministic cancellation semantics remain covered by the 133-test actor contract rather than being misreported as this receipt. |
| TypeKro/Alchemy Kubernetes actor lifecycle (`AC-2b`) | live-qualified | `323fb6e` | `bun run test:v08:actors:kubernetes-live` passes on OrbStack after a bounded Docker/Kubernetes/storage/RBAC/conflict preflight. Alchemy applies the generated local object store and two-node celld graph in dependency order; canonical actor authority admits the initial command and its duplicate receipt; a forced loss of the exact serving pod relocates the actor to the other node with state `2` preserved before the next command reaches `5`; a graph update rolls two replicas down to one without losing state; deletion drift of the owned actor NetworkPolicy is planned and repaired; Alchemy destroys all 14 graph resources and the namespace, and the gate re-inspects namespace and generated-image absence. The refreshed Docker gate and E2E package typecheck also pass. | This is resource-loss drift rather than an unclaimed universal Kubernetes field-drift detector. Raw Kubernetes deletion appears only as bounded failure injection; normal creation, update, drift convergence, and teardown remain TypeKro/Alchemy-owned. |
| Distributed celld realtime connection lifecycle (`AC-2c`) | live-qualified | `b13b953` | `bun run test:v08:actors:realtime-live` passes against the local OrbStack Docker daemon using the real generated Worker artifact and pinned celld/SeaweedFS images. A real WebSocket is admitted by a bounded, signed, single-use connection ticket; typed connection-message callbacks broadcast and return matching positive committed revisions; the socket remains useful across an idle interval; forced loss of its serving node closes the transport; a canonical command on the surviving node proves actor state/ownership relocation; a fresh bounded ticket reconnects on that node; the next message preserves revision continuity; the stale first connection converges to exactly one `lease-expired` disconnection and the new connection to exactly one graceful `closed` disconnection. Fleet diagnosis passes and cleanup re-inspects every container, generated image, network, and temporary directory as absent. The original `bun run test:v08:actors:celld-live`, 133-test actor contract, full workspace typecheck, changed-file Biome, and diff gates also pass. | This closes the bounded `AC-2` provider packet for the advertised local distributed and OrbStack Kubernetes targets. It does not claim real-cloud actor qualification or turn actor alarms into application schedules. |

## Callable-provider evidence transitions

| Slice | State | Commit | Deterministic evidence | Remaining boundary |
| --- | --- | --- | --- | --- |
| Portable provider-operation metadata and generated stream-processor hydration | implemented | `bb7be3e` | 83 focused provider, callback-discovery, compiler, and schedule-state tests pass; the authenticated HTTP/SSE gateway is 589,652 bytes under the unchanged 590,000-byte ceiling; exact Agentic Start discovery and compilation pass; clean packed consumption builds 59 packages and imports 100 public entrypoints; the complete 61-package workspace typecheck, changed-file lint, documentation consistency, and diff checks pass | extend the same public static-operation contract through HTTP, workflows/tasks, schedules, actors, agents, and projections where side effects are permitted; first close actor call/alarm canonical admission, then prove exact credential, access, network, topology, cancellation, and packed external-provider behavior before advancing `PI-1` beyond `implemented` |
| Actor callable-provider graph, access, and generated-worker hydration | integrated | `fce8889`, `b13b953` | `bun run check:v08:actors:contracts` passes 133 tests across 13 suites; `bun run check:package-consumer` packs 59 packages, imports 101 public entrypoints, and executes a clean external provider through both direct `provider.acquire()` and extracted helper-mediated actor calls without an internal provider-runtime import; graph/foundation assertions prove the exact provider resource receives only declared `connection.use` and `network.connect`; generated callbacks execute both binding shapes and exclude authoring-time application setup; deployment-plan coverage places SMTP credentials on the actor host and not an unrelated processor; missing/private runtime exports and undeclared access fail closed; Runtime Integrity and the complete 61-package/four-script/root-config typecheck pass. The `AC-2a`–`AC-2c` live rows above qualify the associated distributed/local-Kubernetes lifecycle. | `PI-1` remains open for every other maintained execution family; the current cursor moves to workflows/tasks. Branch-wide lint remains red on the recorded pre-existing v0.8 typecast baseline, while the changed-file Biome run has warnings but no errors. |
| Workflow/task callable-provider graph, runtime binding, and generated-worker hydration | integrated | `ee2435b`, `44e9fa6`, `f388883` | 39 workflow/provider tests pass across compiler and application packages; 4 generic runtime-lowering tests and 44 affected HTTP, reactive, actor, and Hatchet regressions pass; clean packed consumption builds 59 packages, imports 101 public entrypoints, and proves an external workflow provider with profile-selected public configuration, Secret identity/key projection, readiness, and worker-only placement; direct, extracted, and helper-mediated calls resolve through one public static runtime operation; missing/private exports, missing target runtime, public/Secret collisions, cross-namespace Secrets, and undeclared bindings fail closed. A second independent compilation produces the same workflow digest, artifact source, and generated worker source. Runtime Integrity, the complete 61-package/four-script/root-config typecheck, changed-file checks, and diff checks pass. | This is deterministic integration, not a new live Hatchet lifecycle claim. HTTP is the next bounded `PI-1` family; schedules, agents, projections where effects are permitted, and any remaining family-specific provider qualification stay open. |
| Typed HTTP callable-provider graph, runtime binding, and generated-worker hydration | integrated | `07e3ef3` | The focused external-provider regression passes direct, extracted, and helper-mediated calls, public static runtime import, authoring-setup exclusion, profile configuration, Secret identity/key projection, readiness, exact `connection.use` and `network.connect` access, health-server isolation, deterministic recompilation, and fail-closed placement-only or missing-runtime metadata. The complete HTTP suite passes 5/5; the affected HTTP, workflow, provider, and graph-foundation set passes 40/40; core foundation passes 9/9; `bun run check:v08:provider-conformance` passes 33/33; `bun run check:v08:runtime-integrity` passes; the complete workspace typecheck covers 61 packages, four script shards, and root configs; and clean packed consumption builds 59 packages, imports 101 public entrypoints, and proves the HTTP provider without an internal runtime or authoring-graph import. | Canonical HTTP admission, request identity, idempotency, cancellation, and workflow handoff remain unchanged. This is deterministic integration, not a new live HTTP-provider lifecycle claim. The aggregate target-parity gate now completes under the separately checkpointed 8 GiB heap setting, but remains red only on the existing Chirp/EventBridge second-precision schedule incompatibility; 85/86 tests, including the affected HTTP target coverage, pass. Agents are the next bounded `PI-1` family; schedules and effect-permitted projections remain open. |
| Agent callable-provider graph, runtime binding, and generated-worker hydration | integrated | `26186fb`, `39e8f74` | The complete agent suite passes 6/6, including a clean external provider called directly, through an extracted bound operation, and through a module-local helper. Generated agents import only the public static runtime operation, replace captured authoring roots, project generic configuration plus Secret/readiness metadata, derive exact `connection.use` and `network.connect` access for the consuming agent, reject missing/private/operation-less provider metadata, and reproduce identical output. The maintained Billing prerequisite now binds exact public payment operations rather than whole-provider placement; explicit `objectStore` provenance lets resource-backed placement coexist without weakening ordinary provider rejection. Billing, Stripe, object-storage, and Agentic payment tests pass 15/15; HTTP remains green 5/5; deployment graph passes 7/7; `bun run check:package-consumer` passes 59 packages, 101 entrypoints, all external provider proofs, clean CLI, and complete Agentic Start builds; `bun run typecheck` passes 61 packages, four script shards, and root configs. | This is deterministic integration, not a new live agent-provider lifecycle claim. Canonical agent admission, causal identity, tools, retries, usage, and conversation persistence remain unchanged. Schedule callable-provider characterization is the next bounded `PI-1` family; effect-permitted projection/event paths remain open. |
| Default ApplicationHost schedule-control callable-provider graph, runtime binding, and host placement | integrated | `ae04dab` | Direct, extracted, and module-helper calls from a clean packed external provider retain one public static operation; generated schedule control imports no authoring setup or internal provider runtime; exact `connection.use` and `network.connect` access is attributed only to the schedule; generic configuration and one Secret binding are placed on the ApplicationHost and nowhere else; missing or operation-less metadata fails closed; repeated generation is identical. The focused authoring/compiler/host/actor/deployment set passes 28/28; Applik8s and compiler typechecks pass; Runtime Integrity passes; provider conformance passes 33/33; and clean package consumption builds 59 packages, imports 101 entrypoints, proves external actor/workflow/HTTP/schedule/agent providers, then completes CLI and Agentic Start generation, compilation, and browser/server builds. | This qualifies only default/unqualified schedules executed by the generated ApplicationHost control boundary. Qualified schedulers such as Hatchet are excluded by design and are the next bounded characterization; occurrence identity, canonical admission, overlap, misfire, retry, cancellation, and the distinction from actor alarms remain unchanged. Effect-permitted projection/event families remain open. |
| Qualified Hatchet schedule provider graph, runtime binding, and ApplicationHost placement | integrated | `44d32d8`, `550f6d9` | `bun run check:v08:scheduling` passes 165 tests; provider conformance passes 37; clean package consumption builds 59 packages, imports 102 public entrypoints, and proves external actor/workflow/HTTP/schedule/agent providers plus complete CLI and Agentic Start builds. The complete typecheck passes 61 packages, four script shards, and root configs. Exact tests cover the public Hatchet projection, scheduler-node identity, ApplicationHost-only operation/Secret placement, deterministic output, unsupported cadence/timezone rejection before mutation, restart recovery, overlap fencing, lost-response receipt reuse, and terminal failed-attempt redelivery without a duplicate effect. The OrbStack scheduling receipt suite passes 3/3 with cleanup. Changed-file error checks, documentation consistency, and diff checks pass; branch-wide lint remains red only on the recorded pre-existing typecast/warning baseline. | This is deterministic integration plus live PostgreSQL/Kubernetes receipt-authority evidence, not live Hatchet qualification. Real Hatchet installed through TypeKro/Alchemy still needs fixed, dynamic, and one-time firing; update, disable, deletion, interruption, drift repair, shared-engine lifecycle, exact provider scheduled-time, and leak-free teardown receipts. Effect-permitted projection/event families remain open. |
| Reactive callback callable-provider qualification and projection purity | integrated | `9525a04` | `Stream.onEvent(...)`, `Stream.onBatch(...)`, and `Model.on.create(...)` converge through one stream-processor graph/runtime boundary and retain direct, extracted, and helper-mediated operations from a clean packed external provider. The generated consumer alone receives the public runtime import, profile configuration, Secret reference, and exact `connection.use`/`network.connect` access; an unrelated processor receives none. Event and batch invocation semantics remain distinct. Function-native projection registration rejects asynchronous transforms and recursively captured provider/effect handles before generation. The complete Applik8s reactive suite passes 23/23, the focused compiler hydration regression passes, provider conformance passes 37/37, the clean package-consumer gate passes with the new reactive proof, the 61-package/four-script/root-config typecheck passes, changed-file Biome reports no errors, and diff checks pass. | This is deterministic integration, not a new live broker or projection-rebuild qualification claim. `beforeCommit(...)` remains a transaction-local policy boundary and must not be widened into an external-effect hook. `Resource.on.reconcile(...)` remains the next effect-permitted callable-provider characterization. |
| Transaction-local and componentized-WASM provider boundaries | implemented | `8543138` | 28 focused native-model and resource-event-controller tests pass. Recursively captured provider operations fail at registration in `beforeCommit(...)` while ordinary staged commands/events remain valid. Recursively captured provider operations fail before operator installation in `Resource.on.reconcile(...)`, naming the missing host-mediated bridge and prohibiting ambient credentials or Node runtime embedding. Clean packed consumption builds 59 packages, imports 102 public entrypoints, and proves both boundaries through an external helper-mediated provider; the complete 61-package/four-script/root-config typecheck, changed-file Biome error check, and diff checks pass. | `beforeCommit(...)` is intentionally complete as a transaction-local boundary, not a provider execution family. Resource reconcilers remain effect-permitted but callable-provider support depends on a host-mediated runtime-access bridge after `GP-1` and `RA-1`; `PI-1` must remain open and no live reconciler-provider claim is made. |

## Observability evidence transitions

| Slice | State | Commit | Deterministic evidence | Remaining boundary |
| --- | --- | --- | --- | --- |
| `OB-1a` provider-neutral telemetry semantics and OpenTelemetry runtime | implemented | `18f7c66` | Core owns one versioned bounded carrier with W3C context, stable application/operation/execution/attempt and causal classes, synchronous/asynchronous classification, allowlisted baggage, strict metric descriptors, and exporter-first redaction. The OpenTelemetry runtime implements synchronous parentage, asynchronous links and bounded fan-in, carrier capture, correlated redacted structured logs, counter/gauge/histogram instruments, cardinality/drop/export-failure evidence, failure isolation, head sampling, Secret-header OTLP configuration, and base-path-safe trace/metric endpoints. `bun run check:v08:observability` passes 107/107 after rebuilding publishable packages; the complete 61-package/four-script/root-config typecheck passes; changed telemetry files pass Biome and diff checks. Clean consumption packed 59 packages and imported 105 public entrypoints before failing later on the independent unplaced schedule-runner access regression. | Instrument every maintained execution family and prove one synchronous/asynchronous vertical; add the versioned ComponentizeJS/WIT/Wasmtime/Rust carrier and terminal outcomes; add log export, local collector, external OTLP, TypeKro ClickStack ingestion/lifecycle, truthful CloudWatch tiers, ApplicationPlan policy, performance/cost history, and secret/PII backend canaries. This row is not integrated or live provider evidence. |
| `OB-1b1` canonical ComponentizeJS/WIT/Wasmtime/Rust carrier | implemented | `ec6f2fa` | The parallel `applik8s.telemetryCarrier/v1alpha1` guest/host shape is deleted. TypeScript foundation records, generated JSON Schema, Rust wire types, the operator host, and component input now use the exact `applik8s.telemetry/v1alpha1` carrier. Both TypeScript and Rust enforce W3C sampling/replay consistency, bounded baggage, and application/operation/execution/attempt-instance parity with the outer guest/host identity before invocation; the host retains the single reconcile-span ownership and sends that parent carrier synchronously rather than creating a duplicate guest span. The source gate rejects reintroduction of the old maintained shape. `bun run check:v08:observability` passes 108/108; Runtime Integrity and generated-contract gates pass; 15 focused core tests and all 7 Rust contract tests pass; the real ComponentizeJS → WIT → Wasmtime → Rust regression preserves the exact version, traceparent, semantic identities, instance, attempt, and sampled flag; the complete workspace typecheck passes 61 packages, four script shards, and root configs; focused Biome and diff checks pass. | This is deterministic transport and validation evidence, not a collector or backend receipt. Instrument and link every maintained execution family, add redacted terminal outcomes, and prove one end-to-end synchronous/asynchronous causal chain before advancing `OB-1`; collector, external OTLP, ClickStack, and CloudWatch tiers remain separate. |
| `OB-1b2a` first durable producer/consumer propagation cohort | implemented | `d4acc4f` | An executable matrix inventories all 13 maintained boundary kinds and fails when direct instrumentation/capture call sites drift. HTTP command issuance captures the active canonical carrier; PostgreSQL model execution creates one synchronous child or asynchronous linked boundary and atomically persists its child carrier with event/command outboxes; replay does not fabricate another model boundary; PostgreSQL stream hydration validates internal carriers; JetStream and Kinesis command redelivery restore the exact producer carrier on every attempt; single-event and frozen-batch processors use bounded asynchronous links; and generated command/stream workers install and close the application-selected OpenTelemetry runtime through one shared compiler source. `bun run check:v08:observability` passes 184/184, `bash scripts/run-v08-observability-postgres-live.sh` passes the exact atomic carrier/outbox case on disposable native PostgreSQL with cleanup, the complete workspace typecheck passes 61 packages, four script shards, and root configs, and `git diff --check` passes. The authenticated gateway records a measured 9 KB carrier-capture cost under a separate checked-in 10 KB ceiling that later work may reduce but not silently increase. | This is one bounded durable cohort, not the complete causal vertical or backend qualification. Workflow/task, agent/tool, provider, reconciler, actor-alarm/remote-turn, query, non-command event transports, complete terminal-outcome semantics, and emitted-worker PostgreSQL restart/replay/cleanup evidence remain open. No collector, external OTLP, ClickStack, or CloudWatch claim follows from this row. |
| `OB-1b2b` workflow/task producer-carrier, retry, reattachment, and operational retention | live-qualified | `78f6a07`, `0e58331`, `cbd1699` | Function-native workflow/task handles and private typed-HTTP workflow starts capture the canonical active carrier without author metadata. A focused transport codec preserves causal principal, correlation, causation, trace, trusted context, and provider admission identity across every Hatchet root/child boundary; root gateway retries converge through one durable admission identity instead of misusing Hatchet's child-only key. The gateway now owns an explicit replay window and bounded Lease compactor with Kubernetes pagination, continuation-token reset, provider terminal/missing verification, fail-closed uncertain state, and UID-fenced deletion. The exact OrbStack receipt proves one admission Lease, one matching root run, exactly two intentional effect attempts, correlation through provision and commit tasks, suspension/resumption across worker replacement, signal-driven success/compensation/intervention, cancellation, replay-safe effect idempotency, custom-CA OTLP export, workflow/task links carrying attempts 1 and 2, redaction canaries, and complete generated application plus Hatchet resource cleanup through TypeKro/Alchemy. Generated worker metafile analysis proves zero TypeKro inputs; focused runtime/core/API tests pass 49/49, the complete compiler workflow suite passes 16/16, clean packed consumption passes 59 packages/105 entrypoints, and the refreshed full workspace typecheck passes. | This live-qualifies generated Hatchet delivery, correlation, interruption, cancellation, retention, telemetry linkage, and lifecycle on OrbStack. It does not claim an arbitrary external Hatchet service's behavior or lifecycle. |
| `OB-1b2c` agent, inference-attempt, and tool-operation propagation | integrated | `26d6e16`, `401701c` | The authenticated Fetch gateway captures one framework-owned bounded carrier after admission and never trusts a browser-authored telemetry header. The generated worker decodes that carrier separately from signed admission, persists the first producer carrier on the logical invocation, and restores it for the physical attempt. Only a real dispatch opens one long-lived asynchronous agent boundary; each inference adapter turn is a numbered synchronous provider child, each tool call is a synchronous operation child keyed by its provider call ID, and join/replay creates no new business boundary. The real process receipt runs the compiler-owned front gateway and generated agent as separate Node processes against disposable native PostgreSQL and an inspectable OTLP receiver. It executes a function-native TanStack `chat(...)` callback with no authored cancellation ceremony, performs one typed `Post.edit(...)` tool effect, terminates and restarts the agent on the same endpoint, replays the logical run without another proposal or business span, cancels a second run while deterministic provider dispatch is active, and replays the first run again without duplication. The slice fixed dropped Node socket cancellation, unbounded generated-agent termination, missing server-admitted relational change scopes for local tools, volatile deadline/trace fields incorrectly participating in durable replay identity, and a non-cooperative deterministic-provider delay. Collector assertions prove two agent spans, at least three nested provider spans, exactly one nested tool-operation span, asynchronous links to admitted front HTTP spans, and absence of the prompt canary from OTLP and process output. `bun run test:v08:observability-agent-process-live` passes 1/1 in 20 seconds with complete PostgreSQL/process/temp cleanup; `bun run check:v08:observability` passes the 13-boundary matrix, 112/112 Rust host contracts, 317/317 primary tests, and 41/41 AI/agent tests; the full workspace typecheck passes 61 packages, four script shards, and root configs; clean consumption passes 59 packages and 105 public entrypoints including Agentic Start; focused Biome has warnings but no errors; and diff checks pass. | This qualifies the generated process, deterministic provider/tool stream, restart, cancellation, replay suppression, linkage, redaction, and cleanup contract. It does not qualify a third-party inference provider's credentials, network behavior, throttling, or lifecycle. General callable-provider, reconciler, actor, workflow/Hatchet, Kinesis, and CloudWatch process/backend receipts remain separate. |
| `OB-1b2d` general callable-provider attempt propagation | implemented | `41952aa` | `application.inject()` opens no telemetry during provider discovery, qualification, placement, or property extraction. Each actual direct, extracted, or helper-mediated callable invocation opens one synchronous provider child under the active semantic operation, keyed by stable provider graph identity plus interface/member. Compiler-hydrated HTTP, workflow/task, agent, actor/schedule, and reactive operations use the same public framework wrapper without importing authoring setup or an internal provider runtime. The OpenTelemetry runtime preserves synchronous values, Promise settlement, and exact business errors while keeping asynchronous call spans open through settlement; arguments, results, credentials, and exception messages never enter telemetry. `bun run check:v08:provider-conformance` rebuilds publishable packages and passes 41/41; `bun run check:v08:observability` passes the matrix/build plus 248/248 maintained core/compiler/runtime tests and 39/39 AI/agent tests; the complete workspace typecheck passed 61 packages, four script shards, and root configs before the checkpoint; focused generated-family regressions and diff checks pass. | This is deterministic runtime and generated-source evidence, not a live selected-provider or collector receipt. Prove cancellation, timeout, retry, and redacted terminal behavior in an emitted worker and backend. Reconciler, actor alarm/remote-turn, query, non-command event, complete emitted-worker causal verticals, and collector/backend qualification remain separate. |
| `OB-1b2e` reconciler host/guest attempt propagation | implemented | `ebfe3f7` | The Rust operator host owns the single reconciler attempt span and derives its canonical operation/execution identity from the compiler-emitted runtime envelope. It passes that exact carrier synchronously into the ComponentizeJS/WIT/Wasmtime guest without opening a second business span, derives the physical attempt from durable retry state, classifies retries explicitly, keeps stale-generation work as a phase, and closes unexpectedly dropped work with a typed `ReconcileInterrupted` cancellation terminal. Canonical operation/duration/retry metrics and structured logs contain bounded semantic fields and allowlisted error types only; arbitrary handler, Kubernetes, Secret-shaped, target, field-manager, and stack-path text is excluded from spans, metrics, logs, and CR status. The executable matrix now reports 6 implemented, 7 in-progress, and 0 not-started boundaries. `cargo test -p applik8s-operator-host` passes 112/112; the complete `bun run check:v08:observability` gate passes those Rust contracts plus 248/248 maintained core/compiler/runtime tests and 39/39 AI/agent tests; the full workspace typecheck passes 61 packages, four script shards, and root configs; docs, source-redaction, and diff gates pass. | This is deterministic host/guest ownership, retry, routing, terminal, and redaction evidence. kube-runtime does not expose whether an admitted controller callback came from an immediate watch notification or scheduled requeue, so the host records truthful `kubernetes-controller` provenance rather than inventing a distinction. Live watch/requeue, retry, trap, timeout, shutdown interruption, deletion/finalization, restart continuity, and collector-visible correlation remain qualification work. Actor alarm/remote-turn, query, non-command event, and the complete emitted-worker causal vertical remain separate propagation cohorts. |
| `OB-1b2f` actor remote-turn and durable-alarm attempt propagation | implemented | `e2a04ec` | Remote actor calls emitted from observed workflow, agent, and processor workers capture the exact active carrier and send it through the authenticated application gateway without exposing telemetry controls on authored actor handles. Local and celld alarm scheduling persist that carrier atomically with authority, input, and idempotency state. Every admitted physical delivery validates the carrier, links it asynchronously, and records a one-based attempt; a failed alarm remains durable with its incremented attempt, successful delivery removes it, and receipt replay returns the committed result without opening another business boundary. Gateway and celld failure reporting is bounded and excludes raw exception text. The focused propagation cohort passes 89/89 across six source/runtime suites; `bun run check:v08:actors:contracts` passes 149/149 across 13 suites after stale synthetic workload fixtures were repaired to carry the exact Service selector and pod-template labels required by current placement validation; and `bun run check:v08:observability` passes the matrix, 112/112 Rust host tests, 270/270 maintained core/compiler/runtime tests, and 39/39 AI/agent tests. The compiler typecheck covers all 61 packages, four script shards, and root configs; focused Biome has warnings but no errors; diff checks pass. | This is deterministic local snapshot, generated transport, and celld Durable Object persistence/retry/replay evidence. Live emitted gateway-to-celld relocation, application-worker restart, collector-visible links, and explicit command-authorized alarm cancellation correlation remain qualification work. Query, non-command event, and the complete emitted-worker causal vertical are the next propagation cohorts; no collector or backend claim follows from this row. |
| `OB-1b2g` local, remote, and multiplex query propagation | implemented | `c949c6b` | Query routes now retain an HTTP transport parent instead of relabeling ingress as the semantic query. Relational snapshots, complete SSE iterator lifetimes, each logical multiplex member, and admitted internal invocations open exactly one query boundary. Kubernetes snapshots and complete watch lifetimes use the same semantic owner; reset, watch error, bounded session completion, shutdown, and consumer cancellation converge through one completion barrier, and cancellation does not return before telemetry and subscription release finish. Generated remote gateways overwrite caller-authored internal telemetry, encode/decode one validated 8 KiB carrier without affecting business execution on malformed input, create their own child HTTP transport span, and install/close the selected telemetry runtime. The query matrix row is implemented and the aggregate gate now includes the exact relational and Kubernetes suites. The focused cohort passes 69/69; affected Applik8s, server, and compiler package typechecks pass; the exact `bun run check:v08:observability` candidate passes the matrix, 112/112 Rust host tests, 292/292 maintained primary tests, and 39/39 AI/agent tests; docs and diff checks pass. | This is deterministic runtime, generated-artifact, cancellation, and fail-isolation evidence. It does not qualify a real collector or process lifecycle. Prove root-to-remote-to-query parentage, multiplex cancellation, restart, redacted terminal outcomes, and malformed-carrier behavior against a real collector with verified cleanup. Non-command event propagation and the complete emitted-worker causal vertical are the next bounded cohorts. |
| `OB-1b2h` retry-aware non-command event delivery propagation | implemented | `5720f5d` | The transactional model boundary remains the producer and atomically persists its canonical child carrier with the event outbox. Relay preserves the exact carrier. One shared Applik8s executor validates internal telemetry, discards malformed carriers without changing delivery, and owns exactly one semantic event boundary per admitted JetStream or Kinesis delivery. JetStream maps durable delivery count to the physical attempt; Kinesis persists its attempt before application work so a post-commit checkpoint interruption resumes as retry attempt `2` rather than another live attempt. Processor attempts remain distinct linked boundaries, and lakehouse publication is an idempotent effect rather than a duplicate nested event span. Generated observed lakehouse workers install and close the selected OpenTelemetry runtime across normal, signal, and failed-runner teardown. All four affected package typechecks pass. The exact aggregate `bun run check:v08:observability` rebuilds publishable packages and passes the 13-boundary matrix, 112/112 Rust host contracts, 310/310 primary tests across 28 files, and 39/39 AI/agent tests across 5 files. `bash scripts/run-v08-observability-postgres-live.sh` separately passes the disposable-PostgreSQL model/outbox/relay/processor retry vertical with cleanup. The executable matrix reports 8 implemented, 5 in-progress, and 0 not-started boundaries. | This implements the carrier, semantic-owner, retry-attempt, malformed-input isolation, and generated-worker lifecycle contracts. It is not yet a real emitted application-process, JetStream/Kinesis backend, or collector receipt. Prove authenticated HTTP → PostgreSQL model/outbox → broker delivery → processor retry/checkpoint replay across process restart, one committed effect, redacted terminal outcomes, collector-visible linkage, and verified cleanup before advancing model/processor propagation or backend qualification. |
| `OB-1b2i` generated HTTP, PostgreSQL, JetStream, and lakehouse-publisher retry/restart vertical | implemented | `5e805af` | A generated application now runs as real child processes from authenticated HTTP through one RLS-scoped PostgreSQL model commit and one transactional event-outbox record, relay, a real disposable JetStream stream/consumer, and the generated lakehouse publisher. The test forces attempt 1 to fail without leaking its sensitive error, stops that publisher, restores the durable dataset path, and proves a new process completes attempt 2 with the same logical event execution identity, one model span, linked event attempts, exactly one model row, one outbox event, one manifest/object effect, no replay after acknowledgement, graceful SIGTERM shutdown, and complete PostgreSQL/NATS/temp cleanup. The slice fixed invalid JetStream durable names, stale runtime-schema initialization, missing reconstruction of the authored `(event, output)` callback contract, native dataset shutdown, a duplicated telemetry registry caused by a missing focused bundle alias, and raw terminal-error leakage in NATS/Kinesis logs and dead-letter envelopes. `bun run test:v08:observability-generated-process-live` passes 1/1; the focused compiler/NATS/AWS cohort passes 22/22; the exact `bun run check:v08:observability` checkpoint passes the 13-boundary matrix, 112/112 Rust host contracts, 311/311 primary tests, and 39/39 AI/agent tests; and the complete workspace typecheck passes 61 packages, four script shards, and root configs. | This is a real generated-process, native-PostgreSQL, JetStream, local-OTLP-receiver, retry/restart, and cleanup receipt. Its consumer is a lakehouse event publisher, not the separate stream-processor boundary. It does not qualify Kinesis live delivery, a package-managed or generic external OTLP collector, ClickStack, CloudWatch, workflow/task Hatchet lifecycle, or the remaining query/agent/provider/actor/reconciler process receipts. |
| `OB-1b2j` generated front-to-remote relational query restart and cancellation vertical | implemented | `b6b2169` | The generated-process fixture now exposes one ordinary RLS-scoped Drizzle query and bundles both the compiler-owned application Fetch gateway and its separately owned relational gateway as real Node child processes. Authenticated snapshots traverse the public `__applik8s` route before and after the relational gateway is stopped and restarted on the same endpoint while the front process stays live. A real multiplex subscription is established with its signed cursor, canceled while idle, and followed by another successful snapshot. Collector assertions require at least four distinct semantic query traces, each joined to its front HTTP transport trace, while a caller-authored malformed private carrier is absent from exported OTLP. The test retains the existing PostgreSQL model/outbox, JetStream retry/restart, lakehouse single-effect, redaction, graceful shutdown, and complete PostgreSQL/NATS/process/temp cleanup assertions. The receipt exposed and fixed schedule-runtime source being emitted into schedule-free Fetch gateways without its imports. `bun run test:v08:observability-generated-process-live` passes 1/1; the exact `bun run check:v08:observability` gate passes the 13-boundary matrix, 112/112 Rust host contracts, 317/317 primary tests, and 39/39 AI/agent tests; the complete typecheck passes 61 packages, four script shards, and root configs; and clean consumption packs 59 packages, imports 105 public entrypoints, and completes CLI plus Agentic Start generation/build. | This is a real generated front/remote process, native PostgreSQL, local OTLP capture, restart, cancellation, malformed-carrier, redaction, and cleanup receipt. The local collector's managed Docker lifecycle remains separately qualified by `OB-1c`; this test does not falsely claim that its inspectable receiver is that managed collector. Agent, provider, actor, reconciler, workflow/Hatchet, Kinesis, retained ClickStack, and CloudWatch process/backend receipts remain separate. |
| `OB-1c` package-managed local collector and generic external OTLP contract | implemented | `d83e075` | `Observability.local()` now lowers to the pinned official OpenTelemetry Collector Contrib image rather than the retained ClickStack image. The local supervisor owns one ephemeral, configuration-digested, lease-protected collector with loopback OTLP/HTTP and health endpoints, bounded memory/batch processing, all three signal pipelines, deterministic state, restart, stop, and reset. `Observability.otlp(...)` now freezes HTTP/protobuf, a non-empty signal subset, Secret-backed header authentication, system/custom-CA trust, and HTTPS/loopback constraints; local and Kubernetes lowering preserve exact Secret namespace identity and fail closed on missing or cross-namespace sources. Generated runtime processes all receive the selected endpoint and signal/auth/trust settings. Runtime OTLP export now includes redacted structured logs alongside traces and metrics with bounded queues/timeouts and remains nonblocking across outage, throttling, malformed responses, and recovery. ApplicationPlan exposes collector, protocol, endpoint, lifecycle, authentication, and TLS topology. `bun run test:v08:observability-local-collector-live` passes the real OrbStack Docker lifecycle including health, three receiver endpoints, concurrent-lease rejection, restart, reset, and absence verification. The exact aggregate observability gate passes 112/112 Rust host contracts, 315/315 primary tests, and 39/39 AI/agent tests after a clean publishable-package build; the full workspace typecheck passes 61 packages, four script shards, and root configs; compiler Kubernetes projection and namespace-denial tests pass; diff and focused Biome error checks pass. | The local collector is live-qualified as a lifecycle/receiver, not as a retained query backend. `OB-1e` separately qualifies the custom-trust transport. A genuinely remote external-provider collector lifecycle/receipt remains before claiming an arbitrary external backend is live-qualified. ClickStack/HyperDX TypeKro ingestion/query/deletion and real AWS CloudWatch remain entirely separate and unclaimed. |
| `OB-1d` managed ClickStack lifecycle, authenticated ingestion, and retained query backend | live-qualified | `aa20df6` | The deployment compiler lowers one bounded ClickStack provider name, external ClickHouse authority, generated Secret, and exact runtime-only `hyperdx-api-key` projection. Generated Kubernetes workloads receive the TypeKro-derived OTLP endpoint and SecretKeyRef authorization without serializing ClickHouse passwords, API-key values, or Helm values. TypeKro/Alchemy declarations remain the only apply/destroy authority; application teardown preflights TypeKro instances in reverse graph order and retains resumable state on incomplete cleanup. The exact final source now consumes released TypeKro `0.33.7` and passed the complete OrbStack receipt: all 14 resources were created, authenticated OTLP/HTTP traces, logs, and metrics were ingested from two in-cluster Job generations, all three ClickHouse signal tables were queried, redaction canaries were absent, and all 14 resources—including both owned Namespaces—were deleted through Alchemy/TypeKro. `bun run test:v08:observability-clickstack-live` passes 1/1 in 176 seconds with zero retained test graph resources. The same source passes the complete workspace typecheck (61 packages, four script shards, root configs), `bun run check:v08:observability` (112/112 Rust host contracts, 317/317 primary tests, 41/41 AI/agent tests), and clean consumption of 59 packages and 105 entrypoints. | This live-qualifies the managed ClickStack provider on local OrbStack through the production TypeKro/Alchemy lifecycle. It does not imply arbitrary external ClickStack topology, cross-cloud portability, or CloudWatch qualification. Custom-CA transport remains separately qualified by `OB-1e`; real AWS CloudWatch remains blocked on AWS authority. |
| `OB-1e` custom-CA external OTLP transport | live-qualified | `ba1d81e` | A real Node runtime exports an OTLP trace over HTTPS to an independently created receiver using an ephemeral self-signed CA, explicit DNS server identity, and a Secret-shaped authorization header. The exact custom CA plus matching server name succeeds; ordinary system trust and the same CA with a wrong server name both fail closed before an HTTP request reaches the receiver. Child-process output, TLS failures, and the test source do not disclose the authorization value or persist private key material. `bun run test:v08:observability-custom-ca-live` passes 1/1 after a clean publishable-package build. The exact aggregate `bun run check:v08:observability` includes this proof and passes the 13-boundary matrix, 112/112 Rust host contracts, 317/317 primary tests, and 39/39 AI/agent tests. The runtime-otel package typecheck, focused Biome check, and diff check pass. | This qualifies the generic custom-trust transport and fail-closed identity behavior, not a third-party remote collector's lifecycle, availability, retention, or query semantics. ClickStack exact-final-source rerun and TypeKro release consumption remain under `OB-1d`; CloudWatch still requires real AWS authority. |

| `OB-1b2k` function-native operation boundary closure | implemented | `4e9f46e` | Managed operation calls now have one stable synchronous semantic owner without author instrumentation. Compiler-generated HTTP and workflow/task scopes wrap declared operation execution; ambient transaction-local model handles and compiler-owned atomic handles do the same at their common runtime seams. Direct calls, extracted handles, and module-local helpers preserve the exact operation contract ID. AI tools retain their existing runtime-ai boundary and are deliberately not double wrapped. The helper-mediated transaction regression records the outer HTTP boundary followed by exactly one operation child, generated HTTP and workflow sources contain the same boundary, the focused runtime test passes 10/10, HTTP generation passes 5/5, workflow generation passes 16/16, both affected package typechecks pass, and the complete workspace typecheck passes all 61 packages, four script shards, and root configs. | This is deterministic runtime and emitted-source evidence. It does not independently qualify a collector backend or third-party provider, and it does not turn provider calls, model mutations, or processor deliveries into operation spans; those retain their own semantic owners. |
| `OB-1b2l` relational and Kubernetes model-boundary closure | implemented | `8421ae0` | PostgreSQL model telemetry now begins only after durable replay detection and remains active across handler execution, schema validation, pre-commit authorization, authoritative state/history/change/outbox/result writes, and typed rejection classification. Function-native transactions propagate the actual internal database retry attempt to nested model calls, while physical delivery and transaction attempts form a deterministic one-based attempt identity. Generated Kubernetes create commands use a server-owned model boundary around the mutation and idempotent conflict resolution. The default PostgreSQL suite passes 10/10; server Kubernetes coverage passes 5/5; generated Fetch coverage passes 21/21; the strengthened disposable PostgreSQL receipt passes both atomic commit/replay and intentional deadlock retry with attempt `2`, error/success completion assertions, and cleanup; affected package typechecks, focused Biome, and diff checks pass. | This closes semantic ownership for maintained relational and Kubernetes mutation paths. The Kubernetes proof is deterministic generated/server evidence rather than a live collector receipt. Durable PostgreSQL replay remains deliberately inert; an idempotent Kubernetes resubmission is an admitted command attempt and may still open a boundary while resolving the existing object. The final matrix row is the distinct stream-processor boundary. |
| `OB-1b2m` stream-processor boundary closure | implemented | `f781617` | Single-event and frozen-batch attempts link the exact producer carrier, preserve stable logical identity, record one-based physical attempts, classify explicit history replay without relabeling its retry, and emit bounded delivery-lag metrics plus redacted terminal outcomes. The generated worker receipt uses disposable native PostgreSQL and an inspectable OTLP receiver, intentionally fails attempt 1 with a secret canary, commits attempt 2, restarts the worker, resets the checkpoint to force replay, and proves the second lifetime follows the same retry path without duplicating the inbox, outbox, transition, or model effect. Collector assertions require four processor spans with `live/retry/live/retry`, two error and two success terminals, producer links, nonnegative lag, no canary leakage, and complete checkpoint/event/table cleanup. The runtime suite passes 21/21, the complete generated-reactive suite passes 24/24, `bun run test:v08:observability-processor-live` passes 1/1, and the full workspace typecheck passes 61 packages, four script shards, and root configs. | This closes all 13 semantic telemetry owners. It does not live-qualify Kinesis, an arbitrary broker, or an observability backend. The aggregate observability gate is temporarily blocked by TypeKro ClickStack owning a parent-owned Namespace; the isolated TypeKro root fix is committed as `56e44937` and passes 5,316 tests but needs publication/release before Applik8s can consume it. |

## Graph and plan evidence transitions

| Slice | State | Commit | Deterministic evidence | Remaining boundary |
| --- | --- | --- | --- | --- |
| `GP-1` canonical graph, ApplicationPlan, and semantic diff | integrated | `0dee112` | The plan uses Canonical JSON v1, records its source graph version, composes semantic and native target facets by stable identity, validates every first-class record, rejects sensitive and non-canonical values, and compares only compatible application/schema/graph versions. Diffs classify create/update/replace/delete/no-op changes across semantic, execution, dependency, authority, runtime-access, physical, lifecycle, native-plan, diagnostic, evidence, and provenance records. Provider guarantees resolve aliases and profile/target selections without inventing support. `bun run check:v08:graph-provenance` passes 66/66, including real ComponentizeJS/WASM compilation; `bun run check:v08:application-plan` passes 48/48 across core, compiler, CLI, GuestBook, Chirp, and Agentic Start; workspace typecheck covers 61 packages, four script shards, and root configs; clean packed consumption builds 59 packages, imports 105 public entrypoints, and completes CLI plus Agentic Start discovery/compiler/browser/server builds. | This is deterministic graph/plan qualification, not real AWS or Kubernetes lifecycle evidence. Chirp and Agentic Start retain the truthful AWS incompatibility for second-precision schedules. `RA-1` must now prove exact least-privilege lowering and enforcement; target-specific live claims stay with their own gates. |
| `RA-1a` exact semantic policy and provider-guarantee intersection | implemented | `f6649ae` | Runtime access retains exact per-execution requirements, source-graph and policy digests, provenance-linked lowerings, and collision-resistant workload identities. Typed Kubernetes permissions preserve namespaced/cluster scope; cross-namespace Roles, ClusterRoles, named Secret reads, status/finalizer verbs, wildcard rejection, explicit redundant/widened/unused diagnostics, and safe server materializer refusal are covered. Callable providers replace abstract secret access with exact runtime Secret identities. Provider-backed requirements intersect the selected target/profile guarantee manifest and unsupported providers fail closed. The expanded focused cohort passes 102/102, the exact runtime-access gate passes 22/22 including ComponentizeJS/WASM, adjacent graph-provenance passes 66/66, ApplicationPlan passes 48/48, and the full workspace typecheck passes all 61 packages, four script shards, and root configs. | This is deterministic planning evidence, not physical RBAC/IAM/network enforcement. Local child processes still inherit ambient `process.env`; policy ceilings, target materialization/preflight, live allowed/denied/narrowing/drift, and provider-limit evidence remain before `RA-1` can become integrated. |
| `RA-1b1` declared host-environment isolation | implemented | `d6a0bf5` | Local and production deployment now consume one compiler-derived generated-secret contract. The local plan records only exact host variable names and Secret identities; the supervisor resolves only referenced declarations, excludes their values from the plan, public state, logs, and generated credential store, and starts Node children with a closed toolchain allowlist instead of ambient `process.env`. A real child-process canary proves the declared credential arrives while an undeclared sibling credential does not; a compiler fixture proves Stripe reaches only its consuming host. `bun run check:v08:runtime-access` passes 123/123 including ComponentizeJS/WASM, `bun run check:v08:local` passes 24/24 including the GuestBook local lifecycle, 33 adjacent compiler/CLI tests pass, changed-file checks have no errors, and the full 61-package/four-script/root-config typecheck passes. | Framework-generated credentials are still over-projected within the closed boundary and must be narrowed per artifact. This remains deterministic/local lifecycle evidence, not physical Kubernetes/AWS enforcement or production `.env` qualification. |
| `RA-1b2` exact per-execution framework credentials | implemented | `afefc87` | The deployment contract now has one closed, portable framework-credential vocabulary. Every generated processor, workflow, HTTP, agent, MCP, reactive, and lakehouse executable records the exact credential environment names it consumes; the ApplicationHost records its own canonical requirements. Compiler bundles preserve that metadata, CLI ingestion validates it fail-closed, and the local supervisor creates only the union of required authorities while projecting only each process's declared subset. Production host emission and local host planning reuse the same compiler policy helper. Exact agent/HTTP/host sibling-denial tests pass. `bun run check:v08:runtime-access` passes 126/126 including ComponentizeJS/WASM, `bun run check:v08:local` passes 26/26 plus the real GuestBook local lifecycle, 35 focused contract/compiler/CLI tests pass, changed-file Biome checks have no errors, the full 61-package/four-script/root-config typecheck passes, and `bun run check:package-consumer` rebuilds and verifies 59 packages and 105 public entrypoints including Agentic Start. | This qualifies the portable contract and deterministic/local process isolation only. Kubernetes and AWS materializers must consume it as a mandatory pre-mutation enforcement input; physical RBAC/IAM/network allows and denials, narrowing, drift repair, ceilings, provider limits, and production `.env` qualification remain open under `RA-1c`/`RA-1d`. |
| `RA-1c1a` portable enforcement envelope | implemented | `4e00d7c` | `ApplicationRuntimeAccessPlan` is now a versioned deployment-contract artifact rather than a compiler-private side result. Kubernetes deployment graphs, AWS plans, and their canonical ApplicationPlans carry the same source-bound envelope and reject missing, malformed, target-mismatched, digest-mismatched, duplicated, wildcard, or unresolved policy before effect ownership. Compiler-owned source identity survives installation-value resolution. Generic callable-provider runtimes qualify structurally on maintained local/Kubernetes paths while malformed, target-missing, and AWS bindings fail closed. The runtime-access gate passes 127/127 including ComponentizeJS/WASM, the ApplicationPlan gate passes 48/48 across all three acceptance applications, focused contract/compiler tests pass 40/40, the packed-consumer gate passes all 59 packages and 105 entrypoints including external schedule-provider hydration, and the full 61-package/four-script/root-config typecheck passes. | This proves portable persistence and pre-mutation envelope validation, not materializer parity or physical enforcement. `RA-1c1b` must join emitted Kubernetes and AWS resources back to every requirement ID, distinguish Secret projection from API reads, remove first-role selection, and reject omissions/widening before `RA-1c2`/`RA-1c3` live qualification. |
| `RA-1c1b1` workload identity, RBAC, Secret, and IAM parity | implemented | `85dc385` | Runtime artifacts retain their exact execution-node union; unplaced access-bearing executions fail closed. Kubernetes pre-mutation parity joins Deployments, Jobs, and CronJobs to exact ServiceAccounts, Role/ClusterRole bindings, and kubelet Secret projections without inventing Secret API reads. Provider/profile-selected generated Secrets become source-attributed requirements for their exact consumers. AWS lowering creates one role per deliberate physical workload union, removes first-role selection and post-hoc policy appenders, preserves condition arrays, and rejects missing, widened, or misbound IAM, task-role, or declared private-peer plan edges. AWS-local provider-only planning scopes the subplan to its physically owned execution boundaries while the local supervisor owns application processes. The bounded target cohort passes 82/82 and `bun run check:v08:runtime-access` passes 133/133 including ComponentizeJS/WASM; the deployment-compiler typecheck and diff checks pass. | This is not complete network enforcement or live qualification. `RA-1c1b2` must define one provider-neutral private-peer record and lower it to NetworkPolicy and per-workload/per-target SecurityGroups; direct provider-owned TypeKro workloads need explicit descriptors; ECS execution-role Secret projection needs its own bootstrap record. Do not set the runtime-access acceptance flag from this partial row. |
| `RA-1c1b1a` compiler-artifact placement and exact host credential parity | integrated | `959f11e` | Generated ApplicationHost artifacts now retain every exact non-secret Kubernetes Secret name/key mounted by their compiler-authored environment and volumes, together with the complete co-located semantic execution set. Deployment planning consumes that artifact contract directly rather than treating rendered manifests as a policy source. ApplicationHost-hosted schedules therefore retain their physical placement and exact cursor, internal-operation, PostgreSQL, Hatchet, and profile-selected callable-provider credential union. Secret-volume parity now respects explicit `items` keys and continues to classify an unbounded volume as whole-Secret access. Focused compiler/deployment evidence passes 70/70; `bun run check:v08:runtime-access` passes 138/138 including ComponentizeJS/WASM; the complete workspace typecheck passes all 61 packages, four script shards, and root configs; and `bun run check:package-consumer` builds 59 packages, imports 105 public entrypoints, proves external schedule-provider hydration, and completes clean CLI plus Agentic Start generation/build. | This closes deterministic and clean packed-consumer placement/access parity. It does not replace `RA-1c2` live Kubernetes allow/deny/drift evidence, qualify a non-Cilium cluster, or supply the still-blocked exact-source ClickStack rerun. |
| `RA-1c1b2a` portable private-peer identity | implemented | `6f3a8d4` | One validated deployment-contract record now retains capability, requirement IDs, protocol, port, and exact Kubernetes or AWS endpoint identity. The former connection string list is deprecated and mechanically checked as a derivation. AWS bindings expose exact PostgreSQL, Valkey, Hatchet, and Celld ports/resources; maintained Kubernetes contributors expose NATS, Valkey, Postgres, Hatchet, and Celld namespace/Service/pod-selector identities without reading rendered TypeKro output. Hatchet's AWS/Kubernetes runtime-access guarantee now matches the already-maintained physical implementation. The bounded contract/compiler/provider/AWS cohort passes 86/86 and the filtered deployment package typecheck passes. | This is portable intent, not a deny boundary or physical enforcement. DNS bootstrap and declared external egress must be classified before generated NetworkPolicy; per-workload/per-target AWS SecurityGroups, provider-owned workload placement, ECS execution-role Secret projection, wrong-port/protocol parity, and all live allow/deny evidence remain open. |
| `RA-1c1b2b1` provider-owned direct workload placement | implemented | `4eedc44` | Deployment contributors can now supply source-attributed provider execution requirements and physical workload placement without making rendered TypeKro output a policy source. The portable plan distinguishes application-root from provider-direct materialization, supports StatefulSets, rejects duplicate execution placement, and validates every provider-direct owner against a graph-owned direct lifecycle node. Celld declares distinct worker-deployment and fleet execution boundaries, exact state/authorization Secret keys, application and state-store transports, and actor-to-fleet placement. The TypeKro adapter validates the owning direct declaration group plus compiler-authored root policies before Alchemy mutation; removing the fleet fails closed. `bun run check:v08:runtime-access` passes 136/136 including ComponentizeJS/WASM; the bounded contract/compiler/TypeKro cohort passes 98/98; and the filtered deployment package typecheck passes all 61 packages, four script shards, and root configs. | This qualifies deterministic placement/parity for direct manifests such as Celld's Job and StatefulSet. Celld's configured state-store DNS endpoint remains explicitly external until the FQDN extension is implemented, so no false NetworkPolicy claim is made. Helm-created provider children are not visible as pre-mutation workload manifests and remain unqualified pending a reviewed chart/workload evidence contract. Target-live allow/deny, drift, and cleanup evidence remains open. |
| `RA-1c1b2b2` exact AWS service and Kubernetes FQDN transports | implemented | `d9a38e1` | The portable runtime-access envelope now records exact AWS VPC service endpoints independently from IAM and distinguishes application runtime from framework-owned ECS bootstrap transport. Generated AWS plans synthesize shared interface/gateway endpoints, per-endpoint ingress SecurityGroups, workload-to-endpoint egress, S3/DynamoDB prefix-list rules, private DNS, and exact CloudFormation dependencies; parity rejects missing, widened, wrong-port, wrong-protocol, or misbound endpoint paths. Kubernetes workloads classify standard versus Cilium enforcement explicitly. The CLI observes the target instead of trusting authored configuration and selects Cilium only with an established v2 CRD, a fully ready agent fleet, and explicit L7 proxy enablement. Cilium policies use exact Kubernetes label sources, exact `toFQDNs.matchName`, and proxied cluster-DNS bootstrap rules; parity rejects an inert FQDN policy without DNS proxying. The TypeKro adapter uses its maintained Cilium factory in direct and KRO modes. The focused target cohort passes 73/73; `bun run check:v08:runtime-access` passes 136/136 including ComponentizeJS/WASM; the full workspace typecheck passes 61 packages, four script shards, and root configs; changed-file Biome reports warnings but no errors; and diff checks pass. | This is deterministic materialization/parity evidence. Read-only OrbStack preflight confirms Kubernetes 1.33.5, the Cilium v2 CRD, one fully ready agent, and `enable-l7-proxy=true`, but no allow/deny, drift, restart, or teardown mutation has yet been run. AWS endpoint evidence remains synthesized only; AWS-local cannot qualify real VPC endpoint semantics and real-AWS qualification remains open. Helm-created provider children remain deliberately unqualified pending stable pre-mutation workload evidence. |
| `RA-1c2` Kubernetes live runtime-access qualification | externally-blocked | `e08d1d5` | The new bounded gate compiles one semantic graph into a TypeKro/Alchemy deployment and asserts exact RBAC, Secret-key projection, private service/FQDN allows, sibling resource/Secret/wrong-port/wrong-host denials, pod restart, out-of-band CiliumNetworkPolicy deletion and plan/apply repair, and owned teardown. It uncovered and fixed raw compiler policies that bypassed TypeKro materialized envelopes, non-portable Cilium readiness in Alchemy artifact records, and incorrect Cilium label-source prefixing. Focused compiler/TypeKro evidence passes 40/40; the full workspace TypeScript check, changed-file Biome error check, and diff check pass. Two mutating OrbStack runs proved RBAC and Secret isolation but correctly failed because the denied port remained reachable; both destroyed 14 owned resources and left no namespace, image, or temp directory. Read-only agent inspection showed only host/health endpoints and no Kubernetes pod endpoints. The gate now rejects that environment in about three seconds before Docker pull or cluster mutation. | OrbStack's installed Cilium is not its active pod CNI, so it cannot provide dataplane evidence. Do not infer support from a CRD, ready DaemonSet, or `enable-l7-proxy` alone. Complete this row only on an explicitly authorized Cilium-managed cluster and retain every negative assertion, restart, drift, and cleanup receipt. The runtime-access acceptance flag remains false; real AWS and Helm-created child qualification remain separate. |

## Scheduling evidence transitions

| Slice | State | Commit | Deterministic evidence | Remaining boundary |
| --- | --- | --- | --- | --- |
| Portable schedule definition, occurrence identity, canonical admission, and local reference runtime | implemented | `0c55612` | `bun run check:v08:scheduling` passes 67/67 tests across the public contract, canonical local persistence, generated gateway, provider guarantees, AWS plan/Alchemy/runtime, Kubernetes runtime, and canonical graph; the complete 61-package workspace typecheck passes; exact tests cover immediate caller lineage, provider service admission, stable redelivery identity, revision convergence, bounded lateness, newest-N catch-up, and recovery of already-admitted work after its retry window | bounded history/retention, compiler-proven pure `overlapBy`, workflow cron and delayed-start lowering, general Hatchet provider, exact Kubernetes scheduled-time evidence, OrbStack lifecycle, and real EventBridge retry/DLQ/lifecycle/cost qualification remain; the scheduling acceptance flag stays false |
| Canonical dynamic-schedule desired state, provider projection recovery, and hosted workflow lowering | implemented | `d88f70e` | `bun run check:v08:scheduling` passes 71/71 tests; PostgreSQL lock-key regressions pass 2/2; `APPLIK8S_TYPECHECK_FILTER=package:e2e bun run typecheck` passes; `bun run test:v08:scheduling:orbstack` passes the live PostgreSQL restart, pending projection recovery, Kubernetes create/no-op, server drift repair, UID-preserving update, disable, idempotent delete, and owned cleanup lifecycle | the workflow-only control boundary is closed by `e5ae416` below; exact scheduled-time/duplicate/retry receipt evidence, bounded instance ceilings, general Hatchet lowering, and real AWS qualification remain; the scheduling acceptance flag stays false |
| Workflow-only schedule-control lowering for cron, delayed, and dynamic schedules | integrated | `e5ae416`, `4cbc5bf`, `959f11e` | `bun run check:v08:scheduling` passes 83/83 tests after building all publishable packages; compiler typecheck passes across all 61 packages, script shards, and root configs; focused artifact tests bundle a schedule-only gateway, exact workflow token and caller authority, PostgreSQL state credentials, least-privilege CronJob RBAC, Service/Deployment lifecycle, recurring and one-time CronJobs, delayed-occurrence self-cleanup metadata, shutdown, TypeKro artifact metadata, hosted-worker suppression, and qualified-scheduler exclusion. The clean packed-consumer gate now builds 59 packages, imports 105 public entrypoints, and proves the external schedule-provider fixture through the public compiler with exact ApplicationHost placement and cursor, internal-operation, database, Hatchet, and callable-provider credential parity. | Generated worker deployment/restart and exact Hatchet occurrence evidence remain; exact scheduled-time/duplicate/retry receipts, bounded instance ceilings, real provider firing/update/disable/delete/cleanup, and real AWS qualification remain; the scheduling acceptance flag stays false. |
| Qualified Hatchet delivery projection and provider-neutral occurrence authority | integrated | `44d32d8`, `550f6d9` | The shared PostgreSQL authority owns occurrence identity, overlap fencing, durable prior receipts, retry leases, and terminal-failure deduplication. Hatchet contributes only convergent fixed/dynamic/one-time delivery and transport evidence into canonical admission. Five-field cron lowering preserves only exactly representable cadence; elapsed schedules use UTC and non-UTC calendar cron fails before canonical or provider mutation. The scheduling gate passes 165/165, provider conformance 37/37, clean packed consumption 59 packages/102 entrypoints, workspace typecheck 61 packages/four script shards/root configs, and OrbStack occurrence recovery 3/3 with cleanup. | Do not set scheduling acceptance or `live-qualified` from this row. Real Hatchet/TypeKro firing, update/disable/delete, interruption, drift, shared-engine uninstall, exact provider scheduled-time, and leak-free teardown remain; exact Kubernetes CronJob scheduled-time propagation and real EventBridge qualification remain separate open rows. |

## Lakehouse evidence transitions

| Slice | State | Commit | Deterministic evidence | Remaining boundary |
| --- | --- | --- | --- | --- |
| `LH-1a` immutable authority, admitted queries, and provider differential semantics | implemented | `e907d11`, `e410c17`, `147846b`, `f3b7399`, `a382999` | Local publication is serialized and interruption-safe; AWS publication uses S3 compare-and-swap authority and concurrent writers form one canonical chain; persisted DuckDB and AWS authorities retain metadata, digests, cardinality, schema, frontier, and bounded causal receipts without row payloads; DuckDB rehydrates only referenced immutable objects after restart; public query calls derive cursor scope from canonical admission; Athena admission is retry-addressable and reports unknown outcomes truthfully; deterministic, DuckDB, and simulated Athena providers pass one exported comparison/null/tie-order conformance kit; both lakehouse provider interfaces publish exact machine-readable guarantees. `bun run check:v08:lakehouse` rebuilds all publishable packages and passes 130/130 tests; all five affected package typechecks and diff checks pass. | This is deterministic and simulated-provider evidence, not real AWS qualification. AWS still needs lease-safe staged-object/snapshot/catalog garbage collection, interruption recovery and lifecycle receipts; provider materialization must remain bounded; the generated Agentic Start needs a real local historical-usage publication/query/UI receipt; and real Athena/Glue/S3 IAM, encryption, catalog propagation, cancellation, drift, cost, retention, and cleanup remain required before the beta AWS vertical is complete. |
| `LH-1b` lease-safe AWS retention and interruption recovery | implemented | `02f5fc9` | The AWS dataset runtime creates a staging lease before reading publication authority, refuses cleanup while any live publication lease exists, acquires one compare-and-swap maintenance lease before destructive work, removes expired staging records, deletes only unretained immutable data/manifest/link objects, and drops only owned Glue tables. Lifecycle receipts report deleted and retained identities without promoting cleanup to publication authority. Focused tests cover active-publication exclusion, retained-snapshot cleanup, and recovery of an interrupted publication after staging expiry. The exact `bun run check:v08:lakehouse` gate rebuilds publishable packages and passes 132/132 tests. | This is deterministic simulated-S3/Glue evidence. Real AWS IAM, encryption, catalog propagation, cancellation, drift, cost, retention timing, concurrent long-running maintenance, and complete teardown remain required for the beta AWS claim. The maintenance lease is bounded and publication stays fail-closed while it is active; real-provider duration evidence must establish or tune the lease window. |

## Worktree classification

| Worktree | Authority | Current classification |
| --- | --- | --- |
| `/private/tmp/applik8s-v072-release` | detached at current `origin/main` | Eight dirty canonical source/test files contain schema, native-model, and workflow fixes requiring reconciliation |
| `/private/tmp/applik8s-http-workflow` | focused branch | Two unique commits form the narrow HTTP-to-workflow candidate |
| primary older workspace | stale release branch | Two dirty workflow files contain the declared durable non-retryable error fix; do not merge branch wholesale |
| older agentic worktree | stale, far behind main | Untracked content is generated/hidden build and deployment output after filtered inventory; no source branch merge is permitted |
| active v0.8 worktree | implementation authority after checkpoint | Preserve complete checkpoint series, then rebase and reconcile |

## Exit audit

Phase 2 exits only when:

1. every pending row names an exact canonical commit and passing command;
2. all five downstream package patch categories map to source fixes or explicit
   still-open findings;
3. the focused HTTP-workflow candidate is independently reviewed;
4. the active v0.8 branch contains one deduplicated implementation; and
5. no source change remains unique to an uncommitted worktree.
