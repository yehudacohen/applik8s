# Applik8s v0.7 scorecard

This scorecard is the release authority for the v0.7 Agentic Platform charter. It is intentionally
stricter than a package inventory: a capability is complete only when its public contract, maintained
implementation, generated-consumer path, and required live evidence all exist.

The machine-readable authority is
[`v0.7-scorecard.json`](./v0.7-scorecard.json). Run:

```sh
bun run check:v07:scorecard
bun run check:v07:scorecard:release
bun run check:v07:local
bun run check:v07:prerelease:orbstack
```

The first command validates the scorecard, checked-in contract evidence, required ownership categories,
and pinned compatibility boundaries. The legacy aggregate generated-file ceiling is diagnostic only;
release review uses categorized ownership, largest-file, dependency-direction, and readability budgets.
The release command additionally rejects every partial, pending, or blocked
item and requires schema-v3 receipts tied to the exact commit, complete working-tree digest, current
cluster UID, and classified per-assertion observations. A green contract scorecard is therefore not a
release claim.

`check:v07:local` is the complete source, package, contract, browser-security,
profile, performance-history, TypeKro, the Agentic product baseline, maintained-application build, and
Rust host gate. `check:v07:prerelease:orbstack` runs that exact local gate, both
exact compiler-emitted PostgreSQL workers (event and authenticated typed HTTP),
the durable-signal PostgreSQL recovery suite, the graph lifecycle, the complete
maintained live application cohort, and the Starter, Dedicated, and External
Identity Start profiles before asking the release scorecard for authorization.
It cannot turn a partial scorecard item green by merely running unrelated
tests or by relying on receipts from an earlier source tree.

## Current assessment

| Dimension | State | Evidence and remaining boundary |
| --- | --- | --- |
| Baseline disposition | Complete | `v07-baseline-and-contract-inventory.md` defines the versioned Agentic product baseline and records preserved, improved, deferred, and rejected behavior. |
| Operation and authority model | Complete | One operation catalog, canonical grants, workload envelopes, execution principals, bindings, receipts, explain output, HTTP/MCP/agent reuse, and fail-closed ambiguity tests. |
| Universal causal attribution | Complete | Framework-owned durable metadata preserves the original initiating principal across workflows, child workflows, tasks, event processors, reconcilers, agents, and nested agents without granting that principal ambient authority. Tasks admit the preserved lineage into their execution principal, nested agents retain the human rather than the caller agent, reconciler starts use their authenticated service-account lineage, and missing lineage on an existing execution principal fails closed. Runtime and compiler regressions cover durable serialization, child inheritance, task admission, nested-agent propagation, and generated processor/workflow lowering. Generated processors now give both the authored callback and its downstream delivery a processor-scoped execution principal, while the original initiating human remains the immutable `causalPrincipalId`; the worker cannot accidentally inherit the human's ambient authority. A live generated-processor e2e (`causal-chain-generated-worker-live.e2e.test.ts`) proves that boundary survives the processor hop and remains durable in downstream stream events. |
| Generated route reproducibility | Complete | Agentic Start and all maintained examples use one exact upstream-compatible TanStack Start/Router/Router CLI tuple. Route generation is repeated in an isolated directory and compared byte-for-byte with each tracked tree, while the packed generated application proves a second generation is a no-op. Example typechecking no longer silently rewrites tracked source. |
| Provider-native models | Complete | Drizzle, Kubernetes/CRD, analytical, and framework models retain their native schema authority while sharing operation/event/authority facets. |
| Function-native callback discovery | Complete | Inline, named, imported, and recursively reachable local helpers carry compiler-owned dependency metadata. `Resource.on.reconcile(...)` is the sole public reconciliation spelling, automatically owns its generated operator installation, and identifies direct workflow starts without exposing `job.track()` as an application dependency. Neither the root package nor `/dsl` exports `task()`: provider tasks survive only as an internal lowering target for ordinary callable closures and workflows. |
| Function-native model transactions | Complete | Promoted Drizzle models expose typed `require` and `edit(identity, closure)` methods, while the old command/action/operation registries are absent at type, runtime, and packed-package boundaries. Recursive compiler inference records writable models, nested read participants, and event outboxes. Generated event/batch workers, model-lifecycle processors, workflow steps, ordinary exported agent tools, and authenticated typed HTTP routes all enter the canonical durable command kernel. `app.http` records its contract once and emits one compiler-owned OCI worker—no raw source ConfigMap or provisional `app.server` workload remains. Its boundary pins provider-authenticated principals to the compiled catalog, derives change scopes from server-held authority, validates schemas, rejects raw request capture, and requires context-scoped idempotency. Exact emitted-worker PostgreSQL gates prove atomic event processing and HTTP `Model.edit`, duplicate recovery, restart recovery, and one durable effect. |
| Frozen stream batches | Complete | The immutable admitted-batch contract, bounds, retry-stable identity, whole-batch checkpoint semantics, and retry-safe durable receipts have focused compiler/runtime evidence plus Chirp's live JetStream-to-PostgreSQL batch path. The acceptance receipt proves the generated worker commits one frozen batch before acknowledgement and survives the broader restart suite. |
| Typed durable signals | Complete | Canonical signal storage, transaction/outbox behavior, action hydration, exact-instance visibility, authorization, and PostgreSQL recovery have focused and live evidence. Chirp emits a typed one-shot signal from a durable workflow, delivers the issuance over SSE, survives browser reload, resolves it without accepting actor identity from the browser, and resumes with framework-derived actor/receipt metadata. |
| Resource/workflow tracking | Complete | Canonical CR status, idempotent restart adoption, finalizers, generation convergence, cancellation policy, recovery, and the focused live Hatchet gate are complete. Chirp's `ModerationPolicy.on.reconcile(...)` exercises the ordinary public path against the exact deployed workflow gateway; the live test proves a changed owned resource converges to the current UID/generation and a stable run/result without hot-loop admission. |
| Single-definition model authoring | Complete | `model(...)` is the schema authority and remains directly Drizzle-compatible; application facets are attached without a second `application.model(...)` promotion declaration. Generated Agentic Start uses this surface. |
| AI and TanStack AI | Complete | Provider-neutral AI contracts, TanStack adapter, server runtime, attempt-scoped tool idempotency, streaming, and graph lowering are implemented. The published TanStack `MessageStore`/`RunStore` lifecycle is adapted to the canonical principal-scoped Applik8s conversation store; generated product and research clients use server-authoritative `persistence: true` hydration through the admitted gateway rather than reconstructing a second transcript. The exact Dedicated profile converged Envoy AI Gateway and the maintained browser journey executed the exported agent through its declared typed model operation. |
| Identity and OAuth | Complete | Provider-neutral identities, admission, OAuth, Ory adapters, grants, revocation, sessions, and transport tests are implemented. Starter, Dedicated, and External Identity Start receipts prove the maintained identity application, with the Dedicated profile carrying the Ory implementation. |
| MCP | Complete | Catalog-pinned server/client contracts, typed tools/resources, authorization discovery, transport, versioning, and persistence tests are implemented. The maintained Identity Start graph deployed its MCP artifact in all three profiles and the Dedicated workload passed restart qualification. |
| Search | Complete | Named relationship-aware indexes, PostgreSQL/OpenSearch runtimes, rebuild/cutover/history-loss semantics, and graph/compiler coverage exist. The Dedicated and External provider receipts prove OpenSearch readiness, while Chirp proves bounded typed search and analytical ranking through the public browser path. |
| Maintained modules | Complete | Conversations, Documents, Agents, Knowledge, Integrations, approvals, Artifacts, evaluations, billing, usage, and generic data-lifecycle state now declare reusable protocols, models, or queries once through maintained packages. Declaration-owned identity/revision semantics survive package emission and generated-project discovery, and database auto-binding plus explicit module composition are idempotent rather than installing duplicate operations. Generated source deliberately retains product nouns, policy, provider choice, shell, routes, typed contributions, and journeys: Documents, Inbox/reviews, Library, workspace/account lifecycle policy, plans, entitlements, and branding do not disappear behind a universal starter framework. `@applik8s/operations-ui/health` is a browser-safe reducer; authority, PostgreSQL, signing, and receipt machinery remain outside browser chunks. The exact placement rule and matrix are normative in `agentic-start-source-ownership.md`, and the source gate rejects known copies of maintained mechanisms. |
| Agentic Start generator | Complete | The pinned official TanStack CLI produces a cohesive responsive light/dark product shell, persistent application-owned onboarding, resumable conversations, document/artifact detail, workspace administration, provider-neutral usage/billing, classified route states, public admission/recovery routes, and production browser/server builds. The Inbox is a real durable workflow/signal vertical; account controls expose bounded visible-data controls; conversation trust disclosure names the logical model, resolved provider boundary, tools, authority, data sharing, and uncertain completion. Vite serve installs only deterministic framework-owned local defaults and preserves explicit `.env` values. The post-boundary packed-consumer gate packs 54 packages, imports every public entrypoint, generates the application, emits a Drizzle migration and reproducible route tree, discovers and compiles its graph, typechecks, tests, and completes its client/server production build. The earlier OrbStack/browser receipt remains historical until this exact package-boundary candidate is rerun live. Categorical and semantic ownership budgets prevent monolithic generated code. |
| Starter profile | Complete | Exact-candidate Starter receipts prove credential-free generation, migration, production build, deterministic notification/billing behavior, human admission, typed operations, agent invocation, durable SSE decisions, stable readiness, zero-change reapply, browser requery, and graph-backed teardown. |
| Dedicated profile | Complete | Exact-candidate Dedicated evidence converges CNPG, ClickHouse, JetStream, Hatchet, Rook/Ceph, OpenSearch, Ory, Envoy AI Gateway, object storage, hosting, and generated workers; then proves provider-derived identity/authority, durable approval, MCP, redacted search, rolling-restart recovery, and graph-backed teardown. |
| External profile | Complete | Exact-candidate External evidence proves explicitly owned provider bindings, provider readiness, no accidental bootstrap/adoption, typed application behavior, application teardown that preserves providers, and a separate provider-owner graph teardown. |
| Chirp acceptance | Complete | The maintained application compiles and deploys as one TypeKro/Alchemy graph, a consecutive deployment is a 51-resource no-op, and the exact OrbStack suites prove the SSR→JetStream→PostgreSQL→Valkey→SSE→ClickHouse path, frozen batches, typed durable signals, canonical resource/workflow tracking, complete Valkey rebuild, acknowledged-work restart recovery, and all twelve browser journeys. The RFP matrix, scorecard, executable acceptance manifest, and candidate receipts use the same assertion identities. |
| GuestBook acceptance | Complete | The concise `GuestBookEntry.on.reconcile(...)` application passes production Vite/Nitro and package-consumer gates. Its exact-candidate OrbStack receipt covers browser command admission, Kubernetes reconciliation, rejection, SSE invalidation, authoritative requery, SSR, restart, and graph-backed deletion. |
| Agentic identity acceptance | Complete | The executable seventeen-requirement manifest covers human and machine admission, OAuth, typed authority, runtime grants, bounded delegation, MCP, outcome verification, migration, denial, and isolation. The generated Identity Start passes production browser/server builds and all Starter, Dedicated, and External live profiles; the Dedicated profile supplies provider-derived Ory identity and every profile passes the typed durable-signal and exported-agent browser journeys. |
| Agentic product baseline | Complete | Twelve product paths, eight product areas, nine acceptance journeys, and seven generated product contracts are mechanically mapped and executable. Applik8s preserves or improves acquisition, onboarding, persistent conversations, billing, notification delivery, cohesive source-owned UI, environment-driven deployment, and recovery through its provider-neutral application graph and function-native runtime. |
| Package and browser boundaries | Complete | The authoritative catalog documents all 54 publishable package and module boundaries exactly once, and an executable gate compares it with the package manifests. Packed imports and builds, principal derivation, browser/server separation, object intents, signal confidentiality, cursors, operations redaction, notification transport, public admission continuity, safe brand metadata, and update-check path safety pass the maintained threat matrix. The product browser gate adds zero-error direct navigation and recovery across Chromium, Firefox, WebKit, and mobile. Operator Launchpad receipt authority remains intentionally isolated in its own completed item. |
| TypeKro provider boundary | Complete | The exact TypeKro 0.33.7, Alchemy beta.58, and Effect beta.84 cohort passes released-package, semantic planning, stable artifact-binding, adapter, direct/KRO lifecycle, profile, and External ownership gates. Applik8s consumes TypeKro's composition, readiness, output, dependency, migration, and teardown machinery rather than reimplementing it. |
| Integrated OrbStack lifecycle | Complete | The exact candidate proves direct failure/resume, direct/KRO apply, true no-op, update, instance-before-RGD destruction, final Namespace/RGD absence, generated-secret delete/retain/explicit cleanup, and external adoption. All successful cleanup uses Alchemy/TypeKro; no graph resource or finalizer is deleted imperatively. |
| Performance and cost evidence | Complete | A checked 51-run local v0.7 history covers authority decisions with retained audit, typed signal issuance/resolution/outbox creation, and frozen-batch convergence under explicit throughput, p95, memory, and bounded-concurrency budgets. A separate live OrbStack baseline exercises the generated application's real PostgreSQL hot/distributed contention, one/two/four-consumer JetStream convergence, concurrent SSR requests, pod-ready latency, restart count, and aggregate requested/limited CPU and memory. Both receipts identify the exact candidate and state their limitations; neither is represented as EKS, sustained-soak, internet-edge, or dollar-cost capacity. |
| Planning authority and evidence consistency | Complete | The accepted charter, bounded RFP contracts, function-native conformance rows, the Agentic product baseline capability map, machine-readable scorecard, executable acceptance manifest, and release-evidence assertion catalog are checked together. The gate rejects state drift, missing source assertions, capability/scorecard mismatches, unknown evidence scripts, and receipts that do not carry the required exact assertion identities; path existence alone is not release evidence. |
| Function-native one-shot query | Complete | Relational, analytical, and Kubernetes-backed models expose `Model.query(contract, implementation)` as a distinct one-shot declaration alongside persistent `Model.view(...)`; named identity, compiler capture, direct awaiting, authority, source, read, and budget contracts share the established generated-query path. |
| Public admission and notification | Complete | Landing, sign-up/sign-in, provider-neutral verification/recovery, intended-route resumption, account MFA/session capability shaping, invitation continuation, the transactional notification module, deterministic Starter delivery, and SMTP delivery are implemented. Starter product evidence proves authenticated application invitation delivery and continuation; Dedicated identity evidence proves Ory-backed human admission and provider-derived roles; focused notification contracts cover idempotent delivery and failure classification. |
| Operator Launchpad authority | Complete | `application.role('application-operator').bootstrap(...)` materializes one exact provider-verified role-level lease. The canonical authority store makes that lease permanently inert after first assignment, supports role-wide revocation, and exposes a separately bounded 24-hour break-glass path with explicit acknowledgement and audit. Generated session admission strips unbacked provider claims and restores the role only from canonical grants. `applik8s operator bootstrap/revoke/break-glass` use the compiled catalog, authority manifest, and explicit application database environment. Plan, deploy, and status write redacted integrity-protected receipts bound to source graph, deployment graph, artifact set, installation, cluster, schema revision, and a four-hour evidence window; Launchpad reads only their ordinary authorized projection and never receives kubeconfig or mutation authority. Focused tests reject subject drift, reopened bootstrap, overlong recovery, receipt tampering, staleness, context mismatch, and sensitive evidence. |
| Start lineage/update check | Complete | `applik8s start update --check` compares deterministic template and managed `package.json` lineage, preserves application-owned package entries, rejects path/symlink escape, and classifies conflicts plus compatibility/security relevance without mutation. Focused cross-version security-drift tests and the clean packed-consumer CLI path prove the contract; automatic rewriting stays deferred. |
| Product data lifecycle and AI trust | Complete | The generated product distinguishes mutable Documents from immutable Artifacts and replaces browser-orchestrated deletion with one awaited typed `DataLifecycleRequest.create(...)`. `@applik8s/data-lifecycle` owns only provider-neutral request state, generic progress, and bounded coordination storage; the generated product owns the account/workspace vocabulary, causal admission, blockers, batching, deletion/archive targets, retained-evidence consequences, and UI. Export excludes credentials, object keys, receipt bodies, and conversation bodies. The conversation surface names logical model, server-side provider resolution, declared tools, causal authority, data boundary, cancellation, and uncertain completion. The AI runtime projects redacted canonical attempt observations; uncertain completion is degraded, quarantined, and marked for operator review without prompt or provider content. |
| Cross-browser product quality | Complete | The exact generated product passes 23 journeys across Chromium, Firefox, WebKit, and mobile. Evidence covers every first-run route, responsive product surfaces, keyboard/semantic navigation, dark mode, reduced motion, SSR/hydration integrity, bounded navigation, degraded-network live-query recovery, and zero unexpected browser/server failures. |

## Release policy

`v0.7.0` must not be tagged while any machine-readable scorecard item is `partial`, `pending`, or
`blocked`. External prerequisites remain visible as blocked items; they are not converted into passing
claims by fixture metadata. Multi-cluster qualification is the only explicitly deferred item and does
not block v0.7 because the charter excludes work requiring a second Kubernetes authority.
