---
title: Diagnostic reference
description: Every stable Applik8s diagnostic, its owner, source, and bounded recovery entrypoint.
---

This generated reference is derived from the same checked public diagnostic inventory consumed by
`applik8s explain`. Every cataloged code has a stable anchor, machine-readable owner and source, and a
bounded recovery path. More detailed authored advice overrides the general contract in the CLI.

<a id="diagnostic-abi-incompatible"></a>

## `ABI_INCOMPATIBLE`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/common.ts`
- Explain: `applik8s explain ABI_INCOMPATIBLE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-admission-authority-mismatch"></a>

## `ADMISSION_AUTHORITY_MISMATCH`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-admission.ts`
- Explain: `applik8s explain ADMISSION_AUTHORITY_MISMATCH`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-admission-context-invalid"></a>

## `ADMISSION_CONTEXT_INVALID`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-admission.ts`
- Explain: `applik8s explain ADMISSION_CONTEXT_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-admission-deadline-expired"></a>

## `ADMISSION_DEADLINE_EXPIRED`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-admission.ts`
- Explain: `applik8s explain ADMISSION_DEADLINE_EXPIRED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-admission-identity-invalid"></a>

## `ADMISSION_IDENTITY_INVALID`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-admission.ts`
- Explain: `applik8s explain ADMISSION_IDENTITY_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-admission-operation-invalid"></a>

## `ADMISSION_OPERATION_INVALID`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-admission.ts`
- Explain: `applik8s explain ADMISSION_OPERATION_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-admission-receipt-invalid"></a>

## `ADMISSION_RECEIPT_INVALID`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-admission.ts`
- Explain: `applik8s explain ADMISSION_RECEIPT_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-admission-trace-invalid"></a>

## `ADMISSION_TRACE_INVALID`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-admission.ts`
- Explain: `applik8s explain ADMISSION_TRACE_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-admission-version-invalid"></a>

## `ADMISSION_VERSION_INVALID`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-admission.ts`
- Explain: `applik8s explain ADMISSION_VERSION_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-applik8s-saga-database-url"></a>

## `APPLIK8S_SAGA_DATABASE_URL`

- Owner: `package:@applik8s/compiler`
- Defining source: `packages/compiler/src/application-workflows/contracts.ts`
- Explain: `applik8s explain APPLIK8S_SAGA_DATABASE_URL`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-authority-bound-field-override"></a>

## `AUTHORITY_BOUND_FIELD_OVERRIDE`

- Owner: `workspace:cross-package`
- Defining sources: `packages/applik8s/src/task-operation-runtime.ts`, `packages/core/src/application-operation-authority.ts`
- Explain: `applik8s explain AUTHORITY_BOUND_FIELD_OVERRIDE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-authority-break-glass-acknowledgement-required"></a>

## `AUTHORITY_BREAK_GLASS_ACKNOWLEDGEMENT_REQUIRED`

- Owner: `package:@applik8s/operations`
- Defining source: `packages/operations/src/authority.ts`
- Explain: `applik8s explain AUTHORITY_BREAK_GLASS_ACKNOWLEDGEMENT_REQUIRED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-authority-catalog-incompatible"></a>

## `AUTHORITY_CATALOG_INCOMPATIBLE`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-operation-authority.ts`
- Explain: `applik8s explain AUTHORITY_CATALOG_INCOMPATIBLE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-authority-catalog-migration-invalid"></a>

## `AUTHORITY_CATALOG_MIGRATION_INVALID`

- Owner: `package:@applik8s/operations`
- Defining source: `packages/operations/src/authority.ts`
- Explain: `applik8s explain AUTHORITY_CATALOG_MIGRATION_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-authority-delegation-broadening"></a>

## `AUTHORITY_DELEGATION_BROADENING`

- Owner: `workspace:cross-package`
- Defining sources: `packages/core/src/application-operation-authority.ts`, `packages/operations/src/authority.ts`
- Explain: `applik8s explain AUTHORITY_DELEGATION_BROADENING`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-authority-delegation-unavailable"></a>

## `AUTHORITY_DELEGATION_UNAVAILABLE`

- Owner: `package:@applik8s/operations`
- Defining source: `packages/operations/src/authority.ts`
- Explain: `applik8s explain AUTHORITY_DELEGATION_UNAVAILABLE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-authority-duplicate-operation"></a>

## `AUTHORITY_DUPLICATE_OPERATION`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-operation-authority.ts`
- Explain: `applik8s explain AUTHORITY_DUPLICATE_OPERATION`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-authority-execution-binding-invalid"></a>

## `AUTHORITY_EXECUTION_BINDING_INVALID`

- Owner: `package:@applik8s/applik8s`
- Defining source: `packages/applik8s/src/task-operation-runtime.ts`
- Explain: `applik8s explain AUTHORITY_EXECUTION_BINDING_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-authority-execution-binding-missing"></a>

## `AUTHORITY_EXECUTION_BINDING_MISSING`

- Owner: `package:@applik8s/applik8s`
- Defining source: `packages/applik8s/src/task-operation-runtime.ts`
- Explain: `applik8s explain AUTHORITY_EXECUTION_BINDING_MISSING`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-authority-grant-conflict"></a>

## `AUTHORITY_GRANT_CONFLICT`

- Owner: `package:@applik8s/operations`
- Defining source: `packages/operations/src/authority.ts`
- Explain: `applik8s explain AUTHORITY_GRANT_CONFLICT`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-authority-grant-unavailable"></a>

## `AUTHORITY_GRANT_UNAVAILABLE`

- Owner: `package:@applik8s/operations`
- Defining source: `packages/operations/src/authority.ts`
- Explain: `applik8s explain AUTHORITY_GRANT_UNAVAILABLE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-authority-invalid-operation-id"></a>

## `AUTHORITY_INVALID_OPERATION_ID`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-operation-authority.ts`
- Explain: `applik8s explain AUTHORITY_INVALID_OPERATION_ID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-authority-invalid-reservation-transition"></a>

## `AUTHORITY_INVALID_RESERVATION_TRANSITION`

- Owner: `package:@applik8s/operations`
- Defining source: `packages/operations/src/authority.ts`
- Explain: `applik8s explain AUTHORITY_INVALID_RESERVATION_TRANSITION`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-authority-invalid-scope"></a>

## `AUTHORITY_INVALID_SCOPE`

- Owner: `workspace:cross-package`
- Defining sources: `packages/core/src/application-operation-authority.ts`, `packages/operations/src/authority.ts`
- Explain: `applik8s explain AUTHORITY_INVALID_SCOPE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-authority-manifest-identity-conflict"></a>

## `AUTHORITY_MANIFEST_IDENTITY_CONFLICT`

- Owner: `package:@applik8s/operations`
- Defining source: `packages/operations/src/authority.ts`
- Explain: `applik8s explain AUTHORITY_MANIFEST_IDENTITY_CONFLICT`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-authority-manifest-revision-mismatch"></a>

## `AUTHORITY_MANIFEST_REVISION_MISMATCH`

- Owner: `package:@applik8s/operations`
- Defining source: `packages/operations/src/authority.ts`
- Explain: `applik8s explain AUTHORITY_MANIFEST_REVISION_MISMATCH`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-authority-origin-conflict"></a>

## `AUTHORITY_ORIGIN_CONFLICT`

- Owner: `package:@applik8s/operations`
- Defining source: `packages/operations/src/authority.ts`
- Explain: `applik8s explain AUTHORITY_ORIGIN_CONFLICT`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-authority-outcome-conflict"></a>

## `AUTHORITY_OUTCOME_CONFLICT`

- Owner: `package:@applik8s/operations`
- Defining source: `packages/operations/src/authority.ts`
- Explain: `applik8s explain AUTHORITY_OUTCOME_CONFLICT`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-authority-outcome-self-verification"></a>

## `AUTHORITY_OUTCOME_SELF_VERIFICATION`

- Owner: `package:@applik8s/operations`
- Defining source: `packages/operations/src/authority.ts`
- Explain: `applik8s explain AUTHORITY_OUTCOME_SELF_VERIFICATION`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-authority-outcome-unavailable"></a>

## `AUTHORITY_OUTCOME_UNAVAILABLE`

- Owner: `package:@applik8s/operations`
- Defining source: `packages/operations/src/authority.ts`
- Explain: `applik8s explain AUTHORITY_OUTCOME_UNAVAILABLE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-authority-permission-not-grantable"></a>

## `AUTHORITY_PERMISSION_NOT_GRANTABLE`

- Owner: `package:@applik8s/operations`
- Defining source: `packages/operations/src/authority.ts`
- Explain: `applik8s explain AUTHORITY_PERMISSION_NOT_GRANTABLE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-authority-permission-unavailable"></a>

## `AUTHORITY_PERMISSION_UNAVAILABLE`

- Owner: `package:@applik8s/operations`
- Defining source: `packages/operations/src/authority.ts`
- Explain: `applik8s explain AUTHORITY_PERMISSION_UNAVAILABLE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-authority-request-conflict"></a>

## `AUTHORITY_REQUEST_CONFLICT`

- Owner: `package:@applik8s/operations`
- Defining source: `packages/operations/src/authority.ts`
- Explain: `applik8s explain AUTHORITY_REQUEST_CONFLICT`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-authority-request-invalid-state"></a>

## `AUTHORITY_REQUEST_INVALID_STATE`

- Owner: `package:@applik8s/operations`
- Defining source: `packages/operations/src/authority.ts`
- Explain: `applik8s explain AUTHORITY_REQUEST_INVALID_STATE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-authority-request-unavailable"></a>

## `AUTHORITY_REQUEST_UNAVAILABLE`

- Owner: `package:@applik8s/operations`
- Defining source: `packages/operations/src/authority.ts`
- Explain: `applik8s explain AUTHORITY_REQUEST_UNAVAILABLE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-authority-reservation-not-found"></a>

## `AUTHORITY_RESERVATION_NOT_FOUND`

- Owner: `package:@applik8s/operations`
- Defining source: `packages/operations/src/authority.ts`
- Explain: `applik8s explain AUTHORITY_RESERVATION_NOT_FOUND`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-authority-reservation-required"></a>

## `AUTHORITY_RESERVATION_REQUIRED`

- Owner: `package:@applik8s/operations`
- Defining source: `packages/operations/src/authority.ts`
- Explain: `applik8s explain AUTHORITY_RESERVATION_REQUIRED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-authority-retired-operation"></a>

## `AUTHORITY_RETIRED_OPERATION`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-operation-authority.ts`
- Explain: `applik8s explain AUTHORITY_RETIRED_OPERATION`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-authority-revocation-conflict"></a>

## `AUTHORITY_REVOCATION_CONFLICT`

- Owner: `package:@applik8s/operations`
- Defining source: `packages/operations/src/authority.ts`
- Explain: `applik8s explain AUTHORITY_REVOCATION_CONFLICT`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-authority-revocation-invalid-state"></a>

## `AUTHORITY_REVOCATION_INVALID_STATE`

- Owner: `package:@applik8s/operations`
- Defining source: `packages/operations/src/authority.ts`
- Explain: `applik8s explain AUTHORITY_REVOCATION_INVALID_STATE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-authority-revocation-pending"></a>

## `AUTHORITY_REVOCATION_PENDING`

- Owner: `workspace:cross-package`
- Defining sources: `packages/core/src/application-operation-authority.ts`, `packages/operations/src/authority.ts`
- Explain: `applik8s explain AUTHORITY_REVOCATION_PENDING`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-authority-revocation-unavailable"></a>

## `AUTHORITY_REVOCATION_UNAVAILABLE`

- Owner: `package:@applik8s/operations`
- Defining source: `packages/operations/src/authority.ts`
- Explain: `applik8s explain AUTHORITY_REVOCATION_UNAVAILABLE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-authority-scope-conflict"></a>

## `AUTHORITY_SCOPE_CONFLICT`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-operation-authority.ts`
- Explain: `applik8s explain AUTHORITY_SCOPE_CONFLICT`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-authority-self-approval"></a>

## `AUTHORITY_SELF_APPROVAL`

- Owner: `package:@applik8s/operations`
- Defining source: `packages/operations/src/authority.ts`
- Explain: `applik8s explain AUTHORITY_SELF_APPROVAL`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-authority-self-grant"></a>

## `AUTHORITY_SELF_GRANT`

- Owner: `package:@applik8s/operations`
- Defining source: `packages/operations/src/authority.ts`
- Explain: `applik8s explain AUTHORITY_SELF_GRANT`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-authority-unclassified-operation"></a>

## `AUTHORITY_UNCLASSIFIED_OPERATION`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-operation-authority.ts`
- Explain: `applik8s explain AUTHORITY_UNCLASSIFIED_OPERATION`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-authorization-audience-denied"></a>

## `AUTHORIZATION_AUDIENCE_DENIED`

- Owner: `package:@applik8s/operations`
- Defining source: `packages/operations/src/authority.ts`
- Explain: `applik8s explain AUTHORIZATION_AUDIENCE_DENIED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-authorization-catalog-inactive"></a>

## `AUTHORIZATION_CATALOG_INACTIVE`

- Owner: `package:@applik8s/operations`
- Defining source: `packages/operations/src/authority.ts`
- Explain: `applik8s explain AUTHORIZATION_CATALOG_INACTIVE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-authorization-context-mismatch"></a>

## `AUTHORIZATION_CONTEXT_MISMATCH`

- Owner: `package:@applik8s/operations`
- Defining source: `packages/operations/src/authority.ts`
- Explain: `applik8s explain AUTHORIZATION_CONTEXT_MISMATCH`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-authorization-grant-exhausted"></a>

## `AUTHORIZATION_GRANT_EXHAUSTED`

- Owner: `package:@applik8s/operations`
- Defining source: `packages/operations/src/authority.ts`
- Explain: `applik8s explain AUTHORIZATION_GRANT_EXHAUSTED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-authorization-grant-expired"></a>

## `AUTHORIZATION_GRANT_EXPIRED`

- Owner: `package:@applik8s/operations`
- Defining source: `packages/operations/src/authority.ts`
- Explain: `applik8s explain AUTHORIZATION_GRANT_EXPIRED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-authorization-grant-revoked"></a>

## `AUTHORIZATION_GRANT_REVOKED`

- Owner: `package:@applik8s/operations`
- Defining source: `packages/operations/src/authority.ts`
- Explain: `applik8s explain AUTHORIZATION_GRANT_REVOKED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-authorization-no-grant"></a>

## `AUTHORIZATION_NO_GRANT`

- Owner: `package:@applik8s/operations`
- Defining source: `packages/operations/src/authority.ts`
- Explain: `applik8s explain AUTHORIZATION_NO_GRANT`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-authorization-operation-mismatch"></a>

## `AUTHORIZATION_OPERATION_MISMATCH`

- Owner: `package:@applik8s/operations`
- Defining sources: `packages/operations/src/authority.ts`, `packages/operations/src/runtime.ts`
- Explain: `applik8s explain AUTHORIZATION_OPERATION_MISMATCH`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-authorization-operation-unclassified"></a>

## `AUTHORIZATION_OPERATION_UNCLASSIFIED`

- Owner: `package:@applik8s/operations`
- Defining source: `packages/operations/src/authority.ts`
- Explain: `applik8s explain AUTHORIZATION_OPERATION_UNCLASSIFIED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-authorization-policy-denied"></a>

## `AUTHORIZATION_POLICY_DENIED`

- Owner: `package:@applik8s/operations`
- Defining source: `packages/operations/src/authority.ts`
- Explain: `applik8s explain AUTHORIZATION_POLICY_DENIED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-authorization-scope-empty"></a>

## `AUTHORIZATION_SCOPE_EMPTY`

- Owner: `package:@applik8s/operations`
- Defining source: `packages/operations/src/authority.ts`
- Explain: `applik8s explain AUTHORIZATION_SCOPE_EMPTY`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-authorization-transport-denied"></a>

## `AUTHORIZATION_TRANSPORT_DENIED`

- Owner: `package:@applik8s/operations`
- Defining source: `packages/operations/src/authority.ts`
- Explain: `applik8s explain AUTHORIZATION_TRANSPORT_DENIED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-aws-configuration-unresolved"></a>

## `AWS_CONFIGURATION_UNRESOLVED`

- Owner: `workspace:cross-package`
- Defining sources: `packages/deployment-compiler/src/aws-deployment-plan.ts`, `packages/deployment-contract/src/aws-plan.ts`
- Explain: `applik8s explain AWS_CONFIGURATION_UNRESOLVED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-aws-cost-bound-exceeded"></a>

## `AWS_COST_BOUND_EXCEEDED`

- Owner: `package:@applik8s/deployment-contract`
- Defining source: `packages/deployment-contract/src/aws-plan.ts`
- Explain: `applik8s explain AWS_COST_BOUND_EXCEEDED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-aws-provider-incompatible"></a>

## `AWS_PROVIDER_INCOMPATIBLE`

- Owner: `workspace:cross-package`
- Defining sources: `packages/deployment-compiler/src/aws-deployment-plan.ts`, `packages/deployment-contract/src/aws-plan.ts`
- Explain: `applik8s explain AWS_PROVIDER_INCOMPATIBLE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-aws-runtime-access-unresolved"></a>

## `AWS_RUNTIME_ACCESS_UNRESOLVED`

- Owner: `workspace:cross-package`
- Defining sources: `packages/deployment-compiler/src/aws-deployment-plan.ts`, `packages/deployment-contract/src/aws-plan.ts`
- Explain: `applik8s explain AWS_RUNTIME_ACCESS_UNRESOLVED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-aws-sensitive-data"></a>

## `AWS_SENSITIVE_DATA`

- Owner: `package:@applik8s/deployment-contract`
- Defining source: `packages/deployment-contract/src/aws-plan.ts`
- Explain: `applik8s explain AWS_SENSITIVE_DATA`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-bundle-invalid"></a>

## `BUNDLE_INVALID`

- Owner: `workspace:cross-package`
- Defining sources: `packages/compiler/src/bundling/index.ts`, `packages/compiler/src/container-image/index.ts`, `packages/compiler/src/kubernetes-yaml/index.ts`, `packages/compiler/src/manifest/index.ts`, `packages/compiler/src/pipeline/entrypoint-discovery.ts`, `packages/compiler/src/pipeline/index.ts`, `packages/compiler/src/pipeline/pipeline-selection.ts`, `packages/compiler/src/pipeline/pipeline-shapes.ts`, `packages/compiler/src/pipeline/static-dispatcher.ts`, `packages/compiler/src/pipeline/static-serialization.ts`, `packages/compiler/src/runtime-contract/index.ts`, `packages/compiler/src/wasm-component/index.ts`, `packages/core/src/common.ts`, `packages/typekro-adapter/src/operation-targets.ts`, `packages/typekro-adapter/src/runtime.ts`
- Explain: `applik8s explain BUNDLE_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-cancellation-pending"></a>

## `CANCELLATION_PENDING`

- Owner: `package:@applik8s/runtime-aws`
- Defining source: `packages/runtime-aws/src/lakehouse.ts`
- Explain: `applik8s explain CANCELLATION_PENDING`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-canonical-json-cycle"></a>

## `CANONICAL_JSON_CYCLE`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/canonical-json.ts`
- Explain: `applik8s explain CANONICAL_JSON_CYCLE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-canonical-json-non-finite-number"></a>

## `CANONICAL_JSON_NON_FINITE_NUMBER`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/canonical-json.ts`
- Explain: `applik8s explain CANONICAL_JSON_NON_FINITE_NUMBER`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-canonical-json-undefined"></a>

## `CANONICAL_JSON_UNDEFINED`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/canonical-json.ts`
- Explain: `applik8s explain CANONICAL_JSON_UNDEFINED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-canonical-json-unsupported-value"></a>

## `CANONICAL_JSON_UNSUPPORTED_VALUE`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/canonical-json.ts`
- Explain: `applik8s explain CANONICAL_JSON_UNSUPPORTED_VALUE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-capability-denied"></a>

## `CAPABILITY_DENIED`

- Owner: `workspace:cross-package`
- Defining sources: `packages/core/src/common.ts`, `packages/sdk/src/handler-dispatch.ts`
- Explain: `applik8s explain CAPABILITY_DENIED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-capability-missing"></a>

## `CAPABILITY_MISSING`

- Owner: `workspace:cross-package`
- Defining sources: `packages/applik8s/src/dns.ts`, `packages/core/src/common.ts`, `packages/testing/src/harness.ts`
- Explain: `applik8s explain CAPABILITY_MISSING`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-catalog-incompatible"></a>

## `CATALOG_INCOMPATIBLE`

- Owner: `package:@applik8s/operations`
- Defining source: `packages/operations/src/catalog.ts`
- Explain: `applik8s explain CATALOG_INCOMPATIBLE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-catalog-invalid"></a>

## `CATALOG_INVALID`

- Owner: `package:@applik8s/operations`
- Defining source: `packages/operations/src/catalog.ts`
- Explain: `applik8s explain CATALOG_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-catalog-invalid-state"></a>

## `CATALOG_INVALID_STATE`

- Owner: `package:@applik8s/operations`
- Defining source: `packages/operations/src/catalog.ts`
- Explain: `applik8s explain CATALOG_INVALID_STATE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-catalog-not-found"></a>

## `CATALOG_NOT_FOUND`

- Owner: `package:@applik8s/operations`
- Defining source: `packages/operations/src/catalog.ts`
- Explain: `applik8s explain CATALOG_NOT_FOUND`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-catalog-operation-unavailable"></a>

## `CATALOG_OPERATION_UNAVAILABLE`

- Owner: `package:@applik8s/operations`
- Defining source: `packages/operations/src/catalog.ts`
- Explain: `applik8s explain CATALOG_OPERATION_UNAVAILABLE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-catalog-predecessor-mismatch"></a>

## `CATALOG_PREDECESSOR_MISMATCH`

- Owner: `package:@applik8s/operations`
- Defining source: `packages/operations/src/catalog.ts`
- Explain: `applik8s explain CATALOG_PREDECESSOR_MISMATCH`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-catalog-referenced"></a>

## `CATALOG_REFERENCED`

- Owner: `package:@applik8s/operations`
- Defining source: `packages/operations/src/catalog.ts`
- Explain: `applik8s explain CATALOG_REFERENCED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-catalog-revision-conflict"></a>

## `CATALOG_REVISION_CONFLICT`

- Owner: `package:@applik8s/operations`
- Defining source: `packages/operations/src/catalog.ts`
- Explain: `applik8s explain CATALOG_REVISION_CONFLICT`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-compatibility-failed"></a>

## `COMPATIBILITY_FAILED`

- Owner: `workspace:cross-package`
- Defining sources: `packages/compiler/src/pipeline/index.ts`, `packages/core/src/application-graph-compatibility.ts`, `packages/core/src/application-graph-contract.ts`, `packages/core/src/application-graph-node-validation.ts`, `packages/core/src/common.ts`
- Explain: `applik8s explain COMPATIBILITY_FAILED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-deployment-connection-unsafe"></a>

## `DEPLOYMENT_CONNECTION_UNSAFE`

- Owner: `package:@applik8s/deployment-contract`
- Defining sources: `packages/deployment-contract/src/types.ts`, `packages/deployment-contract/src/validation.ts`
- Explain: `applik8s explain DEPLOYMENT_CONNECTION_UNSAFE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-deployment-edge-cycle"></a>

## `DEPLOYMENT_EDGE_CYCLE`

- Owner: `package:@applik8s/deployment-contract`
- Defining sources: `packages/deployment-contract/src/types.ts`, `packages/deployment-contract/src/validation.ts`
- Explain: `applik8s explain DEPLOYMENT_EDGE_CYCLE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-deployment-edge-invalid"></a>

## `DEPLOYMENT_EDGE_INVALID`

- Owner: `package:@applik8s/deployment-contract`
- Defining sources: `packages/deployment-contract/src/types.ts`, `packages/deployment-contract/src/validation.ts`
- Explain: `applik8s explain DEPLOYMENT_EDGE_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-deployment-graph-invalid"></a>

## `DEPLOYMENT_GRAPH_INVALID`

- Owner: `package:@applik8s/deployment-contract`
- Defining sources: `packages/deployment-contract/src/types.ts`, `packages/deployment-contract/src/validation.ts`
- Explain: `applik8s explain DEPLOYMENT_GRAPH_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-deployment-identity-invalid"></a>

## `DEPLOYMENT_IDENTITY_INVALID`

- Owner: `package:@applik8s/deployment-contract`
- Defining sources: `packages/deployment-contract/src/types.ts`, `packages/deployment-contract/src/validation.ts`
- Explain: `applik8s explain DEPLOYMENT_IDENTITY_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-deployment-lifecycle-unsafe"></a>

## `DEPLOYMENT_LIFECYCLE_UNSAFE`

- Owner: `package:@applik8s/deployment-contract`
- Defining sources: `packages/deployment-contract/src/types.ts`, `packages/deployment-contract/src/validation.ts`
- Explain: `applik8s explain DEPLOYMENT_LIFECYCLE_UNSAFE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-deployment-node-duplicate"></a>

## `DEPLOYMENT_NODE_DUPLICATE`

- Owner: `package:@applik8s/deployment-contract`
- Defining sources: `packages/deployment-contract/src/types.ts`, `packages/deployment-contract/src/validation.ts`
- Explain: `applik8s explain DEPLOYMENT_NODE_DUPLICATE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-deployment-node-invalid"></a>

## `DEPLOYMENT_NODE_INVALID`

- Owner: `package:@applik8s/deployment-contract`
- Defining sources: `packages/deployment-contract/src/types.ts`, `packages/deployment-contract/src/validation.ts`
- Explain: `applik8s explain DEPLOYMENT_NODE_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-deployment-output-invalid"></a>

## `DEPLOYMENT_OUTPUT_INVALID`

- Owner: `package:@applik8s/deployment-contract`
- Defining sources: `packages/deployment-contract/src/types.ts`, `packages/deployment-contract/src/validation.ts`
- Explain: `applik8s explain DEPLOYMENT_OUTPUT_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-deployment-secret-unsafe"></a>

## `DEPLOYMENT_SECRET_UNSAFE`

- Owner: `package:@applik8s/deployment-contract`
- Defining sources: `packages/deployment-contract/src/types.ts`, `packages/deployment-contract/src/validation.ts`
- Explain: `applik8s explain DEPLOYMENT_SECRET_UNSAFE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-deployment-singleton-drift"></a>

## `DEPLOYMENT_SINGLETON_DRIFT`

- Owner: `package:@applik8s/deployment-contract`
- Defining sources: `packages/deployment-contract/src/types.ts`, `packages/deployment-contract/src/validation.ts`
- Explain: `applik8s explain DEPLOYMENT_SINGLETON_DRIFT`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-deployment-status-invalid"></a>

## `DEPLOYMENT_STATUS_INVALID`

- Owner: `package:@applik8s/deployment-contract`
- Defining sources: `packages/deployment-contract/src/types.ts`, `packages/deployment-contract/src/validation.ts`
- Explain: `applik8s explain DEPLOYMENT_STATUS_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-deployment-strategy-unsupported"></a>

## `DEPLOYMENT_STRATEGY_UNSUPPORTED`

- Owner: `package:@applik8s/deployment-contract`
- Defining sources: `packages/deployment-contract/src/types.ts`, `packages/deployment-contract/src/validation.ts`
- Explain: `applik8s explain DEPLOYMENT_STRATEGY_UNSUPPORTED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-development-agent-turn-failed"></a>

## `DEVELOPMENT_AGENT_TURN_FAILED`

- Owner: `package:@applik8s/dev`
- Defining source: `packages/dev/src/server.ts`
- Explain: `applik8s explain DEVELOPMENT_AGENT_TURN_FAILED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-effect-fence-unsupported"></a>

## `EFFECT_FENCE_UNSUPPORTED`

- Owner: `package:@applik8s/operations`
- Defining source: `packages/operations/src/effect-contracts.ts`
- Explain: `applik8s explain EFFECT_FENCE_UNSUPPORTED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-effect-identity-unstable"></a>

## `EFFECT_IDENTITY_UNSTABLE`

- Owner: `package:@applik8s/operations`
- Defining source: `packages/operations/src/effect-contracts.ts`
- Explain: `applik8s explain EFFECT_IDENTITY_UNSTABLE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-effect-receipt-conflict"></a>

## `EFFECT_RECEIPT_CONFLICT`

- Owner: `package:@applik8s/operations`
- Defining source: `packages/operations/src/effect-contracts.ts`
- Explain: `applik8s explain EFFECT_RECEIPT_CONFLICT`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-effect-retry-unsafe"></a>

## `EFFECT_RETRY_UNSAFE`

- Owner: `package:@applik8s/operations`
- Defining source: `packages/operations/src/effect-contracts.ts`
- Explain: `applik8s explain EFFECT_RETRY_UNSAFE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-execution-admission-invalid"></a>

## `EXECUTION_ADMISSION_INVALID`

- Owner: `package:@applik8s/operations`
- Defining source: `packages/operations/src/execution-admission-transport.ts`
- Explain: `applik8s explain EXECUTION_ADMISSION_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-execution-admission-too-large"></a>

## `EXECUTION_ADMISSION_TOO_LARGE`

- Owner: `package:@applik8s/operations`
- Defining source: `packages/operations/src/execution-admission-transport.ts`
- Explain: `applik8s explain EXECUTION_ADMISSION_TOO_LARGE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-finalizer-failed"></a>

## `FINALIZER_FAILED`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/common.ts`
- Explain: `applik8s explain FINALIZER_FAILED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-flow-binding-invalid"></a>

## `FLOW_BINDING_INVALID`

- Owner: `package:@applik8s/identity`
- Defining source: `packages/identity/src/runtime.ts`
- Explain: `applik8s explain FLOW_BINDING_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-flow-rate-limited"></a>

## `FLOW_RATE_LIMITED`

- Owner: `package:@applik8s/identity`
- Defining source: `packages/identity/src/runtime.ts`
- Explain: `applik8s explain FLOW_RATE_LIMITED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-flow-transition-invalid"></a>

## `FLOW_TRANSITION_INVALID`

- Owner: `package:@applik8s/identity`
- Defining source: `packages/identity/src/runtime.ts`
- Explain: `applik8s explain FLOW_TRANSITION_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-flow-unavailable"></a>

## `FLOW_UNAVAILABLE`

- Owner: `package:@applik8s/identity`
- Defining source: `packages/identity/src/runtime.ts`
- Explain: `applik8s explain FLOW_UNAVAILABLE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-foundation-guest-host-envelope-invalid"></a>

## `FOUNDATION_GUEST_HOST_ENVELOPE_INVALID`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-foundation.ts`
- Explain: `applik8s explain FOUNDATION_GUEST_HOST_ENVELOPE_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-foundation-identity-duplicate"></a>

## `FOUNDATION_IDENTITY_DUPLICATE`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-foundation.ts`
- Explain: `applik8s explain FOUNDATION_IDENTITY_DUPLICATE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-foundation-identity-invalid"></a>

## `FOUNDATION_IDENTITY_INVALID`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-foundation.ts`
- Explain: `applik8s explain FOUNDATION_IDENTITY_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-foundation-native-plan-invalid"></a>

## `FOUNDATION_NATIVE_PLAN_INVALID`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-foundation.ts`
- Explain: `applik8s explain FOUNDATION_NATIVE_PLAN_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-foundation-provenance-invalid"></a>

## `FOUNDATION_PROVENANCE_INVALID`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-foundation.ts`
- Explain: `applik8s explain FOUNDATION_PROVENANCE_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-foundation-provider-guarantee-invalid"></a>

## `FOUNDATION_PROVIDER_GUARANTEE_INVALID`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-foundation.ts`
- Explain: `applik8s explain FOUNDATION_PROVIDER_GUARANTEE_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-foundation-runtime-access-invalid"></a>

## `FOUNDATION_RUNTIME_ACCESS_INVALID`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-foundation.ts`
- Explain: `applik8s explain FOUNDATION_RUNTIME_ACCESS_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-foundation-target-invalid"></a>

## `FOUNDATION_TARGET_INVALID`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-foundation.ts`
- Explain: `applik8s explain FOUNDATION_TARGET_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-handler-not-found"></a>

## `HANDLER_NOT_FOUND`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/common.ts`
- Explain: `applik8s explain HANDLER_NOT_FOUND`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-handler-output-invalid"></a>

## `HANDLER_OUTPUT_INVALID`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/common.ts`
- Explain: `applik8s explain HANDLER_OUTPUT_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-handler-timeout"></a>

## `HANDLER_TIMEOUT`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/common.ts`
- Explain: `applik8s explain HANDLER_TIMEOUT`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-handler-trap"></a>

## `HANDLER_TRAP`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/common.ts`
- Explain: `applik8s explain HANDLER_TRAP`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-internal-invocation-invalid"></a>

## `INTERNAL_INVOCATION_INVALID`

- Owner: `package:@applik8s/operations`
- Defining source: `packages/operations/src/internal-transport.ts`
- Explain: `applik8s explain INTERNAL_INVOCATION_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-internal-invocation-too-large"></a>

## `INTERNAL_INVOCATION_TOO_LARGE`

- Owner: `package:@applik8s/operations`
- Defining source: `packages/operations/src/internal-transport.ts`
- Explain: `applik8s explain INTERNAL_INVOCATION_TOO_LARGE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-internal-token-passthrough-forbidden"></a>

## `INTERNAL_TOKEN_PASSTHROUGH_FORBIDDEN`

- Owner: `package:@applik8s/operations`
- Defining source: `packages/operations/src/internal-transport.ts`
- Explain: `applik8s explain INTERNAL_TOKEN_PASSTHROUGH_FORBIDDEN`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-job-execution-deadline-exceeded"></a>

## `JOB_EXECUTION_DEADLINE_EXCEEDED`

- Owner: `package:@applik8s/applik8s`
- Defining sources: `packages/applik8s/src/application-finite-jobs.ts`, `packages/applik8s/src/application-job-durable-runtime.ts`
- Explain: `applik8s explain JOB_EXECUTION_DEADLINE_EXCEEDED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-job-execution-failed"></a>

## `JOB_EXECUTION_FAILED`

- Owner: `package:@applik8s/applik8s`
- Defining sources: `packages/applik8s/src/application-finite-jobs.ts`, `packages/applik8s/src/application-job-durable-runtime.ts`
- Explain: `applik8s explain JOB_EXECUTION_FAILED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-job-lease-lost"></a>

## `JOB_LEASE_LOST`

- Owner: `package:@applik8s/applik8s`
- Defining source: `packages/applik8s/src/application-job-store.ts`
- Explain: `applik8s explain JOB_LEASE_LOST`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-job-store-invariant"></a>

## `JOB_STORE_INVARIANT`

- Owner: `package:@applik8s/applik8s`
- Defining source: `packages/applik8s/src/application-job-store.ts`
- Explain: `applik8s explain JOB_STORE_INVARIANT`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-job-worker-stopped"></a>

## `JOB_WORKER_STOPPED`

- Owner: `package:@applik8s/applik8s`
- Defining source: `packages/applik8s/src/application-job-durable-runtime.ts`
- Explain: `applik8s explain JOB_WORKER_STOPPED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-journey-assertion-timeout"></a>

## `JOURNEY_ASSERTION_TIMEOUT`

- Owner: `package:@applik8s/testing`
- Defining source: `packages/testing/src/journey.ts`
- Explain: `applik8s explain JOURNEY_ASSERTION_TIMEOUT`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-journey-authority-setup-failed"></a>

## `JOURNEY_AUTHORITY_SETUP_FAILED`

- Owner: `package:@applik8s/testing`
- Defining source: `packages/testing/src/journey.ts`
- Explain: `applik8s explain JOURNEY_AUTHORITY_SETUP_FAILED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-journey-cleanup-incomplete"></a>

## `JOURNEY_CLEANUP_INCOMPLETE`

- Owner: `package:@applik8s/testing`
- Defining source: `packages/testing/src/journey.ts`
- Explain: `applik8s explain JOURNEY_CLEANUP_INCOMPLETE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-journey-dependency-unresolved"></a>

## `JOURNEY_DEPENDENCY_UNRESOLVED`

- Owner: `package:@applik8s/testing`
- Defining source: `packages/testing/src/journey.ts`
- Explain: `applik8s explain JOURNEY_DEPENDENCY_UNRESOLVED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-journey-evidence-redaction-failed"></a>

## `JOURNEY_EVIDENCE_REDACTION_FAILED`

- Owner: `package:@applik8s/testing`
- Defining source: `packages/testing/src/journey.ts`
- Explain: `applik8s explain JOURNEY_EVIDENCE_REDACTION_FAILED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-journey-fixture-unavailable"></a>

## `JOURNEY_FIXTURE_UNAVAILABLE`

- Owner: `package:@applik8s/testing`
- Defining source: `packages/testing/src/journey.ts`
- Explain: `applik8s explain JOURNEY_FIXTURE_UNAVAILABLE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-journey-provider-incompatible"></a>

## `JOURNEY_PROVIDER_INCOMPATIBLE`

- Owner: `package:@applik8s/testing`
- Defining source: `packages/testing/src/journey.ts`
- Explain: `applik8s explain JOURNEY_PROVIDER_INCOMPATIBLE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-kubernetes-api-error"></a>

## `KUBERNETES_API_ERROR`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/common.ts`
- Explain: `applik8s explain KUBERNETES_API_ERROR`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-kubernetes-cluster-authority-undeclared"></a>

## `KUBERNETES_CLUSTER_AUTHORITY_UNDECLARED`

- Owner: `package:@applik8s/applik8s`
- Defining source: `packages/applik8s/src/kubernetes-cluster-runtime.ts`
- Explain: `applik8s explain KUBERNETES_CLUSTER_AUTHORITY_UNDECLARED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-kubernetes-cluster-binding-missing"></a>

## `KUBERNETES_CLUSTER_BINDING_MISSING`

- Owner: `package:@applik8s/applik8s`
- Defining source: `packages/applik8s/src/kubernetes-cluster-runtime.ts`
- Explain: `applik8s explain KUBERNETES_CLUSTER_BINDING_MISSING`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-kubernetes-cluster-cancelled"></a>

## `KUBERNETES_CLUSTER_CANCELLED`

- Owner: `package:@applik8s/applik8s`
- Defining source: `packages/applik8s/src/kubernetes-cluster-runtime.ts`
- Explain: `applik8s explain KUBERNETES_CLUSTER_CANCELLED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-kubernetes-cluster-conflict"></a>

## `KUBERNETES_CLUSTER_CONFLICT`

- Owner: `workspace:cross-package`
- Defining sources: `packages/applik8s/src/kubernetes-cluster-runtime.ts`, `packages/runtime-kubernetes/src/cluster-capability.ts`
- Explain: `applik8s explain KUBERNETES_CLUSTER_CONFLICT`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-kubernetes-cluster-continuation-invalid"></a>

## `KUBERNETES_CLUSTER_CONTINUATION_INVALID`

- Owner: `package:@applik8s/applik8s`
- Defining source: `packages/applik8s/src/kubernetes-cluster-runtime.ts`
- Explain: `applik8s explain KUBERNETES_CLUSTER_CONTINUATION_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-kubernetes-cluster-deadline"></a>

## `KUBERNETES_CLUSTER_DEADLINE`

- Owner: `workspace:cross-package`
- Defining sources: `packages/applik8s/src/kubernetes-cluster-runtime.ts`, `packages/runtime-kubernetes/src/cluster-capability.ts`
- Explain: `applik8s explain KUBERNETES_CLUSTER_DEADLINE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-kubernetes-cluster-endpoint-forbidden"></a>

## `KUBERNETES_CLUSTER_ENDPOINT_FORBIDDEN`

- Owner: `package:@applik8s/applik8s`
- Defining source: `packages/applik8s/src/kubernetes-cluster-runtime.ts`
- Explain: `applik8s explain KUBERNETES_CLUSTER_ENDPOINT_FORBIDDEN`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-kubernetes-cluster-forbidden"></a>

## `KUBERNETES_CLUSTER_FORBIDDEN`

- Owner: `workspace:cross-package`
- Defining sources: `packages/applik8s/src/kubernetes-cluster-runtime.ts`, `packages/runtime-kubernetes/src/cluster-capability.ts`
- Explain: `applik8s explain KUBERNETES_CLUSTER_FORBIDDEN`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-kubernetes-cluster-list-unbounded"></a>

## `KUBERNETES_CLUSTER_LIST_UNBOUNDED`

- Owner: `package:@applik8s/applik8s`
- Defining source: `packages/applik8s/src/kubernetes-cluster-runtime.ts`
- Explain: `applik8s explain KUBERNETES_CLUSTER_LIST_UNBOUNDED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-kubernetes-cluster-mutation-ownership-required"></a>

## `KUBERNETES_CLUSTER_MUTATION_OWNERSHIP_REQUIRED`

- Owner: `package:@applik8s/applik8s`
- Defining source: `packages/applik8s/src/kubernetes-cluster-runtime.ts`
- Explain: `applik8s explain KUBERNETES_CLUSTER_MUTATION_OWNERSHIP_REQUIRED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-kubernetes-cluster-not-found"></a>

## `KUBERNETES_CLUSTER_NOT_FOUND`

- Owner: `workspace:cross-package`
- Defining sources: `packages/applik8s/src/kubernetes-cluster-runtime.ts`, `packages/runtime-kubernetes/src/cluster-capability.ts`
- Explain: `applik8s explain KUBERNETES_CLUSTER_NOT_FOUND`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-kubernetes-cluster-protocol-incompatible"></a>

## `KUBERNETES_CLUSTER_PROTOCOL_INCOMPATIBLE`

- Owner: `package:@applik8s/applik8s`
- Defining sources: `packages/applik8s/src/kubernetes-cluster-runtime.ts`, `packages/applik8s/src/kubernetes-cluster-wit-runtime.ts`
- Explain: `applik8s explain KUBERNETES_CLUSTER_PROTOCOL_INCOMPATIBLE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-kubernetes-cluster-response-limit"></a>

## `KUBERNETES_CLUSTER_RESPONSE_LIMIT`

- Owner: `package:@applik8s/applik8s`
- Defining source: `packages/applik8s/src/kubernetes-cluster-runtime.ts`
- Explain: `applik8s explain KUBERNETES_CLUSTER_RESPONSE_LIMIT`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-kubernetes-cluster-schema-mismatch"></a>

## `KUBERNETES_CLUSTER_SCHEMA_MISMATCH`

- Owner: `package:@applik8s/applik8s`
- Defining source: `packages/applik8s/src/kubernetes-cluster-runtime.ts`
- Explain: `applik8s explain KUBERNETES_CLUSTER_SCHEMA_MISMATCH`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-kubernetes-cluster-scope-unbounded"></a>

## `KUBERNETES_CLUSTER_SCOPE_UNBOUNDED`

- Owner: `package:@applik8s/applik8s`
- Defining source: `packages/applik8s/src/kubernetes-cluster-runtime.ts`
- Explain: `applik8s explain KUBERNETES_CLUSTER_SCOPE_UNBOUNDED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-kubernetes-cluster-unavailable"></a>

## `KUBERNETES_CLUSTER_UNAVAILABLE`

- Owner: `workspace:cross-package`
- Defining sources: `packages/applik8s/src/kubernetes-cluster-runtime.ts`, `packages/applik8s/src/kubernetes-cluster-wit-runtime.ts`, `packages/runtime-kubernetes/src/cluster-capability.ts`
- Explain: `applik8s explain KUBERNETES_CLUSTER_UNAVAILABLE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-lifecycle-unsafe"></a>

## `LIFECYCLE_UNSAFE`

- Owner: `workspace:cross-package`
- Defining sources: `packages/applik8s/src/application-builder.ts`, `packages/core/src/common.ts`
- Explain: `applik8s explain LIFECYCLE_UNSAFE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-local-capability-unbound"></a>

## `LOCAL_CAPABILITY_UNBOUND`

- Owner: `package:@applik8s/server`
- Defining source: `packages/server/src/local-operator-runtime.ts`
- Explain: `applik8s explain LOCAL_CAPABILITY_UNBOUND`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-local-kubernetes-read-failed"></a>

## `LOCAL_KUBERNETES_READ_FAILED`

- Owner: `package:@applik8s/server`
- Defining source: `packages/server/src/local-operator-runtime.ts`
- Explain: `applik8s explain LOCAL_KUBERNETES_READ_FAILED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-local-lifecycle-collision"></a>

## `LOCAL_LIFECYCLE_COLLISION`

- Owner: `workspace:cross-package`
- Defining sources: `packages/deployment-compiler/src/local-supervisor-plan.ts`, `packages/deployment-contract/src/local-supervisor.ts`
- Explain: `applik8s explain LOCAL_LIFECYCLE_COLLISION`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-local-provider-unresolved"></a>

## `LOCAL_PROVIDER_UNRESOLVED`

- Owner: `workspace:cross-package`
- Defining sources: `packages/deployment-compiler/src/local-supervisor-plan.ts`, `packages/deployment-contract/src/local-supervisor.ts`
- Explain: `applik8s explain LOCAL_PROVIDER_UNRESOLVED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-local-target-incompatible"></a>

## `LOCAL_TARGET_INCOMPATIBLE`

- Owner: `workspace:cross-package`
- Defining sources: `packages/deployment-compiler/src/local-supervisor-plan.ts`, `packages/deployment-contract/src/local-supervisor.ts`
- Explain: `applik8s explain LOCAL_TARGET_INCOMPATIBLE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-manifest-invalid"></a>

## `MANIFEST_INVALID`

- Owner: `workspace:cross-package`
- Defining sources: `packages/compiler/src/kubernetes-schema/index.ts`, `packages/compiler/src/kubernetes-yaml/index.ts`, `packages/compiler/src/manifest/validation.ts`, `packages/core/src/application-graph-contract.ts`, `packages/core/src/common.ts`, `packages/testing/src/proxy.ts`
- Explain: `applik8s explain MANIFEST_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-mcp-authentication-required"></a>

## `MCP_AUTHENTICATION_REQUIRED`

- Owner: `package:@applik8s/mcp`
- Defining sources: `packages/mcp/src/contracts.ts`, `packages/mcp/src/server.ts`
- Explain: `applik8s explain MCP_AUTHENTICATION_REQUIRED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-mcp-authorization-denied"></a>

## `MCP_AUTHORIZATION_DENIED`

- Owner: `package:@applik8s/mcp`
- Defining sources: `packages/mcp/src/contracts.ts`, `packages/mcp/src/placement.ts`, `packages/mcp/src/server.ts`
- Explain: `applik8s explain MCP_AUTHORIZATION_DENIED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-mcp-catalog-reinitialize-required"></a>

## `MCP_CATALOG_REINITIALIZE_REQUIRED`

- Owner: `package:@applik8s/mcp`
- Defining sources: `packages/mcp/src/contracts.ts`, `packages/mcp/src/server.ts`
- Explain: `applik8s explain MCP_CATALOG_REINITIALIZE_REQUIRED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-mcp-deadline-exceeded"></a>

## `MCP_DEADLINE_EXCEEDED`

- Owner: `package:@applik8s/mcp`
- Defining sources: `packages/mcp/src/client.ts`, `packages/mcp/src/contracts.ts`
- Explain: `applik8s explain MCP_DEADLINE_EXCEEDED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-mcp-external-credential-invalid"></a>

## `MCP_EXTERNAL_CREDENTIAL_INVALID`

- Owner: `package:@applik8s/mcp`
- Defining sources: `packages/mcp/src/client.ts`, `packages/mcp/src/contracts.ts`
- Explain: `applik8s explain MCP_EXTERNAL_CREDENTIAL_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-mcp-external-schema-changed"></a>

## `MCP_EXTERNAL_SCHEMA_CHANGED`

- Owner: `package:@applik8s/mcp`
- Defining sources: `packages/mcp/src/client.ts`, `packages/mcp/src/contracts.ts`
- Explain: `applik8s explain MCP_EXTERNAL_SCHEMA_CHANGED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-mcp-external-unavailable"></a>

## `MCP_EXTERNAL_UNAVAILABLE`

- Owner: `package:@applik8s/mcp`
- Defining sources: `packages/mcp/src/client.ts`, `packages/mcp/src/contracts.ts`, `packages/mcp/src/server.ts`
- Explain: `applik8s explain MCP_EXTERNAL_UNAVAILABLE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-mcp-input-invalid"></a>

## `MCP_INPUT_INVALID`

- Owner: `package:@applik8s/mcp`
- Defining sources: `packages/mcp/src/contracts.ts`, `packages/mcp/src/placement.ts`, `packages/mcp/src/server.ts`
- Explain: `applik8s explain MCP_INPUT_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-mcp-protocol-unsupported"></a>

## `MCP_PROTOCOL_UNSUPPORTED`

- Owner: `package:@applik8s/mcp`
- Defining sources: `packages/mcp/src/client.ts`, `packages/mcp/src/contracts.ts`, `packages/mcp/src/server.ts`
- Explain: `applik8s explain MCP_PROTOCOL_UNSUPPORTED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-mcp-request-too-large"></a>

## `MCP_REQUEST_TOO_LARGE`

- Owner: `package:@applik8s/mcp`
- Defining sources: `packages/mcp/src/client.ts`, `packages/mcp/src/contracts.ts`, `packages/mcp/src/server.ts`
- Explain: `applik8s explain MCP_REQUEST_TOO_LARGE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-mcp-response-too-large"></a>

## `MCP_RESPONSE_TOO_LARGE`

- Owner: `package:@applik8s/mcp`
- Defining sources: `packages/mcp/src/client.ts`, `packages/mcp/src/contracts.ts`, `packages/mcp/src/server.ts`
- Explain: `applik8s explain MCP_RESPONSE_TOO_LARGE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-mcp-session-identity-mismatch"></a>

## `MCP_SESSION_IDENTITY_MISMATCH`

- Owner: `package:@applik8s/mcp`
- Defining sources: `packages/mcp/src/contracts.ts`, `packages/mcp/src/server.ts`
- Explain: `applik8s explain MCP_SESSION_IDENTITY_MISMATCH`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-mcp-session-required"></a>

## `MCP_SESSION_REQUIRED`

- Owner: `package:@applik8s/mcp`
- Defining sources: `packages/mcp/src/client.ts`, `packages/mcp/src/contracts.ts`, `packages/mcp/src/server.ts`
- Explain: `applik8s explain MCP_SESSION_REQUIRED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-mcp-session-unavailable"></a>

## `MCP_SESSION_UNAVAILABLE`

- Owner: `package:@applik8s/mcp`
- Defining sources: `packages/mcp/src/contracts.ts`, `packages/mcp/src/server.ts`
- Explain: `applik8s explain MCP_SESSION_UNAVAILABLE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-mcp-tool-ambiguous"></a>

## `MCP_TOOL_AMBIGUOUS`

- Owner: `package:@applik8s/mcp`
- Defining sources: `packages/mcp/src/contracts.ts`, `packages/mcp/src/server.ts`
- Explain: `applik8s explain MCP_TOOL_AMBIGUOUS`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-mcp-tool-unavailable"></a>

## `MCP_TOOL_UNAVAILABLE`

- Owner: `package:@applik8s/mcp`
- Defining sources: `packages/mcp/src/client.ts`, `packages/mcp/src/contracts.ts`, `packages/mcp/src/server.ts`
- Explain: `applik8s explain MCP_TOOL_UNAVAILABLE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-mcp-tool-unknown"></a>

## `MCP_TOOL_UNKNOWN`

- Owner: `package:@applik8s/mcp`
- Defining sources: `packages/mcp/src/client.ts`, `packages/mcp/src/contracts.ts`, `packages/mcp/src/server.ts`
- Explain: `applik8s explain MCP_TOOL_UNKNOWN`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-migration-concurrent-operation"></a>

## `MIGRATION_CONCURRENT_OPERATION`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-deployment-migration.ts`
- Explain: `applik8s explain MIGRATION_CONCURRENT_OPERATION`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-migration-forward-recovery-required"></a>

## `MIGRATION_FORWARD_RECOVERY_REQUIRED`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-deployment-migration.ts`
- Explain: `applik8s explain MIGRATION_FORWARD_RECOVERY_REQUIRED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-migration-lifecycle-transfer-unsafe"></a>

## `MIGRATION_LIFECYCLE_TRANSFER_UNSAFE`

- Owner: `package:@applik8s/core`
- Defining sources: `packages/core/src/application-deployment-migration-proposal.ts`, `packages/core/src/application-deployment-migration.ts`
- Explain: `applik8s explain MIGRATION_LIFECYCLE_TRANSFER_UNSAFE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-migration-mapping-ambiguous"></a>

## `MIGRATION_MAPPING_AMBIGUOUS`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-deployment-migration-proposal.ts`
- Explain: `applik8s explain MIGRATION_MAPPING_AMBIGUOUS`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-migration-physical-identity-conflict"></a>

## `MIGRATION_PHYSICAL_IDENTITY_CONFLICT`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-deployment-migration-proposal.ts`
- Explain: `applik8s explain MIGRATION_PHYSICAL_IDENTITY_CONFLICT`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-migration-provider-incompatible"></a>

## `MIGRATION_PROVIDER_INCOMPATIBLE`

- Owner: `package:@applik8s/core`
- Defining sources: `packages/core/src/application-deployment-migration-proposal.ts`, `packages/core/src/application-deployment-migration.ts`
- Explain: `applik8s explain MIGRATION_PROVIDER_INCOMPATIBLE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-migration-retained-data-unsafe"></a>

## `MIGRATION_RETAINED_DATA_UNSAFE`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-deployment-migration-proposal.ts`
- Explain: `applik8s explain MIGRATION_RETAINED_DATA_UNSAFE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-migration-rollback-unavailable"></a>

## `MIGRATION_ROLLBACK_UNAVAILABLE`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-deployment-migration.ts`
- Explain: `applik8s explain MIGRATION_ROLLBACK_UNAVAILABLE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-migration-shared-owner-blocked"></a>

## `MIGRATION_SHARED_OWNER_BLOCKED`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-deployment-migration-proposal.ts`
- Explain: `applik8s explain MIGRATION_SHARED_OWNER_BLOCKED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-migration-source-release-unqualified"></a>

## `MIGRATION_SOURCE_RELEASE_UNQUALIFIED`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-deployment-migration-proposal.ts`
- Explain: `applik8s explain MIGRATION_SOURCE_RELEASE_UNQUALIFIED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-migration-source-schema-unsupported"></a>

## `MIGRATION_SOURCE_SCHEMA_UNSUPPORTED`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-deployment-migration-proposal.ts`
- Explain: `applik8s explain MIGRATION_SOURCE_SCHEMA_UNSUPPORTED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-migration-source-state-missing"></a>

## `MIGRATION_SOURCE_STATE_MISSING`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-deployment-migration-proposal.ts`
- Explain: `applik8s explain MIGRATION_SOURCE_STATE_MISSING`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-ml-artifact-integrity-failed"></a>

## `ML_ARTIFACT_INTEGRITY_FAILED`

- Owner: `package:@applik8s/ml`
- Defining source: `packages/ml/src/index.ts`
- Explain: `applik8s explain ML_ARTIFACT_INTEGRITY_FAILED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-ml-data-residency-unsatisfied"></a>

## `ML_DATA_RESIDENCY_UNSATISFIED`

- Owner: `package:@applik8s/ml`
- Defining sources: `packages/ml/src/index.ts`, `packages/ml/src/runtime.ts`
- Explain: `applik8s explain ML_DATA_RESIDENCY_UNSATISFIED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-ml-input-invalid"></a>

## `ML_INPUT_INVALID`

- Owner: `package:@applik8s/ml`
- Defining sources: `packages/ml/src/index.ts`, `packages/ml/src/runtime.ts`
- Explain: `applik8s explain ML_INPUT_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-ml-model-version-unavailable"></a>

## `ML_MODEL_VERSION_UNAVAILABLE`

- Owner: `package:@applik8s/ml`
- Defining sources: `packages/ml/src/index.ts`, `packages/ml/src/runtime.ts`
- Explain: `applik8s explain ML_MODEL_VERSION_UNAVAILABLE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-ml-output-invalid"></a>

## `ML_OUTPUT_INVALID`

- Owner: `package:@applik8s/ml`
- Defining sources: `packages/ml/src/index.ts`, `packages/ml/src/runtime.ts`
- Explain: `applik8s explain ML_OUTPUT_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-ml-provider-failed"></a>

## `ML_PROVIDER_FAILED`

- Owner: `package:@applik8s/ml`
- Defining sources: `packages/ml/src/index.ts`, `packages/ml/src/runtime.ts`
- Explain: `applik8s explain ML_PROVIDER_FAILED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-ml-timeout"></a>

## `ML_TIMEOUT`

- Owner: `package:@applik8s/ml`
- Defining sources: `packages/ml/src/index.ts`, `packages/ml/src/runtime.ts`
- Explain: `applik8s explain ML_TIMEOUT`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-oauth-audience-denied"></a>

## `OAUTH_AUDIENCE_DENIED`

- Owner: `package:@applik8s/identity`
- Defining source: `packages/identity/src/resource-admission.ts`
- Explain: `applik8s explain OAUTH_AUDIENCE_DENIED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-oauth-authentication-required"></a>

## `OAUTH_AUTHENTICATION_REQUIRED`

- Owner: `package:@applik8s/identity`
- Defining source: `packages/identity/src/resource-admission.ts`
- Explain: `applik8s explain OAUTH_AUTHENTICATION_REQUIRED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-oauth-client-invalid"></a>

## `OAUTH_CLIENT_INVALID`

- Owner: `package:@applik8s/identity`
- Defining source: `packages/identity/src/oauth-runtime.ts`
- Explain: `applik8s explain OAUTH_CLIENT_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-oauth-flow-binding-invalid"></a>

## `OAUTH_FLOW_BINDING_INVALID`

- Owner: `package:@applik8s/identity`
- Defining source: `packages/identity/src/oauth-runtime.ts`
- Explain: `applik8s explain OAUTH_FLOW_BINDING_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-oauth-flow-unavailable"></a>

## `OAUTH_FLOW_UNAVAILABLE`

- Owner: `package:@applik8s/identity`
- Defining source: `packages/identity/src/oauth-runtime.ts`
- Explain: `applik8s explain OAUTH_FLOW_UNAVAILABLE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-oauth-principal-invalid"></a>

## `OAUTH_PRINCIPAL_INVALID`

- Owner: `package:@applik8s/identity`
- Defining source: `packages/identity/src/resource-admission.ts`
- Explain: `applik8s explain OAUTH_PRINCIPAL_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-oauth-provider-decision-invalid"></a>

## `OAUTH_PROVIDER_DECISION_INVALID`

- Owner: `package:@applik8s/identity`
- Defining source: `packages/identity/src/oauth-runtime.ts`
- Explain: `applik8s explain OAUTH_PROVIDER_DECISION_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-oauth-request-invalid"></a>

## `OAUTH_REQUEST_INVALID`

- Owner: `package:@applik8s/identity`
- Defining source: `packages/identity/src/oauth-runtime.ts`
- Explain: `applik8s explain OAUTH_REQUEST_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-oauth-resource-owner-invalid"></a>

## `OAUTH_RESOURCE_OWNER_INVALID`

- Owner: `package:@applik8s/identity`
- Defining source: `packages/identity/src/oauth-runtime.ts`
- Explain: `applik8s explain OAUTH_RESOURCE_OWNER_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-oauth-scope-denied"></a>

## `OAUTH_SCOPE_DENIED`

- Owner: `package:@applik8s/identity`
- Defining source: `packages/identity/src/resource-admission.ts`
- Explain: `applik8s explain OAUTH_SCOPE_DENIED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-oauth-token-expired"></a>

## `OAUTH_TOKEN_EXPIRED`

- Owner: `package:@applik8s/identity`
- Defining source: `packages/identity/src/resource-admission.ts`
- Explain: `applik8s explain OAUTH_TOKEN_EXPIRED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-oauth-token-identity-missing"></a>

## `OAUTH_TOKEN_IDENTITY_MISSING`

- Owner: `package:@applik8s/identity`
- Defining source: `packages/identity/src/resource-admission.ts`
- Explain: `applik8s explain OAUTH_TOKEN_IDENTITY_MISSING`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-oauth-token-inactive"></a>

## `OAUTH_TOKEN_INACTIVE`

- Owner: `package:@applik8s/identity`
- Defining source: `packages/identity/src/resource-admission.ts`
- Explain: `applik8s explain OAUTH_TOKEN_INACTIVE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-oauth-token-location-invalid"></a>

## `OAUTH_TOKEN_LOCATION_INVALID`

- Owner: `package:@applik8s/identity`
- Defining source: `packages/identity/src/resource-admission.ts`
- Explain: `applik8s explain OAUTH_TOKEN_LOCATION_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-oauth-token-too-large"></a>

## `OAUTH_TOKEN_TOO_LARGE`

- Owner: `package:@applik8s/identity`
- Defining source: `packages/identity/src/resource-admission.ts`
- Explain: `applik8s explain OAUTH_TOKEN_TOO_LARGE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-ory-cancelled"></a>

## `ORY_CANCELLED`

- Owner: `package:@applik8s/identity-ory`
- Defining source: `packages/identity-ory/src/transport.ts`
- Explain: `applik8s explain ORY_CANCELLED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-ory-request-rejected"></a>

## `ORY_REQUEST_REJECTED`

- Owner: `package:@applik8s/identity-ory`
- Defining sources: `packages/identity-ory/src/hydra.ts`, `packages/identity-ory/src/transport.ts`
- Explain: `applik8s explain ORY_REQUEST_REJECTED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-ory-response-invalid"></a>

## `ORY_RESPONSE_INVALID`

- Owner: `package:@applik8s/identity-ory`
- Defining sources: `packages/identity-ory/src/hydra.ts`, `packages/identity-ory/src/keto.ts`, `packages/identity-ory/src/kratos.ts`, `packages/identity-ory/src/transport.ts`
- Explain: `applik8s explain ORY_RESPONSE_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-ory-timeout"></a>

## `ORY_TIMEOUT`

- Owner: `package:@applik8s/identity-ory`
- Defining source: `packages/identity-ory/src/transport.ts`
- Explain: `applik8s explain ORY_TIMEOUT`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-ory-unauthorized"></a>

## `ORY_UNAUTHORIZED`

- Owner: `package:@applik8s/identity-ory`
- Defining sources: `packages/identity-ory/src/kratos.ts`, `packages/identity-ory/src/transport.ts`
- Explain: `applik8s explain ORY_UNAUTHORIZED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-ory-unavailable"></a>

## `ORY_UNAVAILABLE`

- Owner: `package:@applik8s/identity-ory`
- Defining source: `packages/identity-ory/src/transport.ts`
- Explain: `applik8s explain ORY_UNAVAILABLE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-outcome-unknown"></a>

## `OUTCOME_UNKNOWN`

- Owner: `package:@applik8s/runtime-aws`
- Defining source: `packages/runtime-aws/src/lakehouse.ts`
- Explain: `applik8s explain OUTCOME_UNKNOWN`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-partial-apply-failed"></a>

## `PARTIAL_APPLY_FAILED`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/common.ts`
- Explain: `applik8s explain PARTIAL_APPLY_FAILED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-plan-actor-capability-unsupported"></a>

## `PLAN_ACTOR_CAPABILITY_UNSUPPORTED`

- Owner: `package:@applik8s/deployment-compiler`
- Defining source: `packages/deployment-compiler/src/application-plan.ts`
- Explain: `applik8s explain PLAN_ACTOR_CAPABILITY_UNSUPPORTED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-plan-comparison-application-mismatch"></a>

## `PLAN_COMPARISON_APPLICATION_MISMATCH`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-plan.ts`
- Explain: `applik8s explain PLAN_COMPARISON_APPLICATION_MISMATCH`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-plan-comparison-graph-version-incompatible"></a>

## `PLAN_COMPARISON_GRAPH_VERSION_INCOMPATIBLE`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-plan.ts`
- Explain: `applik8s explain PLAN_COMPARISON_GRAPH_VERSION_INCOMPATIBLE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-plan-comparison-schema-incompatible"></a>

## `PLAN_COMPARISON_SCHEMA_INCOMPATIBLE`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-plan.ts`
- Explain: `applik8s explain PLAN_COMPARISON_SCHEMA_INCOMPATIBLE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-plan-implementation-graph-mismatch"></a>

## `PLAN_IMPLEMENTATION_GRAPH_MISMATCH`

- Owner: `package:@applik8s/deployment-compiler`
- Defining source: `packages/deployment-compiler/src/application-plan.ts`
- Explain: `applik8s explain PLAN_IMPLEMENTATION_GRAPH_MISMATCH`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-plan-implementation-profile-mismatch"></a>

## `PLAN_IMPLEMENTATION_PROFILE_MISMATCH`

- Owner: `package:@applik8s/deployment-compiler`
- Defining source: `packages/deployment-compiler/src/application-plan.ts`
- Explain: `applik8s explain PLAN_IMPLEMENTATION_PROFILE_MISMATCH`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-plan-physical-contributor-undeclared"></a>

## `PLAN_PHYSICAL_CONTRIBUTOR_UNDECLARED`

- Owner: `package:@applik8s/deployment-compiler`
- Defining source: `packages/deployment-compiler/src/application-plan.ts`
- Explain: `applik8s explain PLAN_PHYSICAL_CONTRIBUTOR_UNDECLARED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-plan-physical-implementation-ambiguous"></a>

## `PLAN_PHYSICAL_IMPLEMENTATION_AMBIGUOUS`

- Owner: `package:@applik8s/deployment-compiler`
- Defining source: `packages/deployment-compiler/src/application-plan.ts`
- Explain: `applik8s explain PLAN_PHYSICAL_IMPLEMENTATION_AMBIGUOUS`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-plan-physical-implementation-unknown"></a>

## `PLAN_PHYSICAL_IMPLEMENTATION_UNKNOWN`

- Owner: `package:@applik8s/deployment-compiler`
- Defining source: `packages/deployment-compiler/src/application-plan.ts`
- Explain: `applik8s explain PLAN_PHYSICAL_IMPLEMENTATION_UNKNOWN`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-profile-binding-conflict"></a>

## `PROFILE_BINDING_CONFLICT`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-implementation-plan.ts`
- Explain: `applik8s explain PROFILE_BINDING_CONFLICT`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-provider-completion-invalid"></a>

## `PROVIDER_COMPLETION_INVALID`

- Owner: `package:@applik8s/identity`
- Defining source: `packages/identity/src/runtime.ts`
- Explain: `applik8s explain PROVIDER_COMPLETION_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-provider-configuration-invalid"></a>

## `PROVIDER_CONFIGURATION_INVALID`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-implementation-plan.ts`
- Explain: `applik8s explain PROVIDER_CONFIGURATION_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-provider-continuity-invalid"></a>

## `PROVIDER_CONTINUITY_INVALID`

- Owner: `package:@applik8s/identity`
- Defining source: `packages/identity/src/runtime.ts`
- Explain: `applik8s explain PROVIDER_CONTINUITY_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-provider-dependency-cycle"></a>

## `PROVIDER_DEPENDENCY_CYCLE`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-implementation-plan.ts`
- Explain: `applik8s explain PROVIDER_DEPENDENCY_CYCLE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-provider-dependency-incompatible"></a>

## `PROVIDER_DEPENDENCY_INCOMPATIBLE`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-implementation-plan.ts`
- Explain: `applik8s explain PROVIDER_DEPENDENCY_INCOMPATIBLE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-provider-dependency-missing"></a>

## `PROVIDER_DEPENDENCY_MISSING`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-implementation-plan.ts`
- Explain: `applik8s explain PROVIDER_DEPENDENCY_MISSING`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-provider-implementation-identity-collision"></a>

## `PROVIDER_IMPLEMENTATION_IDENTITY_COLLISION`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-implementation-plan.ts`
- Explain: `applik8s explain PROVIDER_IMPLEMENTATION_IDENTITY_COLLISION`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-provider-implementation-identity-unstable"></a>

## `PROVIDER_IMPLEMENTATION_IDENTITY_UNSTABLE`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-implementation-plan.ts`
- Explain: `applik8s explain PROVIDER_IMPLEMENTATION_IDENTITY_UNSTABLE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-provider-session-orphaned"></a>

## `PROVIDER_SESSION_ORPHANED`

- Owner: `package:@applik8s/identity`
- Defining source: `packages/identity/src/runtime.ts`
- Explain: `applik8s explain PROVIDER_SESSION_ORPHANED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-query-batch-cancelled"></a>

## `QUERY_BATCH_CANCELLED`

- Owner: `workspace:cross-package`
- Defining sources: `packages/applik8s/src/application-query-batching.ts`, `packages/runtime-postgres/src/query-batch.ts`
- Explain: `applik8s explain QUERY_BATCH_CANCELLED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-rbac-denied"></a>

## `RBAC_DENIED`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/common.ts`
- Explain: `applik8s explain RBAC_DENIED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-research-evidence-incomplete"></a>

## `RESEARCH_EVIDENCE_INCOMPLETE`

- Owner: `package:@applik8s/research`
- Defining source: `packages/research/src/agent-runtime.ts`
- Explain: `applik8s explain RESEARCH_EVIDENCE_INCOMPLETE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-research-query-invalid"></a>

## `RESEARCH_QUERY_INVALID`

- Owner: `package:@applik8s/research`
- Defining source: `packages/research/src/agent-runtime.ts`
- Explain: `applik8s explain RESEARCH_QUERY_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-resource-conflict"></a>

## `RESOURCE_CONFLICT`

- Owner: `workspace:cross-package`
- Defining sources: `packages/applik8s/src/dns.ts`, `packages/core/src/common.ts`
- Explain: `applik8s explain RESOURCE_CONFLICT`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-resource-not-found"></a>

## `RESOURCE_NOT_FOUND`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/common.ts`
- Explain: `applik8s explain RESOURCE_NOT_FOUND`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-runtime-access-bootstrap-iam-missing"></a>

## `RUNTIME_ACCESS_BOOTSTRAP_IAM_MISSING`

- Owner: `package:@applik8s/deployment-compiler`
- Defining source: `packages/deployment-compiler/src/aws-runtime-access-parity.ts`
- Explain: `applik8s explain RUNTIME_ACCESS_BOOTSTRAP_IAM_MISSING`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-runtime-access-bootstrap-iam-widened"></a>

## `RUNTIME_ACCESS_BOOTSTRAP_IAM_WIDENED`

- Owner: `package:@applik8s/deployment-compiler`
- Defining source: `packages/deployment-compiler/src/aws-runtime-access-parity.ts`
- Explain: `applik8s explain RUNTIME_ACCESS_BOOTSTRAP_IAM_WIDENED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-runtime-access-credential-cross-namespace"></a>

## `RUNTIME_ACCESS_CREDENTIAL_CROSS_NAMESPACE`

- Owner: `package:@applik8s/deployment-compiler`
- Defining source: `packages/deployment-compiler/src/kubernetes-runtime-access-parity.ts`
- Explain: `applik8s explain RUNTIME_ACCESS_CREDENTIAL_CROSS_NAMESPACE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-runtime-access-credential-missing"></a>

## `RUNTIME_ACCESS_CREDENTIAL_MISSING`

- Owner: `package:@applik8s/deployment-compiler`
- Defining source: `packages/deployment-compiler/src/kubernetes-runtime-access-parity.ts`
- Explain: `applik8s explain RUNTIME_ACCESS_CREDENTIAL_MISSING`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-runtime-access-credential-widened"></a>

## `RUNTIME_ACCESS_CREDENTIAL_WIDENED`

- Owner: `package:@applik8s/deployment-compiler`
- Defining source: `packages/deployment-compiler/src/kubernetes-runtime-access-parity.ts`
- Explain: `applik8s explain RUNTIME_ACCESS_CREDENTIAL_WIDENED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-runtime-access-execution-role-misbound"></a>

## `RUNTIME_ACCESS_EXECUTION_ROLE_MISBOUND`

- Owner: `package:@applik8s/deployment-compiler`
- Defining source: `packages/deployment-compiler/src/aws-runtime-access-parity.ts`
- Explain: `applik8s explain RUNTIME_ACCESS_EXECUTION_ROLE_MISBOUND`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-runtime-access-execution-role-missing"></a>

## `RUNTIME_ACCESS_EXECUTION_ROLE_MISSING`

- Owner: `package:@applik8s/deployment-compiler`
- Defining source: `packages/deployment-compiler/src/aws-runtime-access-parity.ts`
- Explain: `applik8s explain RUNTIME_ACCESS_EXECUTION_ROLE_MISSING`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-runtime-access-explicit-redundant"></a>

## `RUNTIME_ACCESS_EXPLICIT_REDUNDANT`

- Owner: `workspace:cross-package`
- Defining sources: `packages/deployment-compiler/src/runtime-access-plan.ts`, `packages/deployment-contract/src/runtime-access.ts`
- Explain: `applik8s explain RUNTIME_ACCESS_EXPLICIT_REDUNDANT`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-runtime-access-explicit-unused"></a>

## `RUNTIME_ACCESS_EXPLICIT_UNUSED`

- Owner: `workspace:cross-package`
- Defining sources: `packages/deployment-compiler/src/runtime-access-plan.ts`, `packages/deployment-contract/src/runtime-access.ts`
- Explain: `applik8s explain RUNTIME_ACCESS_EXPLICIT_UNUSED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-runtime-access-explicit-widening"></a>

## `RUNTIME_ACCESS_EXPLICIT_WIDENING`

- Owner: `workspace:cross-package`
- Defining sources: `packages/deployment-compiler/src/runtime-access-plan.ts`, `packages/deployment-contract/src/runtime-access.ts`
- Explain: `applik8s explain RUNTIME_ACCESS_EXPLICIT_WIDENING`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-runtime-access-iam-missing"></a>

## `RUNTIME_ACCESS_IAM_MISSING`

- Owner: `package:@applik8s/deployment-compiler`
- Defining source: `packages/deployment-compiler/src/aws-runtime-access-parity.ts`
- Explain: `applik8s explain RUNTIME_ACCESS_IAM_MISSING`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-runtime-access-iam-widened"></a>

## `RUNTIME_ACCESS_IAM_WIDENED`

- Owner: `package:@applik8s/deployment-compiler`
- Defining source: `packages/deployment-compiler/src/aws-runtime-access-parity.ts`
- Explain: `applik8s explain RUNTIME_ACCESS_IAM_WIDENED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-runtime-access-network-misbound"></a>

## `RUNTIME_ACCESS_NETWORK_MISBOUND`

- Owner: `package:@applik8s/deployment-compiler`
- Defining sources: `packages/deployment-compiler/src/aws-runtime-access-parity.ts`, `packages/deployment-compiler/src/kubernetes-runtime-access-parity.ts`
- Explain: `applik8s explain RUNTIME_ACCESS_NETWORK_MISBOUND`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-runtime-access-network-missing"></a>

## `RUNTIME_ACCESS_NETWORK_MISSING`

- Owner: `package:@applik8s/deployment-compiler`
- Defining sources: `packages/deployment-compiler/src/aws-runtime-access-parity.ts`, `packages/deployment-compiler/src/kubernetes-runtime-access-parity.ts`
- Explain: `applik8s explain RUNTIME_ACCESS_NETWORK_MISSING`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-runtime-access-network-widened"></a>

## `RUNTIME_ACCESS_NETWORK_WIDENED`

- Owner: `package:@applik8s/deployment-compiler`
- Defining sources: `packages/deployment-compiler/src/aws-runtime-access-parity.ts`, `packages/deployment-compiler/src/kubernetes-runtime-access-parity.ts`
- Explain: `applik8s explain RUNTIME_ACCESS_NETWORK_WIDENED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-runtime-access-network-wrong-port"></a>

## `RUNTIME_ACCESS_NETWORK_WRONG_PORT`

- Owner: `package:@applik8s/deployment-compiler`
- Defining sources: `packages/deployment-compiler/src/aws-runtime-access-parity.ts`, `packages/deployment-compiler/src/kubernetes-runtime-access-parity.ts`
- Explain: `applik8s explain RUNTIME_ACCESS_NETWORK_WRONG_PORT`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-runtime-access-network-wrong-protocol"></a>

## `RUNTIME_ACCESS_NETWORK_WRONG_PROTOCOL`

- Owner: `package:@applik8s/deployment-compiler`
- Defining sources: `packages/deployment-compiler/src/aws-runtime-access-parity.ts`, `packages/deployment-compiler/src/kubernetes-runtime-access-parity.ts`
- Explain: `applik8s explain RUNTIME_ACCESS_NETWORK_WRONG_PROTOCOL`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-runtime-access-provider-guarantee-unsupported"></a>

## `RUNTIME_ACCESS_PROVIDER_GUARANTEE_UNSUPPORTED`

- Owner: `workspace:cross-package`
- Defining sources: `packages/deployment-compiler/src/runtime-access-plan.ts`, `packages/deployment-contract/src/runtime-access.ts`
- Explain: `applik8s explain RUNTIME_ACCESS_PROVIDER_GUARANTEE_UNSUPPORTED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-runtime-access-rbac-missing"></a>

## `RUNTIME_ACCESS_RBAC_MISSING`

- Owner: `package:@applik8s/deployment-compiler`
- Defining source: `packages/deployment-compiler/src/kubernetes-runtime-access-parity.ts`
- Explain: `applik8s explain RUNTIME_ACCESS_RBAC_MISSING`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-runtime-access-rbac-widened"></a>

## `RUNTIME_ACCESS_RBAC_WIDENED`

- Owner: `package:@applik8s/deployment-compiler`
- Defining source: `packages/deployment-compiler/src/kubernetes-runtime-access-parity.ts`
- Explain: `applik8s explain RUNTIME_ACCESS_RBAC_WIDENED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-runtime-access-role-misbound"></a>

## `RUNTIME_ACCESS_ROLE_MISBOUND`

- Owner: `package:@applik8s/deployment-compiler`
- Defining source: `packages/deployment-compiler/src/aws-runtime-access-parity.ts`
- Explain: `applik8s explain RUNTIME_ACCESS_ROLE_MISBOUND`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-runtime-access-role-missing"></a>

## `RUNTIME_ACCESS_ROLE_MISSING`

- Owner: `package:@applik8s/deployment-compiler`
- Defining source: `packages/deployment-compiler/src/aws-runtime-access-parity.ts`
- Explain: `applik8s explain RUNTIME_ACCESS_ROLE_MISSING`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-runtime-access-service-account-mismatch"></a>

## `RUNTIME_ACCESS_SERVICE_ACCOUNT_MISMATCH`

- Owner: `package:@applik8s/deployment-compiler`
- Defining source: `packages/deployment-compiler/src/kubernetes-runtime-access-parity.ts`
- Explain: `applik8s explain RUNTIME_ACCESS_SERVICE_ACCOUNT_MISMATCH`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-runtime-access-target-unresolved"></a>

## `RUNTIME_ACCESS_TARGET_UNRESOLVED`

- Owner: `workspace:cross-package`
- Defining sources: `packages/deployment-compiler/src/runtime-access-plan.ts`, `packages/deployment-contract/src/runtime-access.ts`
- Explain: `applik8s explain RUNTIME_ACCESS_TARGET_UNRESOLVED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-runtime-access-wildcard-forbidden"></a>

## `RUNTIME_ACCESS_WILDCARD_FORBIDDEN`

- Owner: `workspace:cross-package`
- Defining sources: `packages/deployment-compiler/src/runtime-access-plan.ts`, `packages/deployment-contract/src/runtime-access.ts`
- Explain: `applik8s explain RUNTIME_ACCESS_WILDCARD_FORBIDDEN`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-runtime-access-workload-missing"></a>

## `RUNTIME_ACCESS_WORKLOAD_MISSING`

- Owner: `package:@applik8s/deployment-compiler`
- Defining sources: `packages/deployment-compiler/src/aws-runtime-access-parity.ts`, `packages/deployment-compiler/src/kubernetes-runtime-access-parity.ts`
- Explain: `applik8s explain RUNTIME_ACCESS_WORKLOAD_MISSING`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-runtime-incompatible"></a>

## `RUNTIME_INCOMPATIBLE`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/common.ts`
- Explain: `applik8s explain RUNTIME_INCOMPATIBLE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-saga-compensated"></a>

## `SAGA_COMPENSATED`

- Owner: `package:@applik8s/applik8s`
- Defining source: `packages/applik8s/src/application-sagas.ts`
- Explain: `applik8s explain SAGA_COMPENSATED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-saga-compensation-failed"></a>

## `SAGA_COMPENSATION_FAILED`

- Owner: `package:@applik8s/applik8s`
- Defining source: `packages/applik8s/src/application-sagas.ts`
- Explain: `applik8s explain SAGA_COMPENSATION_FAILED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-saga-effect-outside-boundary"></a>

## `SAGA_EFFECT_OUTSIDE_BOUNDARY`

- Owner: `package:@applik8s/applik8s`
- Defining source: `packages/applik8s/src/application-sagas.ts`
- Explain: `applik8s explain SAGA_EFFECT_OUTSIDE_BOUNDARY`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-saga-nesting-unsupported"></a>

## `SAGA_NESTING_UNSUPPORTED`

- Owner: `package:@applik8s/applik8s`
- Defining source: `packages/applik8s/src/application-sagas.ts`
- Explain: `applik8s explain SAGA_NESTING_UNSUPPORTED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-saga-outcome-unknown"></a>

## `SAGA_OUTCOME_UNKNOWN`

- Owner: `package:@applik8s/applik8s`
- Defining source: `packages/applik8s/src/application-sagas.ts`
- Explain: `applik8s explain SAGA_OUTCOME_UNKNOWN`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-schedule-cadence-unrepresentable"></a>

## `SCHEDULE_CADENCE_UNREPRESENTABLE`

- Owner: `package:@applik8s/deployment-compiler`
- Defining source: `packages/deployment-compiler/src/provider-guarantees.ts`
- Explain: `applik8s explain SCHEDULE_CADENCE_UNREPRESENTABLE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-schedule-cardinality-unsupported"></a>

## `SCHEDULE_CARDINALITY_UNSUPPORTED`

- Owner: `package:@applik8s/deployment-compiler`
- Defining source: `packages/deployment-compiler/src/provider-guarantees.ts`
- Explain: `applik8s explain SCHEDULE_CARDINALITY_UNSUPPORTED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-schedule-lateness-unsupported"></a>

## `SCHEDULE_LATENESS_UNSUPPORTED`

- Owner: `package:@applik8s/deployment-compiler`
- Defining source: `packages/deployment-compiler/src/provider-guarantees.ts`
- Explain: `applik8s explain SCHEDULE_LATENESS_UNSUPPORTED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-schedule-misfire-unsupported"></a>

## `SCHEDULE_MISFIRE_UNSUPPORTED`

- Owner: `package:@applik8s/deployment-compiler`
- Defining source: `packages/deployment-compiler/src/provider-guarantees.ts`
- Explain: `applik8s explain SCHEDULE_MISFIRE_UNSUPPORTED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-schedule-precision-unsupported"></a>

## `SCHEDULE_PRECISION_UNSUPPORTED`

- Owner: `package:@applik8s/deployment-compiler`
- Defining source: `packages/deployment-compiler/src/provider-guarantees.ts`
- Explain: `applik8s explain SCHEDULE_PRECISION_UNSUPPORTED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-schedule-provider-unimplemented"></a>

## `SCHEDULE_PROVIDER_UNIMPLEMENTED`

- Owner: `package:@applik8s/deployment-compiler`
- Defining source: `packages/deployment-compiler/src/provider-guarantees.ts`
- Explain: `applik8s explain SCHEDULE_PROVIDER_UNIMPLEMENTED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-schedule-provider-unresolved"></a>

## `SCHEDULE_PROVIDER_UNRESOLVED`

- Owner: `package:@applik8s/deployment-compiler`
- Defining source: `packages/deployment-compiler/src/provider-guarantees.ts`
- Explain: `applik8s explain SCHEDULE_PROVIDER_UNRESOLVED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-schedule-timezone-unsupported"></a>

## `SCHEDULE_TIMEZONE_UNSUPPORTED`

- Owner: `package:@applik8s/deployment-compiler`
- Defining source: `packages/deployment-compiler/src/provider-guarantees.ts`
- Explain: `applik8s explain SCHEDULE_TIMEZONE_UNSUPPORTED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-schema-invalid"></a>

## `SCHEMA_INVALID`

- Owner: `workspace:cross-package`
- Defining sources: `packages/applik8s/src/dns.ts`, `packages/core/src/common.ts`
- Explain: `applik8s explain SCHEMA_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-schema-unsupported"></a>

## `SCHEMA_UNSUPPORTED`

- Owner: `workspace:cross-package`
- Defining sources: `packages/core/src/common.ts`, `packages/sdk/src/schema-runtime.ts`
- Explain: `applik8s explain SCHEMA_UNSUPPORTED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-serialization-failed"></a>

## `SERIALIZATION_FAILED`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/common.ts`
- Explain: `applik8s explain SERIALIZATION_FAILED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-signed-envelope-algorithm-invalid"></a>

## `SIGNED_ENVELOPE_ALGORITHM_INVALID`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/signed-envelope.ts`
- Explain: `applik8s explain SIGNED_ENVELOPE_ALGORITHM_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-signed-envelope-crypto-unavailable"></a>

## `SIGNED_ENVELOPE_CRYPTO_UNAVAILABLE`

- Owner: `package:@applik8s/runtime`
- Defining source: `packages/runtime/src/signed-envelope.ts`
- Explain: `applik8s explain SIGNED_ENVELOPE_CRYPTO_UNAVAILABLE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-signed-envelope-expired"></a>

## `SIGNED_ENVELOPE_EXPIRED`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/signed-envelope.ts`
- Explain: `applik8s explain SIGNED_ENVELOPE_EXPIRED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-signed-envelope-issued-at-invalid"></a>

## `SIGNED_ENVELOPE_ISSUED_AT_INVALID`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/signed-envelope.ts`
- Explain: `applik8s explain SIGNED_ENVELOPE_ISSUED_AT_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-signed-envelope-key-id-invalid"></a>

## `SIGNED_ENVELOPE_KEY_ID_INVALID`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/signed-envelope.ts`
- Explain: `applik8s explain SIGNED_ENVELOPE_KEY_ID_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-signed-envelope-key-invalid"></a>

## `SIGNED_ENVELOPE_KEY_INVALID`

- Owner: `package:@applik8s/runtime`
- Defining source: `packages/runtime/src/signed-envelope.ts`
- Explain: `applik8s explain SIGNED_ENVELOPE_KEY_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-signed-envelope-key-unknown"></a>

## `SIGNED_ENVELOPE_KEY_UNKNOWN`

- Owner: `package:@applik8s/runtime`
- Defining source: `packages/runtime/src/signed-envelope.ts`
- Explain: `applik8s explain SIGNED_ENVELOPE_KEY_UNKNOWN`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-signed-envelope-lifetime-invalid"></a>

## `SIGNED_ENVELOPE_LIFETIME_INVALID`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/signed-envelope.ts`
- Explain: `applik8s explain SIGNED_ENVELOPE_LIFETIME_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-signed-envelope-malformed"></a>

## `SIGNED_ENVELOPE_MALFORMED`

- Owner: `package:@applik8s/runtime`
- Defining source: `packages/runtime/src/signed-envelope.ts`
- Explain: `applik8s explain SIGNED_ENVELOPE_MALFORMED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-signed-envelope-oversized"></a>

## `SIGNED_ENVELOPE_OVERSIZED`

- Owner: `package:@applik8s/runtime`
- Defining source: `packages/runtime/src/signed-envelope.ts`
- Explain: `applik8s explain SIGNED_ENVELOPE_OVERSIZED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-signed-envelope-payload-invalid"></a>

## `SIGNED_ENVELOPE_PAYLOAD_INVALID`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/signed-envelope.ts`
- Explain: `applik8s explain SIGNED_ENVELOPE_PAYLOAD_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-signed-envelope-purpose-invalid"></a>

## `SIGNED_ENVELOPE_PURPOSE_INVALID`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/signed-envelope.ts`
- Explain: `applik8s explain SIGNED_ENVELOPE_PURPOSE_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-signed-envelope-shape-invalid"></a>

## `SIGNED_ENVELOPE_SHAPE_INVALID`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/signed-envelope.ts`
- Explain: `applik8s explain SIGNED_ENVELOPE_SHAPE_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-signed-envelope-signature-invalid"></a>

## `SIGNED_ENVELOPE_SIGNATURE_INVALID`

- Owner: `package:@applik8s/runtime`
- Defining source: `packages/runtime/src/signed-envelope.ts`
- Explain: `applik8s explain SIGNED_ENVELOPE_SIGNATURE_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-signed-envelope-version-invalid"></a>

## `SIGNED_ENVELOPE_VERSION_INVALID`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/signed-envelope.ts`
- Explain: `applik8s explain SIGNED_ENVELOPE_VERSION_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-start-duplicate-package"></a>

## `START_DUPLICATE_PACKAGE`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-start.ts`
- Explain: `applik8s explain START_DUPLICATE_PACKAGE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-start-duplicate-profile"></a>

## `START_DUPLICATE_PROFILE`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-start.ts`
- Explain: `applik8s explain START_DUPLICATE_PROFILE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-start-duplicate-route"></a>

## `START_DUPLICATE_ROUTE`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-start.ts`
- Explain: `applik8s explain START_DUPLICATE_ROUTE`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-start-generator-budget"></a>

## `START_GENERATOR_BUDGET`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-start.ts`
- Explain: `applik8s explain START_GENERATOR_BUDGET`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-start-generator-path"></a>

## `START_GENERATOR_PATH`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-start.ts`
- Explain: `applik8s explain START_GENERATOR_PATH`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-start-generator-unpinned"></a>

## `START_GENERATOR_UNPINNED`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-start.ts`
- Explain: `applik8s explain START_GENERATOR_UNPINNED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-start-identity-invalid"></a>

## `START_IDENTITY_INVALID`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-start.ts`
- Explain: `applik8s explain START_IDENTITY_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-status-patch-failed"></a>

## `STATUS_PATCH_FAILED`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/common.ts`
- Explain: `applik8s explain STATUS_PATCH_FAILED`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-telemetry-baggage-invalid"></a>

## `TELEMETRY_BAGGAGE_INVALID`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-telemetry.ts`
- Explain: `applik8s explain TELEMETRY_BAGGAGE_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-telemetry-envelope-invalid"></a>

## `TELEMETRY_ENVELOPE_INVALID`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-telemetry.ts`
- Explain: `applik8s explain TELEMETRY_ENVELOPE_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-telemetry-identity-invalid"></a>

## `TELEMETRY_IDENTITY_INVALID`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-telemetry.ts`
- Explain: `applik8s explain TELEMETRY_IDENTITY_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.


<a id="diagnostic-telemetry-metric-invalid"></a>

## `TELEMETRY_METRIC_INVALID`

- Owner: `package:@applik8s/core`
- Defining source: `packages/core/src/application-telemetry.ts`
- Explain: `applik8s explain TELEMETRY_METRIC_INVALID`

This diagnostic means the owning contract could not prove a required invariant from the supplied program,
configuration, authority, or observed provider state. Read the structured fields emitted with the diagnostic,
correct the rejected input or provider state at the defining source, and rerun the same bounded command. Do not
bypass the failure with an unscoped side effect. The CLI explanation supplies any more specific authored
remediation available for this code.
