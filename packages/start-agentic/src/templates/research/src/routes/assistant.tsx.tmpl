import { createFileRoute } from '@tanstack/react-router';
import { PublicAssistantView } from '../features/public-assistant-view';

export const Route = createFileRoute('/assistant')({
  component: PublicAssistantView,
});
