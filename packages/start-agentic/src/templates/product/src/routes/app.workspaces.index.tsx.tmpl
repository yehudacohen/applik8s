import { createFileRoute } from '@tanstack/react-router';
import { WorkspaceHome } from '../features/workspaces/view';

export const Route = createFileRoute('/app/workspaces/')({ component: WorkspaceHome });
