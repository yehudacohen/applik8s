import { createFileRoute } from '@tanstack/react-router';
import { ProductCatalogAdministration } from '../features/administration/view';
export const Route = createFileRoute('/admin/catalog')({ component: ProductCatalogAdministration });
