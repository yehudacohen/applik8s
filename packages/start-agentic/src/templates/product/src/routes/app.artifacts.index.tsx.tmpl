import { createFileRoute } from '@tanstack/react-router';
import { ArtifactsView } from '../features/library/view';

export const Route = createFileRoute('/app/artifacts/')({ component: ArtifactsView });
