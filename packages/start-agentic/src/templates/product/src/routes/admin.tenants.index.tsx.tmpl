import { createFileRoute } from '@tanstack/react-router';
import { TenantDirectory } from '../features/administration/view';
export const Route = createFileRoute('/admin/tenants/')({ component: TenantDirectory });
